# 2026-07-20 작업 요약 (Claude)

## TASK-029 R-5 보정 재리뷰 → **통과 · main 머지** (D-072 / A-0029)

**스캔**: `git fetch origin --prune` 후 미머지 `origin/codex/*` = **1건**
(`codex/2026-07-19-task029-language-router`). handoff 최신줄이 `Codex → Claude | 117b4a3` ⇒ **(가) 리뷰 차례**.
나머지 codex 브랜치 전부 main 포함 = (다) 무시.

**리뷰 범위(멱등)**: 보정 커밋 `117b4a3` + 인계기록 `b23506e` 만. (+30/-0, 2파일)

**판정**: **R-5 해소 · 리뷰 통과 · 비민감(킷 테스트 + README) ⇒ Claude main 머지**(D-007).

**독립 재현**: 진입점 **15/15** · dev **102/102** · `mutation-check` **PASS** ·
`selftest --quick` **PASS** · dev↔kit 게이트 **21파일 md5 전량 동일** · `sync-from-dev.sh` **멱등**
(신규 킷 전용 시험 생존 확인).

**적대적 검증**:
- **RIG**: preflight 에서 `LANGUAGE_ROUTING` 항목만 제거 → **14/15 단독 FAIL** + R-4 계열
  `verdict: blocked` 회귀 재현 ⇒ **죽은 가드 → 살아있는 가드** 전환 실증.
- **음성검증**: 기대 rc `2→3` 변조 → 단독 FAIL(항상-PASS 아님).
- **fresh 적대입력**(픽스처 밖 직접 생성): 레거시 override → `exit 2 · 카드 미생성`,
  README 안내대로 정책 복사 후 → `exit 0 · verdict: pass` ⇒ **문서가 실제 탈출구로 작동**.
- **드리프트**: `run.sh:48`·`manifest.yaml:61–66`·README 필수 5종 일치.

**보수적 개발**: 요청 2건만 반영, intent 밖 파일·무관 리팩터 없음, 게이트/정책/배선 불변
⇒ scope-creep·over-reach 없음.

**차기 AC 가드 G-1(비차단 관찰 아님)**: preflight 5항목 중 시험 보유 2개뿐 —
`sensitive-zones`·`sensitive-capabilities`·`approval-routing` 은 R-5 와 동일한 죽은 가드
(`"$ZONES"` 제거 시 `exit 1 / verdict: blocked` 오판정 부활 실증). 선재 갭이라
보정요청 대신 **차기 태스크 AC 로 명문화**: preflight 표 주도 회귀 전환(5항목 각각 3중 단언 + 항목별 RIG).

**TASK-029 종결** — R-1~R-5 전부 해소. **TASK-030(J1 Java 추출기)** 착수 전제 충족.

---

# 2026-07-20 작업 요약

## TASK-029 R-5 보정 재제출

- 무엇을 위해: A-0028/R-5 보정요청에 따라 legacy `--policies` override 디렉터리에 `language-routing.yaml` 이 없을 때 킷이 R-4 계열 `verdict: blocked` 카드로 회귀하지 않고 정직한 분석 실패(exit 2)로 닫히는지 상설 검증하기 위해.
- 무엇을 수정: `kit/tests/run-entrypoint-tests.sh` 에 `language-routing-legacy-override-preflight` 케이스를 추가하고, `kit/README.md` 사용법에 override 필수 정책 5종 및 기존 도입처의 `language-routing.yaml` 복사 안내를 추가했다.
- 무엇을 고침: `kit/run.sh` preflight 목록의 `LANGUAGE_ROUTING` 항목이 제거되어도 기존 테스트가 모두 초록이던 죽은 가드를, 제거 시 신규 케이스가 단독 FAIL 하는 load-bearing 회귀 가드로 전환했다.
- 검증: `kit/tests/run-entrypoint-tests.sh` 15/15 PASS, `tests/run-tests.sh` 102/102 PASS, `git diff --check` PASS, `bash -n kit/tests/run-entrypoint-tests.sh && bash -n kit/run.sh` PASS. 음성검증으로 `LANGUAGE_ROUTING` preflight 항목만 임시 제거 시 신규 케이스 단독 FAIL(14/15) 및 exit 1 `verdict: blocked` 카드 재현 확인 후 원복.
- 브랜치·커밋: `codex/2026-07-19-task029-language-router` · `117b4a3`.

## TASK-030 Java 인벤토리(J1) 구현

- 무엇을 위해: MVP-3 J1 수용기준에 따라 Java/Spring 계열의 함수수준 분석 기반을 Python IR과 같은 형태로 만들고, TASK-029 리뷰에서 이월된 킷 preflight G-1 죽은 가드도 닫기 위해.
- 무엇을 수정: `.harness/gates/extract-java-inventory.py` 를 추가하고 `map-diff-to-functions.py`/`classify-python-function-changes.py` 에 `.java` 인벤토리 소비 경로를 추가했다. Java inner class·constructor·overload·annotation-line·parse_error·비UTF-8 fixtures 와 runner 검증을 추가했다. 킷은 dev 19종 게이트로 동기화하고 manifest/README/selftest/sync 문구를 MVP-3 J1로 갱신했다.
- 무엇을 고침: Java 어노테이션 줄 변경이 `<module>` 로 빠지는 사각을 막고, 오버로드 메서드를 이름 단독으로 매칭해 added/deleted로 오판할 수 있는 경로를 어노테이션 제외 시그니처+occurrence 매칭으로 보정했다. 킷 필수 정책 preflight 5항목 각각을 exit 2·파일명 안내·blocked 카드 미생성으로 고정했다.
- 검증: `tests/run-tests.sh` 107/107 PASS, `kit/selftest.sh --quick` PASS(러너 107/107 + 진입점 18/18), `tests/mutation-check.sh` PASS(178 expectation mutations), `py_compile`·`bash -n`·`git diff --check` PASS, Java inventory JSON md5 2회 동일(`0d8d31fd1c667c231a65ab6baad2804c`). 음성검증으로 preflight 5항목(`ZONES`/`CAPS`/`ROUTING`/`SINKS`/`LANGUAGE_ROUTING`) 제거 사본에서 각 기대 케이스가 FAIL 함을 확인.
- 브랜치·커밋: `codex/2026-07-20-task030-java-inventory` · `4ccb8c9`.

## TASK-030 R-1 보정 재제출

- 무엇을 위해: A-0030/R-1 보정요청에 따라 Java 분석 의존성(`tree_sitter`/`tree_sitter_java`) 부재·불량이 `.java` 포함 diff 전체를 blocked 또는 전역 `files: []` 로 만드는 판정 회귀를 막기 위해.
- 무엇을 수정: dev/kit의 `extract-java-inventory.py`, `map-diff-to-functions.py`, `classify-python-function-changes.py` 에서 `ImportError`·`OSError` 를 Java 파일 단위 `parse_error`/fallback 으로 격리했다. `tests/run-tests.sh` 에 케이스별 `PYTHONPATH` 주입과 fixture 기반 evidence `language_routing` 전달을 추가하고, Java 분석 불가 회귀 픽스처 4건을 추가했다.
- 무엇을 고침: Java 분석이 불가한 환경에서도 `.java` 파일은 coverage/not_checked 및 `parse_error` 로 정직하게 노출되고, 같은 diff 의 Python 함수 매핑·분류는 살아있도록 보정했다. Java-only evidence 는 `exit 0`/`pass` 를 유지하며 `.java` deep semantic analysis stub 문구를 남긴다.
- 검증: 신규 4케이스 PASS, `tests/run-tests.sh` 111/111 PASS, `kit/tests/run-entrypoint-tests.sh` 18/18 PASS, `py_compile` PASS, `git diff --check` PASS, dev↔kit 변경 게이트 3쌍 md5 동일. 음성검증으로 Java 의존성 격리를 임시 제거하면 `java-analysis-unavailable-function-mapping` 이 단독 FAIL 하며 전역 `error` + `files: []` 가 재현됨을 확인 후 원복.
- 브랜치·커밋: `codex/2026-07-20-task030-java-inventory` · `1b11550`.

---

## TASK-030 J1 Java 인벤토리 리뷰 → **보정요청 R-1 · 코드 머지 보류** (D-073 / A-0030)

**스캔**: `git fetch origin --prune` 후 미머지 `origin/codex/*` = **1건**
(`codex/2026-07-20-task030-java-inventory`). handoff 최신줄이 TASK-029 종결(머지 완료)이고
이 브랜치는 미등장 ⇒ **(가) 리뷰 차례**. 나머지 codex 브랜치 전부 main 포함 = (다) 무시.

**판정**: **R-1 보정요청** — 리뷰 기록만 main 머지, 구현 코드 브랜치 보류(D-007).

**독립 재현(전량 수용)**: dev **107/107**(main 102 → Java 5건) · dev↔kit **21파일 md5 전량 동일** ·
manifest **19종 = 실제 19개** · 진입점 단독 **18/18** · AC#1~#5 충족 ·
D-072 이월 AC 가드 **G-1 실제 구현 확인 ✅**.

**적대적 검증**: 픽스처 밖 fresh 입력(신규 오버로드 앞 삽입 + `@PreAuthorize` 인자만 변경 +
무변경 오버로드) 정확 통과. 음성검증 2건 모두 단독 FAIL(106/107) ⇒ 신규 가드 load-bearing.

**🔴 R-1**: tree-sitter 의존 부재·불량 시 `.java` 든 diff 가 전량 BLOCKED.
**main exit 0 PASS vs 브랜치 exit 1 BLOCKED** (판정 회귀 0→1). 진입점 스위트 **브랜치 3/3 FAIL vs
main 3/3 PASS**, 실제 원인은 카드 `reasons` 의 dlopen 아키텍처 불일치. 민감영역 무접촉인데
`governance violation detected` = **허위 사유**. 더해 `ImportError` 가 캐치 튜플 밖이라
`map-diff` 가 `files: []` 를 반환 → **`.java` 하나가 같은 diff 의 Python 함수 매핑 전체를 무력화**.
과차단 + 과소탐지 동시 ⇒ §2B 비차단 불가. 보정안은 AC#4 의 기존 파일단위 격리 규약을
의존 부재까지 넓히는 것(게이트 2파일 + 픽스처).

**비차단 6건**(O-1 익명/로컬 클래스 이름 손실 → TASK-031 AC 고정 픽스처로 명시, O-2~O-7).

**교훈**: 킷 selftest 는 main 도 일부 FAIL 하고 있었다 — 거기서 "환경 노이즈" 로 접었으면 놓쳤다.
**브랜치/main 반복 실행으로 귀책을 가르고, FAIL 요약이 아니라 카드 전문을 본 것**이 결정적이었다.
그다음 환경 재현이 아니라 **import 실패 shim** 으로 옮겨 환경 무관하게 재현했다.

---

## TASK-030 R-1 보정(`1b11550`) 재리뷰 — **R-1 해소 · 리뷰 통과 · main 머지** (Claude)

`origin/codex/*` 미머지 브랜치는 `task030-java-inventory` 1건. handoff 최신이 Codex→Claude 보정 재제출
(`1b11550`)이라 **내 재리뷰 차례**로 분류하고, 보정 커밋만 재리뷰(멱등성).

**결과**: R-1 필수항목 1~5 전량 해소 ⇒ 리뷰 통과, 비민감이라 D-007 로 Claude 가 main 머지.
- 킷 진입점 **브랜치 18/18 PASS**(R-1 당시 3/3 FAIL) vs main 15/15.
- fresh 적대입력(픽스처 밖 독립 repo, 인가 어노테이션 약화 + Python 동반 변경):
  브랜치 카드 ↔ main 카드 **diff 0**, 둘 다 `approval_required`. 사유는 경로 기반이라 파서와 분리.
- 혼합 diff 에서 `.java` 만 `parse_error` 격리, Python 매핑 **생존**(증상 D 해소).
- dev↔kit 게이트 **19/19 md5 전량 동일**.

**교훈 1 — 중복 가드는 개별 rig 로 죽지 않는다.** `#3`(게이트 내부 가드)만 있어도 의존부재가 막혀서
`#1`(map-diff) 을 지워도 스위트가 조용했다. 하마터면 "죽은 가드" 로 오판할 뻔했는데, 실패모드가
다를 뿐이었다 — `#1·#2` 는 *co-located 게이트 파일 결손*(킷 부분배포)을 덮는다. 그 경로를 재현하니
가드 없으면 전역 `ERROR:` 로 파일이 전량 소실됐다. **rig 가 그 가드의 실패모드를 밟았는지부터 확인해야 한다.**

**교훈 2 — 환경 FAIL 은 귀책부터 가른다.** 내 환경엔 `tree_sitter` 가 없어 4건이 FAIL 했지만,
main 을 같은 환경에서 돌려 `selftest` 가 이미 FAIL(101/102)임을 확인하고 **선재 동작 / TASK-030 무관**으로
정리했다. 반대로 그 미설치 환경이 R-1 실패경로를 **shim 없이 재현**하는 최적 조건이기도 했다.

**차기 이월**: O-4(게이트 파일 결손 픽스처 0건 → TASK-031 AC), O-5(카드가 열화/정상 분석을 구분 못함 —
J1 은 java=stub 이라 무해하나 **J2 에서 허위 카드**가 됨 → J2 AC 로 R-1 item 6 승격).

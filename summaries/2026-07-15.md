# 2026-07-15 작업 요약

## 킷 외부 오픈소스 E2E 검증 + 함정 안내 보강 (Claude · D-051)
- **무엇을 위해**: 형 지시 — 실제 오픈소스에 킷을 돌려 실전 검증. click·requests·flask(Py)·gorilla-mux(Go) 4종 얕은 클론, 통제된 변경 시나리오 주입.
- **결과**: Python 3종 × 4시나리오 **12/12 기대 일치**(무해 PASS·subprocess 주입 APPROVAL·범위밖 APPROVAL·frozen BLOCKED). Go 한계 실측 — 경로층 언어무관 ✓, 능력층 `exec.Command` 미탐(알려진 갭 실증).
- **함정 2건 발견**(킷 버그 아님·도입자 혼동): ①change-intent 스키마 top-level 오기 → 전 변경 승인요구(fail-safe 지만 혼란) ②감사카드 기본 출력이 repo 안 → 커밋 오염.
- **처리(상호견제 준수)**: 러너 ⚠ 안내 2건을 Claude 가 구현·실증까지 했으나 run.sh 는 Codex 저자 파일(D-050) — 커밋 시 거버넌스 분류기가 자기작성·자기머지 차단(정당) → **우회 없이 코드 원복**, 스펙만 `collab/answers/A-0017.md` 로 Codex 이월(비차단·TASK-023 동반 권장). 이번 머지는 문서·표시정합만(README 함정 2건 "Codex 구현 대기" 명시 + E2E 결과 절 + TASK-022b ☐→☑ + D-051 + A-0017).
- **교훈**: 같은 파일 두 번째 차단 — 게이트-인접 파일엔 "안내 echo 정도"도 예외 없음을 분류기가 정확히 집행(dogfooding 실증).

## TASK-022b 배포용 킷 교차감사·보강 (Codex)

- 무엇을 위해: D-047 역할 역전에 따라 Claude 작성 `kit/run.sh`의 verdict 조립을 독립 감사하고, 분석 실패 오흡수와 대상 정책 미적용 온보딩 갭을 폐쇄.
- 무엇을 수정: `run.sh` 공통 게이트 실행 래퍼·타임아웃·fail-closed·`--policies`, `selftest.sh` 진입점 검증 호출, `tests/run-entrypoint-tests.sh` fresh 적대검증 5종, `kit/README.md` 사용·실패 계약. 감사 결과 A-0015와 D-048, handoff 누적.
- 무엇을 고침: 게이트 크래시 exit 1/2가 실제 blocked/approval verdict로 오인되던 증상, 비-range에서 능력·정책 축이 조용히 생략되던 coverage 갭, 킷 내장 정책만 적용돼 대상 repo frozen이 보호되지 않던 함정을 해결.
- 검증: `kit/selftest.sh` — 기존 77/77 PASS + 진입점 5/5 PASS + expectation mutation 127건·policy mutation·metamorphic·negative corpus·원본 불변 PASS. `bash -n`, `git diff --check` PASS.
- 브랜치·커밋: `claude/2026-07-15-kit-draft` · 구현 `00bde19` (최종 기록 커밋은 후속).

## TASK-022 sink 등록 스키마 리뷰 (Claude) — **보정요청** · 코드 머지 보류

**대상**: `codex/2026-07-15-task022-sink-registry` (구현 `4651ea6` · 헤드 `44380c0`) — MVP-2 첫 게이트 `extract-sinks.py` + `extract-gov-annotations.py` `@gov(sink=true)` 파싱 확장 + `tests/fixtures/sinks/`. 설계 §3.1(D-044).

**미머지 브랜치 스캔**: `git fetch origin --prune` 후 `origin/main` 미머지 codex 브랜치 = **`codex/2026-07-15-task022-sink-registry` 단 1개**. handoff-log 대조 → 최신 항목이 D-044 "MVP-2 착수"(설계→Codex)뿐, TASK-022 리뷰/보정 이력 없음 → **분류 (가) 내 리뷰 차례**. 나머지 codex/* 25개는 전부 main 머지 완료(무시).

**심층·적대 리뷰 결과 → 보정요청(1건 블로킹)**:
- **통과(실증)**: `run-tests.sh` 79/79. AC #1~5 fresh 적대입력으로 실증 — 옵트인/frozen 자동/registry 등록 멤버십이 설계 레벨차등(D-044a)과 일치, 하위호환 무회귀(비-sink 경로 행동보존), registry 스키마 오류처리(조용한 무시 없음·enforcing 보수), 결정성. 보수적 개발 OK(`policies/*`·Claude 소유 무접촉).
- **🟠 R-1 블로킹**: `merge_gov_annotations` 에 sink 블록이 `if annotation_is_stronger: … else:` **사이에 삽입**돼 `else` 가 sink 여부에 **오결합** → sink=True 인 약한 @gov annotation 의 **reason/owner 유실**. 다중-@gov 적대입력으로 `owner=None` 실증 + 음성검증(원복 시 복원)으로 load-bearing 확인. sink 멤버십은 정확하나 유실 owner=라우팅·reason=감사 → 거버넌스 메타 손실이라 비차단 불가(CLAUDE §2B). 보정: sink 설정 if/else 분리 + 다중-@gov 회귀 픽스처 신설.
- **🟡 R-2 비차단(권장)**: frozen-auto-sink 테스트가 라이브 `policies/sensitive-zones.yaml` 결합(TASK-021 G-broad-1 부류) → fixture-local zones 권장(또는 TASK-023 G-sink-1 이월).
- **비차단 관찰**: O-1 frozen+@gov(sink) 이중 sink(maturity 상이) → TASK-024 강한 maturity 채택 고려. O-2 `normalize_hops` bool 통과.

**산출(리뷰 기록만 main 머지 — 코드 보류)**: `collab/answers/A-0013.md`(보정요청) · `collab/decisions.md` D-045 · `review-notes.md` TASK-022 절 · `handoff-log.md` 보정요청 신호줄. 리뷰 브랜치 `claude/2026-07-15-review`.

**멱등성**: `4651ea6`·`44380c0` 재처리 금지 — 재제출은 보정 델타만 재리뷰.

## TASK-022 sink 등록 스키마
- 무엇을 위해: MVP-2 간접영향 추적의 입력이 될 sink 목록을 판정 변경 없이 결정적으로 산출.
- 무엇을 수정: `.harness/gates/extract-sinks.py` 추가, `extract-gov-annotations.py`의 `@gov` 파서에 `sink` bool 필드 지원 추가, `tests/fixtures/sinks`와 `tests/cases.yaml`/`tests/run-tests.sh`에 회귀 케이스 추가, README 실행 예시 갱신.
- 무엇을 고침: 신규 구현. 옵트인 `@gov(..., sink=True/true)`, frozen zone 함수 자동 sink, sink-registry 등록/검증을 고정하고 일반 `@gov` 및 protected zone 함수는 sink에서 제외되도록 검증.
- 검증: `tests/run-tests.sh` 79/79 PASS, `tests/mutation-check.sh` PASS(기대값 변조 129건 감지), `git diff --check` PASS, `py_compile` PASS, `bash -n` PASS.
- 브랜치·커밋: `codex/2026-07-15-task022-sink-registry` · `4651ea6`

## TASK-022 R-1 보정 재제출
- 무엇을 위해: A-0013/D-045에서 확인된 다중 `@gov` sink 메타데이터 유실을 폐쇄.
- 무엇을 수정: `extract-gov-annotations.py`의 sink 누적을 annotation 강도 병합 `if/else` 밖으로 분리하고, `tests/fixtures/gov-annotations/multi_sink.py` 및 reason/owner/sink 단언을 추가.
- 무엇을 고침: 약한 `sink=true` annotation에서 잘못 결합된 `else` 때문에 reason/owner backfill이 건너뛰어지던 제어흐름을 복원. sink 추출 구조와 정책은 변경하지 않음.
- 검증: `tests/run-tests.sh` 80/80 PASS, `python3 -m py_compile` PASS, `bash -n tests/run-tests.sh` PASS, `git diff --check` PASS.
- 브랜치·커밋: `codex/2026-07-15-task022-sink-registry` · `f18cc32`

## TASK-022 R-1 재리뷰 (Claude) — **통과 · Claude main 머지 · TASK-022 완결** (D-046)
- **미머지 브랜치 스캔**: `git fetch origin --prune` 후 `origin/main` 미머지 codex 브랜치 = `codex/2026-07-15-task022-sink-registry` **단 1개**(나머지 25개 머지완료). handoff 대조 → 최신이 Claude 보정요청이나 브랜치 헤드가 `44380c0` 이후 `f18cc32`·`95b4399` **신규 델타 존재** = Codex 보정 재제출 = **분류 (가) 내 재리뷰 차례**(델타만, 멱등성).
- **무엇을 재리뷰**: 보정 델타 `f18cc32` — `merge_gov_annotations` else 오결합(R-1/D-045) 수정 + 회귀 픽스처 신설. 원 구현·`extract-sinks.py`·sinks 픽스처는 D-045 통과분이라 재론 안 함.
- **심층·적대 재검증 → 통과**: A-0013 요청 보정 ①(제어흐름 분리)·②(다중-@gov 상설 회귀 픽스처) 모두 채택. `run-tests.sh` 80/80. **음성검증**(픽스→버그형태 원복→신규 케이스 단독 FAIL·`reason=None owner=None` 재현) = 회귀 픽스처 load-bearing. **fresh 적대입력 3종**(3중@gov·최강측 sink·sink 전무 하위호환) 정상. 하류 TASK-024 라우팅 owner/reason 보존 확인. 보수적 개발 OK(경계 준수).
- **머지**: 비민감(하네스 sink 추출·판정무변경 D-044·게이트 계열 Claude 머지 선례) → 구현자(Codex)≠머지자(Claude)로 Claude `main` 머지·push. **TASK-022 완결 → 다음 TASK-023**(착수 시 R-2 G-sink-1 결정적 고정 이월 처리).
- **산출**: `collab/answers/A-0014.md`(통과) · `collab/decisions.md` D-046 · `review-notes.md` TASK-022 R-1 재리뷰 절 · `handoff-log.md` 통과·머지완료 신호줄. 리뷰 브랜치 `claude/2026-07-15-review`.

## TASK-022b 킷 최종리뷰 (Claude) — **보정요청 1건(🟡 R-1)** · 코드 머지 보류 (D-049)
- **대상**: `claude/2026-07-15-kit-draft` 헤드 `88a7d4e` — 리뷰 초점 = Codex 보강 델타 `00bde19`(역할역전 계약 D-047: verdict-affecting 셸 Codex 저자 → Claude 최종리뷰). 워크트리 격리 체크아웃으로 리뷰.
- **통과 확인(실증)**: 스냅샷 충실성(13종=manifest 명시·12/13 dev 동일·드리프트/`extract-sinks` 부재는 선언범위 `0.1-mvp1.5` 그대로·selftest 심볼릭 링크로 킷 게이트 검증) / 판정 조립(차단>승인>통과, L2·메타 exit{0,2}만 방출이라 "2·3층 자동차단 금지" 구조 보장) / 분석실패 정직성(fresh 적대 8종: Traceback·비-range·exit1 위장·차단+실패 동시→차단 우선·intent 부재→fail-closed 차단카드) / 선언 수치 전부 재현(selftest 77/77·진입점 5/5·mutation 127·dev 80/80) / 음성검증 2종(사보타주→진입점 테스트 FAIL = load-bearing).
- **🟡 R-1(블로킹·`00bde19` 도입 회귀)**: `run_gate()` watcher 자식 `sleep` 이 kill 후 생존, 상속 stdout 파이프 점유 → **파이프 캡처 호출(CI·spine)마다 EOF 지연 = GATE_TIMEOUT 전액**(기본 60s 실측·3s 설정 시 4s 비례·fd 분리 1줄 패치 사본에서 0s+타임아웃 감지 보존 실증). 기존 테스트는 전 케이스 timeout=1 이라 가려짐. 보정 = ①watcher `>/dev/null 2>&1` 1줄 ②파이프-지연 회귀가드 테스트 1건.
- **비차단**: O-a "의도이탈 층은 생략" 문구 부정확(실동작=fail-closed 차단) / O-b bash3.2+intent 부재 빈배열 즉사 / O-c 분석실패 시 카드 비-YAML / O-d sync 체크섬 검증(A-0015 O-1 승인)은 MVP-2 킷 재생성 AC 채택.
- **산출**: `collab/answers/A-0016.md`(보정요청) · D-049 · `review-notes.md` TASK-022b 절 · handoff 맨 위 보정요청 신호줄. 코드 브랜치 머지 보류, 리뷰기록만 main. 리뷰 브랜치 `claude/2026-07-15-review`.

## TASK-022b R-1 보정 재제출 (Codex)

- 무엇을 위해: A-0016/D-049에서 확인된 watcher 고아 `sleep`의 stdout 파이프 점유로 명령치환·CI 호출이 기본 timeout 전액을 기다리던 회귀를 제거.
- 무엇을 수정: `kit/run.sh` watcher 서브셸의 stdout/stderr를 `/dev/null`로 분리하고, `kit/tests/run-entrypoint-tests.sh`에 timeout 30초인 빠른 정상 호출의 파이프 캡처가 10초 미만에 끝나는 회귀가드 추가.
- 무엇을 고침: watcher 서브셸 kill 뒤 자식 `sleep`이 상속 파이프 write end를 유지해 EOF를 지연하던 원인을 폐쇄. 판정 조립·우선순위·`--policies`는 변경하지 않음.
- 검증: `kit/tests/run-entrypoint-tests.sh` 6/6 PASS, `tests/run-tests.sh` 80/80 PASS, `bash -n kit/run.sh kit/tests/run-entrypoint-tests.sh` PASS, `git diff --check` PASS.
- 브랜치·커밋: `claude/2026-07-15-kit-draft` · `f1bf533`

## TASK-022b R-1 재리뷰 (Claude) — **통과 · Claude main 머지 · TASK-022b 완결 (킷 v0 수용)** (D-050)
- **미머지 스캔**: `git fetch origin --prune` 후 `origin/codex/*` 미머지 0개. handoff 대조 — 최신이 Claude 보정요청(D-049)이나 코드 브랜치 `claude/2026-07-15-kit-draft` 헤드가 `88a7d4e` 이후 `f1bf533`·`39c2285` 신규 델타 = **Codex 보정 재제출 = 분류 (가) 내 재리뷰 차례**(멱등성: 델타만).
- **재리뷰 → 통과**: A-0016 처방 ①(watcher fd 분리 1줄)·②(파이프-지연 회귀가드, timeout=30 명시) 모두 채택. 진입점 6/6·selftest 77+mutation127 재현. **fresh**(합성 repo·기본 60s 타임아웃 파이프 캡처) elapsed 0s·pass. **rig-and-revert**: fd 분리 원복 → 가드 단독 FAIL(5/6·elapsed 31s)·기존 5케이스는 못 봄 = 가드 load-bearing. 타임아웃 감지 보존. 보수적 개발 OK.
- **머지**: 비민감(신규 kit/ 스냅샷·기존 게이트/정책/dev 무변경·D-047 판정 유지) → 구현자(Codex 델타)≠머지자(Claude)로 Claude `main` 머지·push. 충돌 3건(decisions/handoff/summaries) 양측 보존·시간역순 해소. Codex 의 "재제출 전 origin/main merge" 미이행은 머지자 해소로 흡수(비차단).
- **산출**: D-050 · `review-notes.md` TASK-022b R-1 재리뷰 절 · `collab/answers/A-0016.md` §해소 · handoff 통과·머지완료 신호줄. 리뷰 브랜치 `claude/2026-07-15-review`. **다음 = MVP-2 본선 TASK-023(콜그래프 빌더) Codex 착수.**

## TASK-023 intra-repo 정적 콜그래프 빌더 (Codex)
- 무엇을 위해: MVP-2 간접 영향 추적의 입력이 될 repo 내부 Python 함수 호출 그래프를 판정 미연결 산출물로 결정적으로 생성.
- 무엇을 수정: `.harness/gates/extract-callgraph.py` 신규 추가, `tests/fixtures/callgraph/`와 `tests/cases.yaml`/`tests/run-tests.sh`에 노드·엣지·미해소 호출 coverage·결정성 검증 추가, `.harness/gates/README.md`와 `README.md` 실행 예시 갱신.
- 무엇을 고침: 신규 구현. 정적/별칭 import/from-import/동일 모듈 호출 중 repo 내부 함수로 해소되는 호출만 edge 로 기록하고, `getattr(... )()` 같은 동적 호출은 조용히 버리지 않고 `unresolved_calls`/`coverage.unevaluated` 로 노출한다. 조건부 동명 def 는 동일 id 중복 노드와 합집합 edge 로 보수 산출한다.
- 동반작업: G-sink-1 — `tests/fixtures/sinks/sensitive-zones.yaml`를 추가하고 sink 케이스가 fixture-local policy 를 쓰게 해 라이브 `policies/sensitive-zones.yaml` 결합을 제거. A-0017 — `kit/run.sh`에 change-intent top-level/빈 allowed_paths 및 상대경로 감사카드 출력 `⚠` 안내를 판정 무영향 echo 로 구현하고 진입점 회귀 3케이스를 추가.
- 검증: `tests/run-tests.sh` 81/81 PASS, `kit/selftest.sh --quick` PASS(러너 77/77 + 진입점 9/9), `tests/mutation-check.sh` PASS(기대값 변조 131건·policy mutation·metamorphic·negative corpus·원본 불변), `python3 -m py_compile .harness/gates/extract-callgraph.py`, `bash -n`, `git diff --check` PASS.
- 브랜치·커밋: `codex/2026-07-15-task023-callgraph` · `2a6cd09`

## TASK-023 R-1 보정 재제출 (Codex)
- 무엇을 위해: A-0018/D-052에서 확인된 bare 모듈함수 호출의 동명 클래스메서드 오해소를 폐쇄.
- 무엇을 수정: `extract-callgraph.py`의 `visible_local_names()`에서 클래스명 후보 확장 루프를 제거하고, `tests/fixtures/callgraph/repo/app/overloads.py`와 기대 엣지를 추가.
- 무엇을 고침: 같은 모듈에 `foo`와 `C.foo`가 공존할 때 bare `foo()`가 사전순 때문에 `C.foo`로 잘못 연결되어 실제 caller→sink 엣지가 유실될 수 있던 문제를 해결. 회귀 픽스처가 `app.overloads.bar -> app.overloads.foo`를 고정한다.
- 검증: `TEST_CASE_NAME=callgraph-repo-static tests/run-tests.sh` 1/1 PASS, `tests/run-tests.sh` 81/81 PASS, `python3 -m py_compile .harness/gates/extract-callgraph.py` PASS, `bash -n tests/run-tests.sh` PASS, `git diff --check` PASS.
- 브랜치·커밋: `codex/2026-07-15-task023-callgraph` · `df7e54c`

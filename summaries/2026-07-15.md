# 2026-07-15 작업 요약

## TASK-022 sink 등록 스키마 리뷰 (Claude) — **보정요청** · 코드 머지 보류

**대상**: `codex/2026-07-15-task022-sink-registry` (구현 `4651ea6` · 헤드 `44380c0`) — MVP-2 첫 게이트 `extract-sinks.py` + `extract-gov-annotations.py` `@gov(sink=true)` 파싱 확장 + `tests/fixtures/sinks/`. 설계 §3.1(D-044).

**미머지 브랜치 스캔**: `git fetch origin --prune` 후 `origin/main` 미머지 codex 브랜치 = **`codex/2026-07-15-task022-sink-registry` 단 1개**. handoff-log 대조 → 최신 항목이 D-044 "MVP-2 착수"(설계→Codex)뿐, TASK-022 리뷰/보정 이력 없음 → **분류 (가) 내 리뷰 차례**. 나머지 codex/* 25개는 전부 main 머지 완료(무시).

**심층·적대 리뷰 결과 → 보정요청(1건 블로킹)**:
- **통과(실증)**: `run-tests.sh` 79/79. AC #1~5 fresh 적대입력으로 실증 — 옵트인/frozen 자동/registry 등록 멤버십이 설계 레벨차등(D-044a)과 일치, 하위호환 무회귀(비-sink 경로 행동보존), registry 스키마 오류처리(조용한 무시 없음·enforcing 보수), 결정성. 보수적 개발 OK(`policies/*`·Claude 소유 무접촉).
- **🟠 R-1 블로킹**: `merge_gov_annotations` 에 sink 블록이 `if annotation_is_stronger: … else:` **사이에 삽입**돼 `else` 가 sink 여부에 **오결합** → sink=True 인 약한 @gov annotation 의 **reason/owner 유실**. 다중-@gov 적대입력으로 `owner=None` 실증 + 음성검증(원복 시 복원)으로 load-bearing 확인. sink 멤버십은 정확하나 유실 owner=라우팅·reason=감사 → 거버넌스 메타 손실이라 비차단 불가(CLAUDE §2B). 보정: sink 설정 if/else 분리 + 다중-@gov 회귀 픽스처 신설.
- **🟡 R-2 비차단(권장)**: frozen-auto-sink 테스트가 라이브 `policies/sensitive-zones.yaml` 결합(TASK-021 G-broad-1 부류) → fixture-local zones 권장(또는 TASK-023 G-sink-1 이월).
- **비차단 관찰**: O-1 frozen+@gov(sink) 이중 sink(maturity 상이) → TASK-024 강한 maturity 채택 고려. O-2 `normalize_hops` bool 통과.

**산출(리뷰 기록만 main 머지 — 코드 보류)**: `collab/answers/A-0013.md`(보정요청) · `collab/decisions.md` D-045 · `review-notes.md` TASK-022 절 · `handoff-log.md` 보정요청 신호줄. 리뷰 브랜치 `claude/2026-07-15-review`.

**멱등성**: `4651ea6`·`44380c0` 재처리 금지 — 재제출은 보정 델타만 재리뷰.

## TASK-022 sink 등록 스키마
- 무엇을 위해: MVP-2 간접영향 추적의 입력이 될 sink 목록을 판정 변경 없이 결정적으로 산출.
- 무엇을 수정: `.harness/gates/extract-sinks.py` 추가, `extract-gov-annotations.py`의 `@gov` 파서에 `sink` bool 필드 지원 추가, `tests/fixtures/sinks`와 `tests/cases.yaml`/`tests/run-tests.sh`에 회귀 케이스 추가, README 실행 예시 갱신.
- 무엇을 고침: 신규 구현. 옵트인 `@gov(..., sink=True/true)`, frozen zone 함수 자동 sink, sink-registry 등록/검증을 고정하고 일반 `@gov` 및 protected zone 함수는 sink에서 제외되도록 검증.
- 검증: `tests/run-tests.sh` 79/79 PASS, `tests/mutation-check.sh` PASS(기대값 변조 129건 감지), `git diff --check` PASS, `py_compile` PASS, `bash -n` PASS.
- 브랜치·커밋: `codex/2026-07-15-task022-sink-registry` · `4651ea6`

## TASK-022 R-1 보정 재제출
- 무엇을 위해: A-0013/D-045에서 확인된 다중 `@gov` sink 메타데이터 유실을 폐쇄.
- 무엇을 수정: `extract-gov-annotations.py`의 sink 누적을 annotation 강도 병합 `if/else` 밖으로 분리하고, `tests/fixtures/gov-annotations/multi_sink.py` 및 reason/owner/sink 단언을 추가.
- 무엇을 고침: 약한 `sink=true` annotation에서 잘못 결합된 `else` 때문에 reason/owner backfill이 건너뛰어지던 제어흐름을 복원. sink 추출 구조와 정책은 변경하지 않음.
- 검증: `tests/run-tests.sh` 80/80 PASS, `python3 -m py_compile` PASS, `bash -n tests/run-tests.sh` PASS, `git diff --check` PASS.
- 브랜치·커밋: `codex/2026-07-15-task022-sink-registry` · `f18cc32`

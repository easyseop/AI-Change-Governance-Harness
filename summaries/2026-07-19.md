# 2026-07-19 작업 요약

## Claude — TASK-034 보정(`9ecad20`) 재리뷰 **통과** · `main` 머지 (D-068)

**미머지 브랜치 스캔**: `origin/codex/*` 35개 중 미머지는 `codex/2026-07-16-task034-intent-not-declared` **1개**. `main` handoff 최신은 "보정요청"이었으나 브랜치에 **보정 커밋 `9ecad20`·`c5a04e3` 가 이미 올라와 있어 (가) 리뷰 차례**로 분류(멱등 — `22fe433`·`a4037ce` 재처리 금지).

**판정: 통과 · 비민감 → Claude `main` 머지**(D-007, 구현자 Codex ≠ 머지자). D-067 의 **R-1·R-2 둘 다 실증 해소.**

- **R-1 해소**(미선언이 1층 frozen 차단 무력화): 카드 전체 대체 경로(`intent_not_declared_evidence()`) 삭제 → 의도층만 `not_declared` 로 두고 민감경로·@gov 층은 계속 실행. **fresh 적대 실증(pre-fix 대비)**: `services/settlement/**`+미선언 **exit 2 → 1** · `@gov(frozen)` 함수+미선언 **2 → 1** · 무해+미선언 **2**(AC#1) · 빈 선언은 `status: fail`(미선언과 구분). **E2E `kit/run.sh` = 🔴 BLOCKED(exit 1)·2/3/메타층 전부 실행** → 배포 최전선 전파 확인.
- **R-2 해소**(카드 허위): 미선언 카드가 실제 `base_commit`·`policy_sha`·`files_changed: 1`·`frozen_touched`(사유 포함)·`checked: [check-sensitive-zones, check-function-gov-level]`·`in_allowed_paths: null` 을 실는다. 승인자가 **정산 변경 사실을 카드에서 직접 본다.** 허위 픽스처 계약(`files_changed: 0`)도 실제값으로 정정 + 상설 회귀 2종 신설.
- **음성검증(rig-and-revert)**: RIG-1(판정층에 R-1 재유입) → frozen-zone 케이스 **단독 FAIL(97/98)** · RIG-2(`in_allowed_paths` 위조 재유입) → 미선언 **3케이스 전부 FAIL(95/98)** ⇒ 신규 가드 **load-bearing**.
- **재현**: dev **98/98** · mutation **165** · 킷 진입점 **13/13** · `kit/selftest.sh` **PASS** · dev↔kit 게이트 **16종 md5 동일** + 킷 레이아웃 실작동. **AC 6/6.**
- **보수적 개발**: scope-creep·over-reach 없음. D-067 의 over-reach(판정 완화)가 제거되고 **차단 복원(안전 방향)** 으로 전환.
- **비차단 관찰**: **O-F(선재)** `kit/tests/run-tests.sh` 가 게이트를 `.harness/gates/*` 로 잡아 킷 단독 실행 시 0/98 — `main` 도 동일이라 회귀 아님, 차기 킷 태스크에서 해소/명시 권고. **O-G(선재)** 분석 실패 일반 경로가 `blocked` — 과탐 방향이라 무해.

**산출**: `collab/answers/A-0025.md` · `collab/decisions.md` D-068 · `review-notes.md` · `TASKS.md`(TASK-034 ☑ 완결).
**다음**: **MVP-3(TASK-029~032)** 착수 가능.

## TASK-034 R-1/R-2 보정 재제출
- 무엇을 위해: A-0024 보정요청에 따라 `change-intent.yaml` 미선언이 frozen 경로와 `@gov(frozen)` 차단을 무력화하는 under-detection 을 제거.
- 무엇을 수정: `generate-change-evidence` dev/kit 게이트가 미선언 예외로 카드 전체를 대체하지 않고, 의도층만 `intent_check.status: not_declared` 로 표시한 뒤 sensitive-zones 와 function-gov 검사를 계속 실행하게 보정. 미선언 `changed_files[].in_allowed_paths` 는 `null` 로 출력.
- 무엇을 고침: 기존 미선언 카드가 `files_changed: 0`, `changed_files: []`, `sensitive_zone_check: not_checked` 를 내어 실제 변경과 frozen 접촉을 숨기던 문제를 수정. `missing-intent + frozen zone` 및 `missing-intent + @gov(frozen)` 회귀 케이스를 dev/kit 테스트에 추가.
- 검증: `tests/run-tests.sh` 98/98 PASS, `tests/mutation-check.sh` 165 mutations PASS, `kit/tests/run-entrypoint-tests.sh` 13/13 PASS, `kit/selftest.sh` PASS, `python3 -m py_compile`, `git diff --check` PASS.
- 브랜치·커밋: `codex/2026-07-16-task034-intent-not-declared` / `9ecad20`.

## TASK-029 다국어 어댑터 seam/J0 구현
- 무엇을 위해: MVP-3 Java/Spring 우선 다국어 확장 착수를 위해 공통 IR 계약, 확장자 라우터, tree-sitter 의존 스모크, 미지원 확장자 coverage 노출 기반을 추가.
- 무엇을 수정: `.harness/gates/language-router.py`, `check-tree-sitter-languages.py`, `language-ir.schema.yaml`, `policies/language-routing.yaml`, `requirements.txt` 를 추가. `generate-change-evidence.py` 는 stub/unsupported deep-analysis coverage 를 카드 `not_checked` 에 반영. `extract-python-inventory.py` 는 `lang` 및 공통 IR 보조 필드(`signature_start_line`, `signature`, `annotations`)를 추가. 테스트 러너·fixtures·`tests/parity/` 훅과 README 실행 안내도 갱신.
- 무엇을 고침: 신규. Java/TS 같은 다음 단계 stub 확장자와 Go 같은 미지원 확장자가 조용히 분석된 것처럼 보이지 않도록 감사카드 coverage 에 드러낸다. Python 기존 AST 로직은 재작성하지 않고 Python-only 카드 coverage 계약은 유지.
- 검증: `tests/run-tests.sh` 101/101 PASS, `tests/mutation-check.sh` 171 mutations PASS, `python3 -m py_compile`, `git diff --check` PASS. 음성검증으로 `not_checked` 결합 제거 시 `evidence-language-coverage` 단독 FAIL 확인 후 원복.
- 브랜치·커밋: `codex/2026-07-19-task029-language-router` / `d1dbdca`.

## TASK-029 R-1/R-2/R-3 보정 재제출
- 무엇을 위해: A-0026 보정요청에 따라 인벤토리 회귀 가드 약화, language-routing 정책 부재 무음 통과, 킷 J0 산출물 누락을 보정.
- 무엇을 수정: `validate_inventory` 를 전체 `items` 동등비교로 되돌리고 Python inventory 기대값에 공통 IR 필드를 명시. `generate-change-evidence` 는 명시된 language-routing 정책 경로가 없으면 `blocked(1)` 및 coverage/reasons 흔적을 출력. 킷에는 J0 게이트·IR 스키마·language-routing 정책·requirements·테스트를 동기화하고 manifest/README/sync 스크립트를 18종 기준으로 갱신.
- 무엇을 고침: 유령 inventory 항목이 초과되어도 통과하던 무음 회귀와 `.java`/`.go` coverage 가 정책 파일 누락 시 사라지던 조용한 통과를 제거. dev↔kit 핵심 파일 md5 동일성을 회복.
- 검증: `tests/run-tests.sh` 102/102 PASS, `git diff --check` PASS, `python3 -m py_compile` PASS. 음성검증: 유령 inventory 항목 주입 시 `python-inventory` 단독 FAIL, language-routing missing 분기 무력화 시 `evidence-language-policy-missing` 단독 FAIL 후 원복. dev↔kit 핵심 7쌍 md5 동일 확인.
- 브랜치·커밋: `codex/2026-07-19-task029-language-router` / `f734355`.

# 2026-07-02 작업 요약

### [TASK-007 리뷰 — 보정요청] Claude · claude/2026-07-02-review
- 무엇을 위해: Codex TASK-007(`classify-python-function-changes.py`, impl `0502589`) 산출물을 수용기준 5개 + COMMON-RULES §1 보수적 개발 평가축 대비 심층·적대적 리뷰(CLAUDE.md §2B).
- 무엇을 검증: `run-tests.sh` 10/10 PASS·exit 0 재현 + **픽스처 밖 fresh repo ×3** 독립검증(고정 적대 세트: 데코레이터 인자 변경·`@overload` 동명·조건부 def·property/setter·occurrence-shift) + 음성검증(기대값 변조→FAIL 9/10·exit 1) + 결정성(md5 동일) + 리네임/불량 ref/three-dot 에러 경로.
- 판정: **보정요청 (D-013, 머지 보류)** — 🔴 R-1: diff 에 리네임(R) 1건만 있어도 전체 출력이 `error`+`files:[]` 로 붕괴해 무관한 M 파일 분류까지 소실(AC1·AC3 위반) + host `diff.renames` config 의존(AC4 결정성 위반). 보정요청서 `collab/answers/A-0001.md`(수정: `--no-renames` 또는 R/C 파일 단위 fallback + 형제 파일 보존 + 회귀 픽스처). AC2·🔴AC5(동명 매칭 가드)는 실증 통과 — 국소 수정 후 신속 재리뷰 예정.
- 파생 조치: **TASK-012 AC 가드 보강**(TASKS.md) — 모듈레벨 변경은 TASK-007 출력에 비가시이므로 함수분류 단독 판정 금지·TASK-006 `<module>` 매핑 병합 필수·`fallback:true` 파일 보수 취급.
- 기록: `review-notes.md` TASK-007 절(상세), `collab/decisions.md` D-013, `collab/answers/A-0001.md`, `collab/handoff-log.md` 인계, TASKS.md 가드.
- 브랜치·커밋: `claude/2026-07-02-review` (아래 커밋 참조). 머지 없음(보정요청).

### [TASK-007 보정 재제출] Codex · codex/2026-07-02-task007-rename-fix
- 무엇을 위해: D-013/A-0001 보정요청의 R-1(리네임 감지 시 전체 출력 `error`+`files:[]` 붕괴, host `diff.renames` 환경 의존)을 고쳐 TASK-007을 재제출.
- 무엇을 수정: `classify-python-function-changes.py`를 최신 `main` 기준 브랜치에 추가하고 `git diff --name-status --no-renames`로 R/C를 A+D 파일 fallback 경로로 정규화. `tests/run-tests.sh`에 TASK-007 검증 분기와 삭제 파일 반영 fixture 준비를 추가.
- 무엇을 고침: 리네임 파일은 `file_added_or_deleted` fallback으로 안전 표시하고, 같은 diff의 평범한 M 파일(`app/companion.py`) 함수 변경은 소실 없이 분류되도록 회귀 fixture/기대값을 추가.
- 검증: `bash tests/run-tests.sh` → 10/10 PASS. 별도 fresh repo에서 `diff.renames=true/false` 설정을 바꿔도 classifier JSON diff 없음 확인.
- 브랜치·커밋: `codex/2026-07-02-task007-rename-fix` / `2243173`.

### [TASK-007 재리뷰 — 통과·머지] Claude · claude/2026-07-02-review
- 무엇을 위해: Codex 의 A-0001(R-1) 보정 재제출(`codex/2026-07-02-task007-rename-fix`, impl `2243173`)을 심층·적대적 재리뷰(CLAUDE.md §2B)하고 D-007 머지 판정.
- 무엇을 검증: ① 구 리뷰본(`0502589`) 대비 전체 트리 diff 로 델타 동일성(게이트 +7줄: `--no-renames`+R/C fallback 방어 분기) ② fresh repo 에서 **구 게이트 붕괴 재현 ↔ 신 게이트 형제 M 파일 분류 보존** ③ `diff.renames=true/false` md5 동일(환경 의존 제거) ④ 10/10 PASS·음성검증(기대 변조→FAIL 9/10·exit 1)·결정성 ⑤ 고정 적대 세트 재실행(데코레이터 인자 변경·property/setter·`@overload`×3·조건부 동명 def — 전부 정확).
- 판정: **리뷰통과 (D-014)**. 비민감(분석 전용 게이트·테스트 하네스) → Claude 가 main 머지·push (구현자≠머지자). **MVP-1 Phase A 3/3 게이트 완료.**
- 재리뷰 신규 발견(비차단 → **TASK-013 AC 가드 2건 신설**, TASKS.md): ① 리네임 회귀 픽스처 무력 — 초소형+내용 수정이라 git 유사도 감지 불가, `--no-renames` 제거해도 스위트 통과(실증) → 순수 리네임(내용 동일, R100 항상 감지) 쌍 추가 요구. ② 비-UTF8 `.py` → `UnicodeDecodeError` 전역 붕괴(R-1 동류, 실증) → 파일별 예외 격리 + 픽스처 요구.
- 기록: `review-notes.md` TASK-007 재리뷰 절, `collab/decisions.md` D-014, `collab/handoff-log.md` 인계, TASKS.md(TASK-007 ☑·TASK-013 가드).
- 브랜치·커밋: `claude/2026-07-02-review` — 구현 브랜치 머지 반입 + 리뷰 기록 커밋 → main 머지·push.

# 2026-07-16 작업요약 (누적)

## TASK-023 R-3 보정 재제출 재재재리뷰 — R-3 해소 확인 · **통과 · Claude main 머지** (MVP-2 콜그래프 빌더 완결) · D-055

**맥락**: `git fetch --prune` 후 미머지 `origin/codex/*` 는 `2026-07-15-task023-callgraph` 하나. 브랜치 헤드 `456a965` 에 R-3 보정 impl `5c58b2c` 가 main(`49c39dd`) 위에 얹혀 있어 = **Codex 재제출 = Claude 재리뷰 차례**. 멱등성상 **R-3 델타(코드 부분)만** 재리뷰(R-2 bare 해소·엔진 본체·getattr·조건부 union·결정성·kit 는 D-052~054 통과 확정).

**R-3 해소 확인 ✓ (A-0020 권장 ①+③ 문자 그대로 채택)**:
- `extract-callgraph.py` `build_module_locals`(285–286): `if "." in node["name"]: continue` **2줄** — 클래스메서드(`C.sink`)·중첩 def(`Outer.Inner.deep`) 를 `module.<basename>` 후보서 제외. `module_locals["app.overloads.sink"]=["app.overloads.sink"]`(모듈함수 단독). Python 의미상 `mod.<name>`=모듈 속성=최상위 함수만이므로 메서드 등록은 항상 오류 → 제외가 정론. 메서드는 `definitions` 직접경로(220–221)로 여전히 `mod.C.sink` 해소(무손실).
- 픽스처 `overloads.py`(+7): 자기-import `from app import overloads as overloads_module` + module-qualified caller `qualified_bar(): overloads_module.sink()`. `cases.yaml`(+4): 노드 `app.overloads.qualified_bar`·엣지 `qualified_bar -> app.overloads.sink`. → §2B 동명 오버로드 **3변형 상설화 완성**(bare-module R-1 · bare-method R-2 · module-qualified R-3).

**적대·음성검증 (격리 worktree `wt-task023`)**:
- `tests/run-tests.sh` **81/81 PASS** · `py_compile` OK.
- **fresh 적대(픽스처 밖) 4종 전부 정답**: repoB(`mod.sink()`, `class C` 를 `def sink` **앞**에 두어 정렬운 배제)→`app.mod.sink` · **repoD(governed `reports.export` settlement vs `Report.export`)**→`app.reports.export`(**민감 엣지 보존 = TASK-024 역도달 구멍 복구**) · repoE(대상 메서드만 존재)→`unresolved`(거짓엣지 없음·조용한 오염 폐쇄) · repoF(깊은중첩 `Outer.Inner.deep` vs 모듈 `deep`)→모듈 `deep`.
- **rig-and-revert 음성검증**: 2줄 가드 제거 → 4종이 각각 `C.sink`/`Report.export`/`C.onlym`/`Outer.Inner.deep` 로 **오염 재현**(fresh 입력 비-rigged·버그 실재 확인) + `callgraph-repo-static` **단독 FAIL(0/1)**; 원복 81/81 = 수정·픽스처 load-bearing(항상-PASS 아님).

**보수적 개발(§1)**: 코드 델타 게이트 2줄 + 픽스처 7줄 + cases 4줄 = 수정계약 정확 준수. bare 경로·getattr·union·판정로직 무변경. `policies/*`·`docs/*`·`CLAUDE.md`·`templates/*` name-only diff **무접촉**. scope-creep 없음. 함께 유입된 A-0018/19/20·D-052~054 는 **재제출 전 `origin/main` merge**(Codex 가 지침대로 병합 — **D-050 함정 종식**)로 들어온 기존 기록(변조 없음).

**머지 판정(D-007)**: 정적 콜그래프 분석 게이트 = **비민감**(정산·인증/인가·암호화·DB migration·infra 무해당·자동차단 권한 없음·D-020~032·D-050 선례). 구현자(Codex)≠머지자(Claude) → **Claude `main` 머지·push. MVP-2 TASK-023 완결.**

**비차단 이월**: O-1(중첩 데코레이터/기본인자 조용한 유실 — 틀린 엣지 없이 누락만) TASK-025 고정 적대세트 이월 유지.

**다음**: MVP-2 TASK-024 역도달성(reverse-reachability) — 콜그래프 + sink 레지스트리로 변경 함수 → governed sink 상류 도달 판정.

**산출 기록**: `collab/decisions.md` D-055 · `collab/answers/A-0020.md` §R-3 해소 · `review-notes.md` TASK-023 R-3 재재재리뷰(D-055) 절 · `collab/handoff-log.md` 맨 위(통과·머지완료) · `TASKS.md` TASK-023 ☑.

## TASK-024 역도달성 게이트 구현 — Codex 제출 · 74bdcf2

**무엇을 위해**: MVP-2 간접 영향 추적의 첫 판정 게이트를 추가했다. 등록된 sink가 호출하는 N홉 이내 상류 함수가 이번 diff에서 바뀌면 `indirect_impact` finding으로 승인요구를 내도록 했다.

**무엇을 수정**: `.harness/gates/check-indirect-impact.py` 신규 추가. 기존 `map-diff-to-functions.py`, `classify-python-function-changes.py`, `extract-callgraph.py`, `extract-sinks.py`를 재사용해 변경 함수·콜그래프·sink를 조합한다. `tests/run-tests.sh`와 `tests/cases.yaml`에 새 게이트 validator를 추가하고, `tests/fixtures/indirect-impact/`에 enforcing 1홉, shadow, N=1 2홉 경계, 분석실패 fail-closed fixture를 추가했다. README 실행 명령도 한 줄 갱신했다.

**무엇을 고침**: 신규 기능. enforcing sink의 직접 상류 변경은 `approval_required`/exit 2, shadow sink는 verdict 미반영 `shadow_hits`, N=1에서 2홉 상류 변경은 미발화, 분석 실패는 `tool_owner` fail-closed 승인요구로 고정했다.

**검증**: `bash tests/run-tests.sh` 85/85 PASS. `bash tests/mutation-check.sh` PASS(139 expectation mutations + policy mutation, 원본 불변). `git diff --check`, `bash -n tests/run-tests.sh`, `python3 -m py_compile .harness/gates/check-indirect-impact.py` PASS.

**브랜치·커밋**: `codex/2026-07-16-task024-indirect-impact` · 구현 커밋 `74bdcf2`.

## TASK-024 역도달성 게이트 `check-indirect-impact.py` 리뷰 — **통과 · Claude main 머지** (MVP-2 간접영향) · D-056

**맥락**: `git fetch --prune` 후 미머지 `origin/codex/*` 는 `2026-07-16-task024-indirect-impact` 하나. handoff 최신 = `[2026-07-16] Codex → Claude | 74bdcf2`(신규 제출·보정요청 아님) = **Claude 리뷰 차례**. 브랜치는 `origin/main`(`0e7e258`) 클린 후손(Codex 재제출 전 병합 준수 — **D-050 함정 종식 지속**).

**게이트 요지**: 등록/frozen-auto/@gov sink 로부터 **forward(caller→callee) N홉** BFS 도달 = sink 의 **의존(callee)**. changed 함수가 그 집합 ∈ → `indirect_impact`+`approval_required`(exit 2). **exit 1 없음 = 차단금지 불변식**(최상위 except 도 exit 2). shadow sink → `shadow_hits`(verdict-neutral). map/classify/callgraph/sinks 오류 → `fail_closed`+`tool_owner`. 기존 4게이트 importlib 재사용.

**심층·적대 검증(격리 worktree `wt-task024` + fresh advrepo1~3)**:
- **방향 반전 아님(advrepo2)**: sink `transfer` 의 **소비자** `consumer.run` 변경 → 미발화(소비자는 의존 아님) · 의존 `compute_fee` 변경 → 발화. forward-from-sink 의미 정확.
- **이름조인 무결(fresh)**: method-as-sink(advrepo1 `app.reports.Report.export`→module-level `check_permission`)·frozen-auto sink(advrepo2 `frozen:app.settlement.core.transfer`, zone `required_approval:settlement-reviewer` 전파)·registry sink 전부 class-qualified 정확 매칭. map/classify/callgraph/sinks **동일 `full_function_name`+class-qualified 스코프명** → TASK-023 R-1~R-3 오해소 폐쇄가 **민감 엣지 보존**으로 결실.
- **AC#5 파라미터·경계**: two-hop-boundary(N=1 서 2홉 미발화)+`unrelated` 미발화. `hops:2` 로 올리면 2홉 `normalize_user` 발화(하드코딩 아님). **음성검증**: `if depth >= max_hops` 무력화 → `indirect-impact-two-hop-boundary` 단독 FAIL(0/1) = 경계필터 load-bearing.
- **정직성(ADR-001 D4, advrepo3)**: `self.compute()` 미해소 → verdict pass 이나 `coverage.unevaluated` 에 명시 노출(조용한 누락 아님·설계§7 영구한계). TASK-025 AC#3 회귀 고정 예정.
- **fail-closed(analysis-error 픽스처)**: head `broken.py` 구문오류 → 부분결과(진짜 `check_permission` finding)와 `fail_closed` 동시 방출·reviewer_required=[security-reviewer, tool_owner]. 실오류가 진짜 finding 안 가림·최악에도 fail-safe.
- `run-tests.sh` **85/85 PASS**(병합 후 main 재확인)·`mutation-check.sh` PASS(139 expectation + policy mutation).

**보수적 개발(§1)**: 델타 = 신규 `check-indirect-impact.py`(337L)+`cases.yaml`/`run-tests.sh`/fixtures+`README.md`(-1 stale/+1 usage)·handoff·summary. `policies/*`·`templates/*`·`docs/*`·`CLAUDE.md`·기존 게이트 `.py` **무접촉**. 기존 게이트 재구현 아닌 재사용. scope-creep·over-reach 없음.

**머지 판정(D-007)**: 정적 콜그래프 역도달성 분석 게이트 = **비민감**(정산·인증/인가·암호화·DB migration·infra 무해당·자동차단 권한 없음·approval 상한). TASK-018/019/022/023 선례(D-020~D-032·D-046·D-052~D-055). 구현자(Codex)≠머지자(Claude) → **Claude `main` 머지·push. MVP-2 TASK-024 완결.**

**비차단 이월(차기 AC 가드 명시)**: **O-1** 상설 픽스처가 registry(module-level) sink 만 커버 → method/frozen-auto/@gov sink 변형을 TASK-025 AC#1(고정 적대세트 7종)에 포함. **O-2** self/attribute 메서드 호출 의존은 verdict 미포착·coverage 노출 → TASK-025 AC#3 정직성 회귀 고정. 둘 다 거버넌스 구멍 아님(현행 동작 정확)·§2B 필수질문=아니오.

**다음 = MVP-2 TASK-025** 과탐 통제 + 고정 적대 입력 세트 7종.

## TASK-025 과탐 통제 + 고정 적대 입력 세트 — Codex 제출 · b15696c

**무엇을 위해**: TASK-024 리뷰에서 이월된 method/frozen-auto/@gov sink 상설 픽스처 부재와 동적 호출 coverage 노출을 TASK-025 회귀 세트로 고정했다. 간접영향 게이트가 너무 넓게 울리거나 조용히 놓치는 대표 경계를 매번 검증하게 하는 목적이다.

**무엇을 수정**: `tests/fixtures/indirect-impact/`에 6개 fixture를 추가하고, 기존 `indirect-impact-direct`까지 포함해 `adversarial` 그룹 7종을 구성했다. 항목은 직접경로, method sink, frozen-auto sink, `@gov(sink=true)` opt-in 경계, sink 소비자 과탐 경계, 동적 호출 `coverage.unevaluated` 노출, 조건부 동명 함수 2홉 케이스다. `tests/run-tests.sh`는 `check-indirect-impact` 결과의 `coverage.unevaluated`를 기대값으로 단언하도록 확장했다.

**무엇을 고침**: 신규 테스트 보강. gate 판정 로직은 변경하지 않고, TASK-025 AC의 N홉 경계·정책 hops 파라미터·shadow/opt-in·동적 미해소 정직성·과탐 경계를 fixture로 고정했다.

**검증**: `TEST_CASE_GROUP=adversarial bash tests/run-tests.sh` 7/7 PASS. `bash tests/run-tests.sh` 91/91 PASS. `bash tests/mutation-check.sh` PASS(151 expectation mutations + policy mutation + metamorphic/negative groups). `bash -n tests/run-tests.sh`, `git diff --check` PASS.

**브랜치·커밋**: `codex/2026-07-16-task025-adversarial-fixtures` · 구현 커밋 `b15696c`.

## TASK-025 과탐 통제 + 고정 적대 입력 세트 — Claude 리뷰 **통과 · main 머지** · D-057 (헤드 11ec86b)

**무엇을 리뷰했나**: `codex/2026-07-16-task025-adversarial-fixtures`(impl `b15696c`) — MVP-2 간접영향 게이트의 §9 고정 적대 입력 세트를 상설 회귀 픽스처로 고정하는 순수 테스트 태스크(게이트 로직 무변경).

**결론**: **통과 · 비민감(테스트 전용 분석 게이트 픽스처) → Claude `main` 머지**(구현자 Codex≠머지자 Claude, D-007).

**AC 검수**: AC#1 §9 7종 상설 회귀(`adversarial` 7 + `two-hop-boundary` + `mutation-check` 로 개념 전부 상설화) · AC#2 hops 정책값 실배선(하드코딩 아님·fresh 변조 실증) · AC#3 동적 미탐 `coverage.unevaluated` 정직성 노출 · AC#4 dampening 미구현(자명).

**적대 검증(격리 worktree)**: 5픽스처 게이트 raw JSON = 기대값 완전일치 · fresh 변조 3종(의존 수정→발화·if-분기 normalize→union hop2 발화·정적화+hops2→hop2 발화) · rig-and-revert 2종(coverage_unevaluated/verdict) 단독 FAIL=load-bearing. `run-tests.sh` **91/91**·`adversarial` 7/7·`mutation-check` PASS(151).

**보수적 개발 OK**: 테스트 픽스처·cases·러너 단언만·`.harness/*`·`policies/*`·`docs/*`·`CLAUDE.md` 무접촉·scope-creep 없음.

**비차단(§2B 필수질문=아니오)**: O-1 `adversarial` 그룹이 §9 문자적 7종과 1:1 아님(N홉경계·음성검증 타 위치 상설) → 후속 `two-hop-boundary` 를 그룹 편입 제안 · O-2 dynamic-coverage `hops:1` 이중원인 → `hops:2` 변형이면 대조 더 순수.

**멱등성**: `b15696c`·`8256fe4`·`11ec86b` 재처리 금지. **다음 = MVP-2 완결·형 지시 대기(신규 태스크 없음).**

## TASK-026 킷에 MVP-2 간접영향층 배선 — Codex 제출 · 9e04c13

**무엇을 위해**: 개발 하네스에서 완결된 MVP-2 sink 역도달성 기능을 배포용 킷의 실제 실행 경로에 반영해, 간접영향 판정이 파일만 동봉되고 최종 verdict에서는 누락되는 상태를 막았다.

**무엇을 수정**: `sync-from-dev.sh`로 신규 `extract-sinks`·`extract-callgraph`·`check-indirect-impact`와 TASK-022~025 테스트를 반영해 킷 게이트를 13→16종으로 올렸다. `run.sh`에 3층 간접영향 게이트를 허용 exit `0 2`로 배선하고 카드3축·능력·정책과 최종 판정에 합쳤다. `--policies`의 `sink-registry.yaml`을 사용하며 누락 시 tool-owner 승인요구로 fail-safe 한다. `manifest.yaml`·`README.md`·`selftest.sh`를 MVP-2/16종으로 갱신했다. 킷 전용 진입점 적대테스트와 sink 기본 정책이 재동기화에서 삭제되지 않도록 보존했다. dev 원본 정책 부재는 `collab/questions/Q-0004.md`로 Claude에 요청했다.

**무엇을 고침**: 킷이 MVP-1.5 스냅샷에 머물러 간접 sink 영향이 실사용 진입점에서 조용히 누락되던 문제를 해결했다. sync가 `kit/tests/run-entrypoint-tests.sh`를 삭제하는 재생성 결함도 함께 고쳤다.

**검증**: `bash kit/selftest.sh` PASS — 개발 회귀 91/91, 킷 진입점 적대 11/11, expectation mutation 151개+정책 mutation+metamorphic+negative corpus 전부 PASS. fresh sink→helper 변경은 approval_required, 대상 sink-registry 누락은 fail-safe를 확인했다. 복제 킷에서 간접 게이트를 무력화하면 해당 진입점 케이스가 기대대로 FAIL(10/11)했다. sync 재실행 전후 진입점 테스트·sink 정책 SHA-256 동일, `bash -n`, `py_compile`, `git diff --check` PASS.

**브랜치·커밋**: `codex/2026-07-16-task026-kit-mvp2` · 구현 커밋 `9e04c13`.

## Claude — TASK-026 리뷰 통과 · main 머지 (D-058)

**무엇을 함**: TASK-026(킷 MVP-2 배선) 심층·적대 리뷰. run.sh 3층 배선을 한 줄씩 검수(exit 계약 `0 2`·argparse 1:1·max 조립·HAS_RANGE fail-closed·`--policies` SINKS 재계산), sync 실작동(`cmp` 16종 바이트 동일·재실행 멱등), fresh 적대 5종(기본정책 frozen 자동 sink 발화 / sink 무관 PASS / 범위없음 fail-closed / hops 정책값 실구동+owner 라우팅 / 문법오류 fail-closed), rig-and-revert 3종(배선무력화→진입점 단독 FAIL 10/11 = 가드 load-bearing / 게이트 삭제→gate_missing 2 / 기대값 변조→FAIL). `kit/selftest.sh` 전량 green(91/91·11/11·mutation 151) 재현.

**판정**: 통과·비민감 → Claude 머지(D-007). Q-0004 는 `policies/sink-registry.yaml` dev 원본 추가(A-0021)로 해소. 비차단 O-1(main 기존결함: intent 없는 repo+bash 3.2 `INTENT_ARGS[@]` unbound 크래시 — 차기 킷 정비 AC)·O-2(킷 전용 자산 보존 패턴)·O-3(frozen zone `required_approval` 부재 → reviewer 폴백 tool_owner·정책면 후속).

**브랜치**: `claude/2026-07-16-review` (codex 헤드 `259d49e` 위 리뷰 기록 + 정책 원본).

## Claude — TASK-027·028 설계 (`expected_paths` 부재 탐지 / 패치 생존성) · D-059

**무엇을 위해**: 형의 벤더-브랜치 질문(Q2 — "패치 브랜치에 명시된 파이썬 파일들이 실제로 수정됐는지")에 답하는 신규 탐지 방향을 태스크화. 형 제안("민감경로에 A.py 정의")은 성립 안 함을 논리로 실증 — 민감경로는 **존재 탐지**라 패치 적용 시(불필요) 발화하고 **패치 유실 시 침묵**(정확히 필요한 순간 실패). 필요한 건 **부재 탐지**(선언한 파일이 diff 에 없으면 발화).

**무엇을 설계**: `change_intent.expected_paths`(반드시 건드려야 하는 파일 선언·allowed/forbidden 과 대칭·**선택·기본 off·하위호환**). 미변경 시 `missing_expected`→최소 `approval_required`(**차단 없음**). 빈 diff+expected 선언 → 전부 missing → approval(패치 전면 유실 신호). 카드(`generate-change-evidence`) 미러링 필수 — 킷 run.sh 가 카드 게이트를 intent 층으로 호출하므로 카드 반영 없으면 킷에서 안 돎.

**적대적 자기검토**: glob 거친 보증(→리터럴 권장·정직 문구)·rename/delete 코너(→방향 안전·approval cap·문서화)·거버넌스 구멍 질문=아니오(탐지 추가일 뿐·통제 약화 없음·기본 off·차단 없음). 최우선 회귀 가드=하위호환(현행 91/91 무변화).

**구조**: TASK-027(dev 게이트 확장·Codex 저자·Claude 리뷰 비민감→머지) → TASK-028(킷 스냅샷 sync + 진입점 실증·run.sh 무변경). 최종 = MVP-2 킷에 부재 탐지 반영("최종은 그걸 반영한 MVP2").

**산출(Claude 정책면·docs-only)**: `TASKS.md` TASK-027/028 · `collab/decisions.md` D-059 · `handoff-log.md` 착수 신호. 게이트 코드는 Codex 몫(Claude 미작성·상호견제).

**브랜치**: `claude/2026-07-16-expected-paths-task`.

## TASK-027 `expected_paths` 부재 탐지 — Codex 제출 · d8e6fa4

**무엇을 위해**: 벤더 패치처럼 반드시 변경돼야 할 파일이 diff에 아예 없을 때 기존 존재 탐지기가 침묵하던 공백을, 선택형 `change_intent.expected_paths` 부재 탐지로 보완했다.

**무엇을 수정**: `check-change-intent.py`와 `generate-change-evidence.py`의 인라인 intent 판정에 `expected_paths`/정렬된 `missing_expected`를 추가했다. 각 패턴은 변경 파일 중 하나 이상 매칭될 때 충족되며, 누락 시 승인요구로 올린다. 감사카드 스키마·coverage 문구와 change-intent 템플릿/예시에 리터럴 경로 권장, glob의 거친 보증, rename 목적지/delete 경로 판정 한계를 명시했다. expected-present/missing/empty-diff/none 네 픽스처와 카드 통합 회귀를 추가했다.

**무엇을 고침**: expected 선언 파일이 하나도 변경되지 않은 패치 유실과 빈 diff를 `missing_expected`로 드러내도록 했다. forbidden 변경의 blocked 우선순위와 expected 미선언/빈 리스트의 기존 동작은 그대로 유지하며, 신규 판정은 `approval_required`를 상한으로 한다.

**검증**: `bash tests/run-tests.sh` **96/96 PASS**(기존 91개+신규 5개), `bash tests/mutation-check.sh` **PASS**(161 expectation mutations+policy mutation+metamorphic+negative corpus), 신규 JSON 2회 `cmp` 동일, 텍스트 `missing_expected`·정직 문구 확인, `python3 -m py_compile`, `git diff --check` PASS.

**브랜치·커밋**: `codex/2026-07-16-task027-expected-paths` · 구현 커밋 `d8e6fa4`.

## TASK-027 리뷰 — 통과 · main 머지 (Claude · D-060)

**무엇을 위해**: Codex 제출 `d8e6fa4`(expected_paths 부재 탐지)를 TASKS.md AC 7항 대비 심층·적대 리뷰하고, 통과·비민감 판정으로 main 에 머지했다(D-007, 구현자≠머지자).

**무엇을 검증**: 격리 worktree 에서 96/96·mutation 161 재현. fresh 적대입력 11종(rename 목적지 시맨틱, delete, forbidden 우선순위, 하위호환 빈 diff, glob 거친 보증, 스칼라/비문자열 비정형, 카드 빈 diff 미러링(픽스처 미커버 경로), 카드 frozen 우선순위, fresh git repo ref-모드 양방향, 결정론 cmp). rig-and-revert 음성검증 3종 — intent 언와이어(신규 2케이스 단독 FAIL)·카드 언와이어(카드 케이스 단독 FAIL=미러 독립 load-bearing)·기대값 변조 FAIL, 원복 96/96.

**판정**: 통과. 비차단 관찰 3건(디렉토리 리터럴 영구 missing 과탐 / 비문자열 크래시 과차단 / 중복 에코) — 전부 §2B 필수질문=아니오(안전 방향). Claude 소유 템플릿 접촉은 AC#5·#7 명시 위임으로 scope-creep 아님.

**브랜치·커밋**: 리뷰 기록 `claude/2026-07-16-review` · 머지 커밋 `main`(TASK-027 merge + 기록). **다음 = Codex TASK-028**(킷 sync·진입점 케이스).

## Claude — MVP-3 다국어 확장 설계 (Java/Spring 우선) · D-061

**무엇을 위해**: 형 요구("파이썬 말고 프론트·Java/Spring도 똑같은 하네스로") 기획/설계. 형 문답으로 Java/Spring 먼저 + tree-sitter 백본(Python `ast` 무개조) 확정.

**핵심 설계**: 판정 엔진 하나 + 언어별 추출기(어댑터). **공통 IR 4종(인벤토리·능력·주석·콜그래프) + 확장자 라우터**(파일별 분배·미지원=coverage 정직노출). "N언어 × 4추출기 → 판정엔진 1개". Python 은 `ast` 유지(검증자산 보존·무회귀). Java 는 `@Gov`+**Spring 어노테이션 카탈로그**(신규 정책·`@PreAuthorize`→protected 등)로 오히려 더 강함.

**정직 한계(§2B)**: 경로층은 이미 언어무관(공짜) / 깊은층은 재구현 / **간접영향은 Java 에서 DI·AOP·다형성으로 더 약함 → coverage 정직노출**("다 커버"는 거짓). tree-sitter 는 타입 미해소(현행 Python 콜그래프와 동급 한계).

**태스크**: TASK-029(J0 seam/라우터/tree-sitter 도입) → 030(J1 Java 인벤토리) → 031(J2 @Gov+Spring 카탈로그) → 032(J3 Java 능력). 이후 W1(프론트)·X(콜그래프→간접영향). 각 게이트=Codex 저자·Claude 리뷰. 다국어를 MVP-3 로 확정, cross-commit(구 MVP-3 후보)은 후속 마일스톤 재이월.

**산출(Claude 정책면·docs-only)**: `docs/multi-language-adapter-design.md` 신규 · `TASKS.md` MVP-3 섹션(TASK-029~032) · `collab/decisions.md` D-061 · handoff 착수순서 신호(TASK-028 먼저 → MVP-3). **착수순서 주의**: expected_paths 킷 반영(TASK-028)이 먼저.

**브랜치**: `claude/2026-07-16-multilang-design`.

## Claude — MVP-3 설계 재검토 · 파이썬 동등성(parity) 최우선 격상 · D-062

**무엇을 위해**: 형 지시("전체 상세 재검토 + 가장 중요한 건 파이썬과 동일 성능"). parity 를 다국어 확장 1순위 제약으로 승격.

**재검토 결과**: 경로층·인벤토리·주석·능력은 parity 공짜(구조/이름 기반). **유일하게 L3(간접영향)만 공짜 아님** — Java 인터페이스+DI+AOP 로 이름기반 콜그래프가 실제 엣지 놓침. 앞 초안(D-061)은 이걸 "더 약함"으로 방치 → parity 요구 하 결함.

**핵심 보정**: L3 을 **보수적 과대근사**로 안전 parity 달성 — 인터페이스 호출→모든 구현체 엣지·`@Autowired`→모든 구현·프록시 직접엣지 → 실제 엣지의 superset → **놓침 없음**(과탐은 승인상한이라 감내). tree-sitter 로 달성, 정밀 parity 는 후속 네이티브 심볼솔버(Java 정적타입이라 도입 시 Python L3 능가 가능).

**parity 조작정의 4축 + 강제장치**: ①탐지 ②엄밀성 ③정직성 ④안전방향(불완전 시 과탐 쪽·과소탐=놓침 금지). 강제 = `tests/parity/` 교차언어 등가 픽스처(py판+java판 동일 verdict 단언+음성검증) → "동일 성능" 자동 증명.

**기타 보정**: 문법버전 pin(재현성)·IR 중립화(annotations/signature)·라우터 additive(Python 골든패스 무개조)·Java java.lang 암시적 import 비대칭(call 신호로 커버).

**산출**: 설계문서 §1.5 신설·§5 재작성·§3.1/§3.3 보강 · TASKS MVP-3 intro/공통/TASK-029·031·032 parity AC · D-062. 게이트/픽스처는 Codex 저자. 착수순서 불변(TASK-028 → MVP-3).

**브랜치**: `claude/2026-07-16-multilang-parity`.

## TASK-028 `expected_paths` 킷 스냅샷 — Codex 제출 · c858c9b

**무엇을 위해**: TASK-027의 필수 변경 파일 부재 탐지를 배포용 킷에도 반영해, 패치 유실이 실사용 진입점에서 조용히 통과하지 않도록 했다.

**무엇을 수정**: `sync-from-dev.sh`로 dev 최신 intent·감사카드 게이트, change-intent 정책 양식, 카드 템플릿, expected_paths 회귀 픽스처를 킷에 동기화했다. 진입점 적대검증에 선언한 `app/required_patch.py`가 diff에 없을 때 exit 2와 카드 `missing_expected`를 함께 확인하는 케이스를 추가했다. manifest 버전을 `0.2.1-mvp2`로 올리고 README/manifest에 패치 생존성·부재 탐지를 명시했다. 격리 selftest 루트를 git 저장소로 구성해 name-status 픽스처가 단일 변경 디렉터리를 저장소 전체로 오인하지 않게 했다.

**무엇을 고침**: 킷이 TASK-026 시점 스냅샷에 머물러 expected_paths 선언을 집행하지 못하던 누락을 해소했다. 신규 dev 픽스처 2건이 비-git selftest 환경에서 `app/**`를 최상위 디렉터리 100%로 계산해 승인요구로 뒤집히던 재현성 문제도 해결했다.

**검증**: 핵심 게이트 2종 dev↔kit `cmp` 동일, 게이트 수 16종 유지, `kit/run.sh` 무변경, sync 재실행 전후 상태 digest 동일. `bash kit/selftest.sh` **96/96 + 진입점 12/12 + mutation PASS**. 임시 복제본에서 신규 기대값 2→0 변조 시 해당 케이스가 실패해 11/12가 됨을 확인했다. `bash -n`, `python3 -m py_compile`, `git diff --check` PASS.

**브랜치·커밋**: `codex/2026-07-16-task028-expected-paths-kit` · 구현 커밋 `c858c9b`.

## TASK-028 리뷰 — 보정요청 R-1 (진입점 케이스 판별력) · Claude

**무엇을 위해**: 킷 `expected_paths` 스냅샷(`codex/2026-07-16-task028-expected-paths-kit`)을 CLAUDE.md §2B 심층·적대 기준(킷 = 배포 최전선)으로 리뷰했다.

**확인한 것(통과)**: 게이트 17파일 dev↔kit md5 전부 동일(main D-060 승인본과도 동일)·정책/템플릿/테스트 트리 바이트 동일·sync 재실행 멱등·게이트수 16=16(AC#1). manifest `0.2.1-mvp2`·README 능력/정직 문구(AC#3). selftest 96/96+진입점 12/12+mutation 161 재현(bash 3.2)·`run.sh` 무접촉(AC#4). selftest git init 신규 블록은 제거 rig 로 정확히 신규 2픽스처만 FAIL(81/83) = load-bearing·비-git fallback 은 과탐 방향이라 마스킹 아님. fresh E2E 8종(격리 missing 단독→2·충족→0·frozen+missing→1·no-range→2·--policies 오버라이드/부재·intent 파손→1(의도된 카드층 fail-closed)·결정론 cmp).

**무엇을 발견(보정요청)**: 진입점 신규 케이스가 선언 수정(change-intent.yaml)을 코드 변경과 같은 커밋에 실어 diff 에 intent 파일이 포함 → `out_of_scope` 가 항상 동반 = exit 2 이중 원인. 카드 grep 2종은 빈 키/에코에도 매칭. **킷 카드 게이트의 missing 계산을 무력화해도 진입점 12/12 PASS 유지** — 케이스가 자기 목적(부재 탐지 실증)에 load-bearing 아님. 진입점 스위트는 sync 가 보존하는 유일한 킷 소유 가드라 §2B 필수질문=예 → 보정요청(A-0022·D-063). 보정안(선언을 base 커밋으로 분리 + reasons 라인 grep)은 rig 로 사전 실증(detection-kill 시 exit 0→FAIL).

**부수 발견**: intent 파일 없는 repo + bash<4.4 → `run.sh` `INTENT_ARGS[@]` 크래시(exit 1·카드 미생성) — main 재현 = 기존 결함 → **TASK-033 등록**(콘솔 missing_expected 표기 O-B 병합).

**처리(D-007)**: 코드 브랜치 머지 보류·보정 커밋만 재리뷰(멱등 `c858c9b`·`7b3faf9`). 리뷰 기록은 `claude/2026-07-16-review` → main 머지.

## TASK-028 `expected_paths` 킷 스냅샷 — 보정 재제출 · 0f4d1a0

**무엇을 위해**: A-0022 R-1에 따라 킷 소유 진입점 케이스가 필수 경로 부재 탐지 자체의 회귀를 독립적으로 막도록 보정했다.

**무엇을 수정**: `expected_paths` 선언을 대상 저장소의 선행 base 커밋에 분리하고, 검사 대상 `HEAD~1..HEAD`에는 `app/service.py`만 남겼다. 카드 검증도 에코/빈 키가 아닌 reasons의 정확한 `missing_expected:app/required_patch.py` 문자열을 요구하도록 강화했다.

**무엇을 고침**: 기존에는 `change-intent.yaml`이 diff에 포함돼 `out_of_scope`만으로도 exit 2가 되었고 느슨한 grep이 실제 탐지 없이 통과했다. 이제 missing 계산을 제거하면 exit 0으로 바뀌어 해당 케이스가 실패한다. `kit/run.sh`와 게이트 코드는 변경하지 않았다.

**검증**: `tests/run-tests.sh` **96/96 PASS**, `kit/tests/run-entrypoint-tests.sh` **12/12 PASS**, `bash -n`, `git diff --check` PASS. 기대값 2→0 변조 rig와 카드 missing 계산 제거 rig가 각각 해당 케이스만 실패시켜 **11/12**가 됨을 확인했다.

**브랜치·커밋**: `codex/2026-07-16-task028-expected-paths-kit` · 보정 커밋 `0f4d1a0`.

## TASK-028 보정(0f4d1a0) 재리뷰 — 통과·main 머지 · Claude

**무엇을 위해**: A-0022 R-1 보정 재제출(`0f4d1a0`)을 멱등 원칙대로 보정 커밋만 재리뷰했다(§2B 심층·적대 — 킷 = 배포 최전선).

**확인한 것**: 보정 델타는 진입점 케이스 1건 3+/2- — ① `expected_paths` 선언을 선행 커밋으로 분리해 마지막 diff 가 `app/service.py` 단독(명시 `git add`) ② 카드 grep 을 reasons 정확 문자열 `missing_expected:app/required_patch.py` 로. 게이트 reasons 포맷과 문자 일치·필드 에코/빈 키와 비매칭 확인.

**실증(격리 worktree·전부 fresh 재실행)**: 진입점 12/12 · selftest 96/96+진입점 12/12+mutation 161 · sync 재실행 멱등(보정 스크립트 생존). **detection-kill rig**(킷 카드 게이트 missing 계산 `[]` 고정·에코 유지) → 케이스 **단독 FAIL(11/12)** = D-063 R-1(기능 죽어도 12/12 침묵) 해소·load-bearing 회복. fresh 픽스처 밖 입력(타 패턴 2종·코드단독 diff) → exit 2 원인 missing 단독(out_of_scope 0건). 음성검증 2종(기대 rc 2→0·grep 문자열 변조 — 변조 적용을 count==1 assert 로 확인) 각각 단독 FAIL.

**처리(D-007)**: 통과·비민감(킷 테스트 하네스) → Claude 머지 `d1e3c94` → main push. **TASK-028 완결.** 비차단 O-D(out_of_scope 부재 단언 보강)는 TASK-033 AC#5 로 병합. 다음 = Codex TASK-033 착수 가능. 상세 D-064·review-notes TASK-028 보정 절.

## TASK-033 킷 intent 부재 실행 견고성 — Codex 제출 · 8e6b54b

**무엇을 위해**: macOS 기본 `/bin/bash` 3.2에서 대상 저장소에 `change-intent.yaml`이 없을 때 빈 배열 확장이 `unbound variable`로 크래시해 exit 1 차단으로 오인되고 카드와 후속 게이트가 실행되지 않던 기존 결함을 해소했다.

**무엇을 수정**: `kit/run.sh`이 intent 부재 시 카드 생성에만 임시 `**` 허용 선언을 사용하고 즉시 삭제하도록 해 의도층을 생략하면서 민감경로/@gov 판정을 보존했다. 배열 확장을 bash 3.2 안전 관용구로 바꾸고 `ANALYSIS_FAILURES`도 전수 점검했으며, 1층 콘솔 필터에 `missing_expected`를 추가했다. `kit/tests/run-entrypoint-tests.sh`에는 `/bin/bash` 무-intent E2E와 expected-path의 콘솔 표시·`out_of_scope` 부재 단언을 추가했다.

**무엇을 고침**: intent 부재 실행이 exit 0·생략 안내·감사카드 생성·2층/3층/메타층 정상 실행으로 끝난다. expected-path 회귀 케이스에 다른 승인 원인이 재유입돼 탐지 결함을 가리는 드리프트도 자동 검출한다.

**검증**: `/bin/bash 3.2.57`에서 진입점 **13/13 PASS**. `bash kit/selftest.sh` **96/96 + 진입점 13/13 + mutation 161 PASS**. 안전처리 제거 rig는 무-intent 케이스만 FAIL(12/13), 콘솔 표시 제거 rig는 expected-path 케이스만 FAIL(12/13). `bash -n`·`git diff --check` PASS.

**브랜치·커밋**: `codex/2026-07-16-task033-kit-run-robustness` · 구현 커밋 `8e6b54b`.

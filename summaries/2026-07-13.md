# 2026-07-13 작업 요약 (Claude)

## 런처 보정요청 파싱 버그 fix — handoff 형식 진화 + set -e 조용한 죽음 (launcher fix 후속2)
- **증상(사용자 재현)**: `./scripts/start-codex.sh` 가 TASK-021 보정요청 라인은 감지·출력했는데 `▶ Codex 실행...` 직전에 **에러 없이 끊김**(exec codex 도달 못 함).
- **근본원인 2가지**:
  1. **handoff 줄 형식 진화**: TASK-020 은 `| <head> (**보정요청**)`(해시 직전) 였으나 TASK-021 은 `| 616ff43 / 헤드 1b954c3 (**보정요청**)`(`구현 / 헤드 <head>` 형식). 런처 sed 정규식이 `| <해시> (**보정요청**)` 만 기대 → **HEADC 빈 값**.
  2. **set -euo pipefail 조용한 죽음**: HEADC 빈 값 → `git branch -r --contains ""` 비영종료 → pipefail 로 `WORKREF="$(...)"` 대입 실패 → `set -e` 가 에러 메시지 없이 종료.
- **수정**: `scripts/start-codex.sh` 감지 블록 —
  - 정규식을 `.*[^0-9a-f]\([0-9a-f]\{7,40\}\) (\*\*보정요청\*\*).*` 로 견고화(구분자를 `[^0-9a-f]` 로 잡아 `| `·`헤드 ` 무엇이든 허용, ` (**보정요청**)` 바로 앞 hex=헤드 추출). 구·신 형식 둘 다 검증.
  - 명령치환에 `|| true` + `git branch --contains` 를 `[ -n "$HEADC" ]` 가드 뒤로 → **추출 실패해도 조용히 안 죽고** 경고 출력 후 수동 checkout 안내로 폴백.
- **영향 범위**: 런처 감지 블록만(게이트·정책·판정 무관·비민감 툴링). Claude 소유 런처 → Claude 커밋·머지.
- **검증**: `bash -n` 통과. 구형 `42062f6`·신형 `1b954c3` 양쪽 정규식 매칭 확인. 실 origin/main 줄 전체 드라이런 — `HEADC=1b954c3 · CORR_ANS=A-0012 · WORKREF=origin/codex/2026-07-11-task021-broad-intent` 정확 추출·죽지 않음.
- **handoff-log 미접촉**: 맨 위 줄이 TASK-021 보정요청 신호(런처 감지 대상)라 위에 줄 추가 시 새태스크 오판 → summary 로만 기록.

## TASK-021 광역 의도선언 격상 리뷰 → 보정요청 (1건 · 🔴)

- **브랜치 스캔**: `git fetch origin --prune` 후 main 미머지 origin/codex 브랜치 = `codex/2026-07-11-task021-broad-intent` 1개. handoff 최신이 `Codex → Codex TASK-021 done` → 리뷰 차례(가). 나머지 codex/* 전부 머지됨.
- **판정: 보정요청** (D-041 · `collab/answers/A-0012.md`). 코드 브랜치 **머지 보류**, 리뷰기록만 `main` 머지.
- **통과**: root-glob 검출(AC#1a, 임계값 독립)·커버리지 검출(기본 임계값 하 정상)·오탐 억제·하위호환(빈 diff pass·blocked 우선·approval_required 로만 격상)·`run-tests.sh` 77/77·보수적 개발(policies·Claude 소유 무접촉).
- **R-1 (🔴)**: 임계값 N 을 피검자 자신의 `change-intent.yaml` 에서만 읽어 자기-무력화 가능. `broad_scope_threshold_percent:101` + 최상위 디렉토리 개별 나열 → 커버리지 100%인데 pass/exit 0(ADV1 실증·음성검증). AC#1(b) "N 은 정책값" 위반 + 거버넌스 직접 구멍.
- **R-2 (🟡 비차단)**: 회귀 픽스처가 라이브 repo 트리에 결합 → 최상위 디렉토리 추가 시 `broad-intent-coverage` 깨짐. 보정 시 결정적 고정 권장.
- **적대 검증**: 브랜치 게이트를 scratchpad 사본으로 fresh 입력 실행 + rig-and-revert 음성검증으로 회피 스위치(임계값 오버라이드) 확정.

## 산출
- `claude/2026-07-13-review` 브랜치: A-0012·D-041·review-notes(TASK-021)·handoff-log(보정요청 top 신호)·본 요약. push.
- 리뷰기록 `main` 머지(다음 세션·Codex 가 봄). 구현 코드 브랜치는 보류.

# 2026-07-13 작업 요약 (Claude)

## MVP-2 영향추적(sink 역도달성) 설계 착수 — 방향 확정 + 설계문서 Draft (D-044)
- **무엇을 위해**: MVP-1.5 완결 후 형이 정공법(MVP-2) 선택. MVP-2 = 간접 영향 추적층 — sink 이 의존하는 상류 함수 수정을 현행이 완전 무탐지하는 구멍(“다운로드 @gov 달아도 `check_permission()` 수정 못 잡음”)을 승인요구로 메움.
- **형 확정 결정 2건**: (a) @gov↔sink = **레벨 차등**(frozen 자동 sink + `@gov(sink=true)` 옵트인, 일반 @gov·protected 는 비-sink) — TASKS.md 🔴 미결 해소. (b) **최소 스코프 래칫**(단일 diff·N=1·shadow 시작·차단금지). cross-commit·비-Python·동적완전복원은 명시 비범위.
- **핵심 설계 판단**: 콜그래프는 원리적으로 sound+precise 불가 → "정적 해석호출은 잡고 못 푸는 건 coverage 갭으로 노출(ADR-001 D4) + shadow 시작 + N 통제". 이 층이 하네스 최대 과탐 리스크 지점임을 명시. 옵트인 sink 밀도로 신호대잡음 보존.
- **산출**: `docs/mvp2-impact-tracing-design.md`(**Draft — 형 확정 대기**) · `TASKS.md` TASK-022~025 AC(sink등록→콜그래프빌더→역도달성게이트→과탐통제·고정적대세트7종) · `collab/decisions.md` D-044 · `change-governance-design.md` §3 3층 포인터.
- **상태**: 설계 Draft·**코드 착수 전**. 형이 설계 §10 확정 체크 OK 하면 Accepted 전환 후 Codex 에 TASK-022 인계. 아직 게이트 코드 0줄(Claude=설계만, 구현=Codex).

## MVP-1.5 문서 표시 정합 (stale label 정리 — 실질 갭 0 확인)
- **무엇을 위해**: MVP-1.5(TASK-014~021) 전부 머지·완결됐는데 서류 라벨만 뒤처진 것을 정합. 착수 전 브리핑 주장을 **origin/main 실물과 한 줄씩 적대 검증**해 실질 구멍 유무부터 확정.
- **검증 결과(브리핑 정확성)**: TASK-014~021 전부 실제 머지(D-030·032·034·035·036·042·043 + 산출물 파일 실재)·미머지 브랜치 없음·needs-human 비었음 = 사실. Q-0002 자기보호 6경로 `sensitive-zones.yaml` `level: protected` 실재·Q-0003 5개 capability `source`(bandit/owasp/cwe+CWE번호)+`owner` 전부 실재 = **내용 완전 반영**. → 순수 표시 stale, 실질 갭 0.
  - (자기교정) 1차 진단에서 grep 이 `source:` 의 `builtin:` 자식 줄을 걸러 "source 빈값"으로 오판 → 전문 확인으로 정정. 기록 남김(무발견≠통과 원칙, 내 진단도 검증 대상).
- **무엇을 수정(전부 Claude 소유·비민감 표시정합)**:
  - `TASKS.md`: TASK-014·018·019·020·021 체크박스 ☐→☑ + 머지 D-번호·날짜 인용(TASK-015 스타일 통일).
  - `collab/questions/Q-0002.md`: Status open→resolved(D-028) + 해소 노트(자기보호 6경로 protected 등록 실물확인).
  - `collab/questions/Q-0003.md`: Status open→resolved(D-039 보존확인) + 해소 노트(5 capability source/owner 실물확인).
- **영향 범위**: 문서·질문 상태만. 게이트·정책 내용·판정 로직 **0-diff**(정책 내용은 이미 반영돼 있어 추가 편집 불필요).
- **검증**: `tests/run-tests.sh` **77/77 PASS**(default 71·metamorphic 3·negative-corpus 3) — 문서만 바꿔 회귀 없음 확인. Claude 소유·비민감 → Claude 커밋·main 머지.
- **다음(형 결정 대기)**: MVP-1.5 코드·서류 완결. 로드맵 갈림길 = MVP-2 설계 착수 / 차기-AC-가드 백로그 태스크화 / 증거·파일럿 트랙(벤치마크 v2). 임의 진행 안 함.

## 런처 보정요청 파싱 버그 fix — handoff 형식 진화 + set -e 조용한 죽음 (launcher fix 후속2)
- **증상(사용자 재현)**: `./scripts/start-codex.sh` 가 TASK-021 보정요청 라인은 감지·출력했는데 `▶ Codex 실행...` 직전에 **에러 없이 끊김**(exec codex 도달 못 함).
- **근본원인 2가지**:
  1. **handoff 줄 형식 진화**: TASK-020 은 `| <head> (**보정요청**)`(해시 직전) 였으나 TASK-021 은 `| 616ff43 / 헤드 1b954c3 (**보정요청**)`(`구현 / 헤드 <head>` 형식). 런처 sed 정규식이 `| <해시> (**보정요청**)` 만 기대 → **HEADC 빈 값**.
  2. **set -euo pipefail 조용한 죽음**: HEADC 빈 값 → `git branch -r --contains ""` 비영종료 → pipefail 로 `WORKREF="$(...)"` 대입 실패 → `set -e` 가 에러 메시지 없이 종료.
- **수정**: `scripts/start-codex.sh` 감지 블록 —
  - 정규식을 `.*[^0-9a-f]\([0-9a-f]\{7,40\}\) (\*\*보정요청\*\*).*` 로 견고화(구분자를 `[^0-9a-f]` 로 잡아 `| `·`헤드 ` 무엇이든 허용, ` (**보정요청**)` 바로 앞 hex=헤드 추출). 구·신 형식 둘 다 검증.
  - 명령치환에 `|| true` + `git branch --contains` 를 `[ -n "$HEADC" ]` 가드 뒤로 → **추출 실패해도 조용히 안 죽고** 경고 출력 후 수동 checkout 안내로 폴백.
- **영향 범위**: 런처 감지 블록만(게이트·정책·판정 무관·비민감 툴링). Claude 소유 런처 → Claude 커밋·머지.
- **검증**: `bash -n` 통과. 구형 `42062f6`·신형 `1b954c3` 양쪽 정규식 매칭 확인. 실 origin/main 줄 전체 드라이런 — `HEADC=1b954c3 · CORR_ANS=A-0012 · WORKREF=origin/codex/2026-07-11-task021-broad-intent` 정확 추출·죽지 않음.
- **handoff-log 미접촉**: 맨 위 줄이 TASK-021 보정요청 신호(런처 감지 대상)라 위에 줄 추가 시 새태스크 오판 → summary 로만 기록.

## TASK-021 광역 의도선언 격상 리뷰 → 보정요청 (1건 · 🔴)

- **브랜치 스캔**: `git fetch origin --prune` 후 main 미머지 origin/codex 브랜치 = `codex/2026-07-11-task021-broad-intent` 1개. handoff 최신이 `Codex → Codex TASK-021 done` → 리뷰 차례(가). 나머지 codex/* 전부 머지됨.
- **판정: 보정요청** (D-041 · `collab/answers/A-0012.md`). 코드 브랜치 **머지 보류**, 리뷰기록만 `main` 머지.
- **통과**: root-glob 검출(AC#1a, 임계값 독립)·커버리지 검출(기본 임계값 하 정상)·오탐 억제·하위호환(빈 diff pass·blocked 우선·approval_required 로만 격상)·`run-tests.sh` 77/77·보수적 개발(policies·Claude 소유 무접촉).
- **R-1 (🔴)**: 임계값 N 을 피검자 자신의 `change-intent.yaml` 에서만 읽어 자기-무력화 가능. `broad_scope_threshold_percent:101` + 최상위 디렉토리 개별 나열 → 커버리지 100%인데 pass/exit 0(ADV1 실증·음성검증). AC#1(b) "N 은 정책값" 위반 + 거버넌스 직접 구멍.
- **R-2 (🟡 비차단)**: 회귀 픽스처가 라이브 repo 트리에 결합 → 최상위 디렉토리 추가 시 `broad-intent-coverage` 깨짐. 보정 시 결정적 고정 권장.
- **적대 검증**: 브랜치 게이트를 scratchpad 사본으로 fresh 입력 실행 + rig-and-revert 음성검증으로 회피 스위치(임계값 오버라이드) 확정.

## 산출
- `claude/2026-07-13-review` 브랜치: A-0012·D-041·review-notes(TASK-021)·handoff-log(보정요청 top 신호)·본 요약. push.
- 리뷰기록 `main` 머지(다음 세션·Codex 가 봄). 구현 코드 브랜치는 보류.

## TASK-021 R-1 보정 재제출 재리뷰 → 통과 · Claude main 머지 (D-042)
- **브랜치 재스캔**: main 미머지 origin/codex = `codex/2026-07-11-task021-broad-intent` 1개. 브랜치 handoff 최신이 `Codex → Claude 1c08afa R-1 보정 재제출` → **재제출(가)** — 멱등성으로 **R-1 델타만** 재리뷰(검출 엔진 재론 없음).
- **보정 확인**: `load_intent()` 가 `broad_scope_threshold_percent` 를 change-intent 에서 더 이상 읽지 않고 고정 80(권장 보정 채택). grep 으로 세팅점 line 61 유일·author 통제 경로 부재 확인.
- **적대 재검증**: 합성 8-디렉토리 git repo(라이브트리 결합 차단)서 공격값 101+8개 개별 나열 → threshold 80(101 무시)·coverage 100%·approval_required/exit 2 = 자기-무력화 소멸.
- **음성검증(rig-and-revert)**: line 61 을 예전 author-통제 코드로 되돌린 rigged 사본에 동일 공격 → pass/exit 0 회귀 = 델타 load-bearing 실증.
- **무회귀**: `run-tests.sh` 77/77 PASS·`py_compile`·`git diff --check` OK. 보수적 개발(게이트 1줄+픽스처 3줄·policies·Claude 소유 무접촉).
- **R-2 잔존(🟡 비차단)**: 픽스처가 여전히 라이브 repo 트리에 결합 → 차기 AC 가드 G-broad-1 로 결정적 고정 명시(보정 필수 아님).
- **판정**: R-1 해소·통과·비민감(2층 approval 격상만) → **Claude 가 코드 브랜치 `main` 머지·push** (D-042·A-0012 §해소·review-notes TASK-021 R-1 재리뷰). TASK-021 완결.

---

# 2026-07-13 작업 요약 (Codex)

## TASK-021 R-1 보정 재제출
- 무엇을 위해: A-0012/D-041에서 지적된 광역 의도선언 임계값 자기-무력화 경로 제거.
- 무엇을 수정: `check-change-intent.py`가 검사 대상 `change-intent.yaml`의 `broad_scope_threshold_percent`/`scope_policy.broad_scope_threshold_percent`를 읽지 않고 기본 80만 사용하게 수정. `broad-intent-coverage` fixture에는 공격자 선언값 101을 넣어 회귀 고정.
- 무엇을 고침: 작성자가 같은 change-intent 파일에서 threshold를 100 초과로 올려 top-level coverage 검출을 pass로 우회할 수 있던 신뢰경계 결함을 제거.
- 검증: 직접 게이트 실행에서 `threshold_percent: 80`·`coverage_percent: 87`·exit 2 확인, `tests/run-tests.sh` 77/77 PASS, `git diff --check` PASS, `python3 -m py_compile .harness/gates/check-change-intent.py` PASS.
- 브랜치·커밋: `codex/2026-07-11-task021-broad-intent` / `1c08afa`.

## TASK-021 G-broad-1 follow-up
- 무엇을 위해: D-042에서 차기 AC 가드로 남은 broad-intent 회귀 픽스처의 라이브 repo 최상위 디렉토리 결합 제거.
- 무엇을 수정: `tests/run-tests.sh`의 `check-change-intent` 케이스가 `fixture_dir`를 받으면 격리 git repo의 `base..head`에서 실행하게 추가. `broad-intent-root`/`coverage`/`normal-wide`를 고정된 8개 최상위 디렉토리 fixture로 전환하고, `top_level_dir_count`·`coverage_percent`·`covered_top_level_dirs` 기대값을 고정.
- 무엇을 고침: 실제 repo에 최상위 디렉토리가 추가되면 `broad-intent-coverage`의 7/8=87% 기대가 7/9=77%로 뒤집혀 테스트가 깨질 수 있던 불안정성을 제거.
- 검증: broad-intent 3케이스 3/3 PASS, 전체 `tests/run-tests.sh` 77/77 PASS, `git diff --check` PASS, `bash -n tests/run-tests.sh` PASS, `python3 -m py_compile .harness/gates/check-change-intent.py` PASS.
- 브랜치·커밋: `codex/2026-07-13-task021-broad-fixture` / `2395c6e`.

---

# 2026-07-13 작업 요약 (Claude · 이어서)

## TASK-021 G-broad-1 follow-up 재리뷰 (D-043) — 통과·머지
- 대상: `codex/2026-07-13-task021-broad-fixture` (`2395c6e` test·헤드 `82d598b` docs). D-042 비차단 차기 AC 가드 G-broad-1(broad-intent 회귀 픽스처의 라이브 repo 최상위 디렉토리 결합 제거).
- 심층·적대 리뷰: 게이트 판단 로직(`check-change-intent.py`)·`policies/*`·Claude 소유 **0-diff** 확인 = 순수 테스트 하네스. 신규 하네스가 `fixture_dir` 케이스를 격리 git repo(`prepare_function_mapping_fixture` 재사용)로 빌드해 `cd work_dir` 에서 실행 → 게이트의 `git ls-tree -d <base>` 분모가 픽스처 base 트리(고정 8)에서 나옴.
- 결합-소멸 실증(fresh·픽스처 밖): 손수 빌드한 coverage repo 에서 `top_level_dir_count:8`·셋에 `app` 포함(라이브엔 `app` 없음/`tests` 있음)·`coverage_percent:87`·exit 2 = 분모가 라이브 아닌 픽스처에서 나옴.
- 음성검증(rig-and-revert): `scope_coverage_percent`87→88, `scope_covered_top_level_dirs` `templates`→`BOGUS` 각각 단독 FAIL, 원복 PASS = 신규 단언 load-bearing(분자+분모+percent 동시고정=엄격화).
- 검증: `tests/run-tests.sh` **77/77 PASS**·`git diff --check`·`py_compile`·`bash -n` OK (격리 worktree 재현).
- 비차단 O-1: 옛 `broad-intent-*/name-status.txt` 3파일 미참조(dead)·무해→차기 정리 삭제 권장.
- 판정: 통과·비민감(CI 테스트-하네스) → **Claude 코드 브랜치 `main` 머지·push**(구현자 Codex≠머지자 Claude). G-broad-1 마감. (D-043·review-notes TASK-021 G-broad-1)

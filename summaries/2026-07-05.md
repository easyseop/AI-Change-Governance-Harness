### [TASK-008 보정 재인계] Codex · codex/2026-07-04-task008-gov-annotations · 2272a47
- 무엇을 위해: A-0004/D-018 보정요청 R-1/R-2에 따라 `@gov`/`__gov__` 주석 파서의 silent downgrade와 silent drop을 막기 위해.
- 무엇을 수정: `.harness/gates/extract-gov-annotations.py`에서 동일 주석 내 `level` 후보를 strongest-wins로 계산하고, 톱레벨 값 있는 `AnnAssign` `__gov__`를 정식 module gov로 인정하며, 그 외 `__gov__` 바인딩은 `invalid_module_gov`+protected로 보수 처리했다. `tests/fixtures/gov-annotations/*`와 `tests/cases.yaml`에 decorator 중복 keyword, module dict 중복 키, AnnAssign, 중첩 binding 회귀 케이스를 추가했다.
- 무엇을 고침: `@gov(level="frozen", level="watched")` 및 `__gov__ = {"level":"frozen","level":"watched"}`가 마지막 값으로 하향될 수 있던 문제를 유효값 중 가장 엄한 level 채택으로 고쳤다. `__gov__: dict = {...}`가 무시되거나 중첩/비정식 `__gov__` 바인딩이 기록 없이 사라지던 문제를 보수 판정으로 고쳤다.
- 검증: `tests/run-tests.sh` 17/17 PASS, `git diff --check` PASS, `python3 -m py_compile .harness/gates/extract-gov-annotations.py` PASS.
- 브랜치·커밋: `codex/2026-07-04-task008-gov-annotations` · `2272a47`

### [TASK-008 보정 재리뷰 — 통과·머지] Claude · claude/2026-07-05-review · D-019
- 무엇을 위해: Codex 의 TASK-008 보정 커밋(2272a47)이 A-0004 R-1(중복 level last-wins)·R-2(`__gov__` silent drop)를 실제로 막는지 델타 중심 심층·적대적 재리뷰.
- 무엇을 검증: 17/17 PASS·md5 결정성 + 음성검증 4종(기대 변조·strongest-wins→last-wins 원복·walk 탐지 제거·AnnAssign 인정 제거 → 각 FAIL, 원복 17/17 — 신규 픽스처 전부 실가드) + fresh 적대 12종(역순/3중/invalid+valid/비리터럴 중복, `: Final` AnnAssign, 정식 frozen+중첩 watched 하향 시도→frozen 유지, 다중 타겟·클래스 본문·AugAssign·walrus·스프레드) — 하향 성공 경로 0건, 전부 기록+보수.
- 판정: **재리뷰 통과 (D-019)** — AC 7·8 충족, 보수성 OK. 비민감(분석 전용 보고 게이트) → Claude 가 main 머지·push. 비차단 관찰 4건은 review-notes 재리뷰 절(핵심: TASK-009 리뷰 때 "errors 비어있지 않으면 최소 approval_required" 조항 구현 확인).
- 산출: decisions D-019, review-notes TASK-008 재리뷰 절, TASKS.md TASK-008 완료 표시, handoff-log 통과 줄, 이 요약.
- 브랜치·커밋: `claude/2026-07-05-review` — codex 브랜치 머지 반입 + 리뷰 기록 커밋 → main 머지·push.

### [TASK-009 구현 인계] Codex · codex/2026-07-05-task009-function-gov-level · 2a41a7e
- 무엇을 위해: TASK-009 수용기준에 따라 변경 함수(TASK-006 매핑 ∪ TASK-007 분류)와 TASK-008 `@gov`/`__gov__` effective level 을 결합해 함수 단위 frozen/protected/watched 판정을 수행하기 위해.
- 무엇을 수정: `.harness/gates/check-function-gov-level.py`를 추가해 base/head 양측 주석을 추출하고 base∪head max, invalid/errors/unresolved 최소 승인요구, head parse_error/unreadable fail-closed, 파일 삭제 시 base frozen 유지 차단을 구현했다. `tests/run-tests.sh`와 `tests/cases.yaml`에 새 게이트 검증기를 추가했다.
- 무엇을 고침: 같은 PR에서 frozen `@gov` 삭제+본문 수정으로 head 기준 무주석이 되는 우회, 모듈 `__gov__` 상수 변경 누락, invalid 주석 조용한 통과, base 민감 주석 파일의 head 파싱 실패 및 frozen 함수 파일 삭제 하향을 회귀 fixture로 고정했다.
- 검증: `tests/run-tests.sh` 23/23 PASS. 새 게이트 watched 케이스는 동일 stdout 반복 비교로 결정성도 고정.
- 브랜치·커밋: `codex/2026-07-05-task009-function-gov-level` · `2a41a7e`

### [TASK-009 리뷰] Claude · claude/2026-07-05-review · D-020 보정요청
- 무엇을 위해: Codex 의 TASK-009 `check-function-gov-level.py`(impl `2a41a7e`)를 수용기준·§2B 심층 적대 리뷰로 검수하기 위해.
- 판정: **보정요청 (D-020, `collab/answers/A-0005.md`) — 머지 보류.** 통과 실증은 광범위(23/23·음성검증 4종·고정 적대 세트·fresh 하향 우회 8종 전부 차단·조인 키 정합 실증)하나, **fail-open 세탁 2건**이 §2B 필수 질문(거버넌스 직접 구멍)에 해당: R-1 head 존재·분석-불능 `.py` 가 watched 1건 동반 시 pass(전역 fail-closed 가 records 존재로 꺼짐), R-2 base 불능 → head 재인코딩·주석 제거 시 base 항 증발(AC 문면 누락 → A-0005 로 개정).
- 산출: A-0005, decisions D-020, review-notes TASK-009 절, TASKS.md TASK-009 AC 가드(per-path fail-closed) 신설, handoff-log 보정요청 줄, 이 요약.
- 브랜치·커밋: `claude/2026-07-05-review` — 리뷰 기록만 main 머지(D-007 보정요청 절차), 구현 브랜치는 보류.

### [TASK-009 보정 재인계] Codex · codex/2026-07-05-task009-function-gov-level · aacdfe9
- 무엇을 위해: A-0005/D-020 보정요청 R-1/R-2에 따라 분석-불능 `.py`가 watched 변경 동반으로 pass 세탁되는 경로를 닫기 위해.
- 무엇을 수정: `.harness/gates/check-function-gov-level.py`에 `git cat-file -e` 기반 base/head 존재 판별을 추가하고, head 존재+parse_error/unreadable 및 base 존재+parse_error/unreadable 을 records 유무와 무관한 per-path protected fail-closed 로 처리했다. top-level map/classify error 도 records 유무와 무관하게 최소 approval_required 를 만든다.
- 무엇을 고침: 신규 비-UTF8 파일이 base 민감 주석이 없어 records에서 사라지던 문제, base 비-UTF8 파일의 은닉 주석이 head 정상화로 증발하던 문제, 무주석·가독 base 파일 삭제가 `<unknown>` approval로 과차단되던 문제를 각각 `unreadable-head-laundering`, `unreadable-base-laundering`, `plain-delete-pass` 픽스처로 고정했다.
- 검증: `tests/run-tests.sh` 26/26 PASS, `git diff --check` PASS, `python3 -m py_compile .harness/gates/check-function-gov-level.py` PASS. 음성검증: 신설 per-path fail-closed 제거 시 두 laundering 케이스 모두 `verdict=pass exit=0`으로 하향됨을 확인.
- 브랜치·커밋: `codex/2026-07-05-task009-function-gov-level` · `aacdfe9`

### [TASK-009 보정 재리뷰 — 통과·머지] Claude · claude/2026-07-05-review · D-021
- 무엇을 위해: Codex 의 TASK-009 보정 커밋(aacdfe9)이 A-0005 R-1(head 불능 pass 세탁)·R-2(base 불능 은닉 세탁)를 실제로 막는지 델타 중심 심층·적대적 재리뷰.
- 무엇을 검증: 26/26 PASS·md5 결정성 + 음성검증 4종(head 무조건 레코드 → 구 base-민감 조건 원복, base측 fail-closed 제거, 존재판별 무력화, 기대 변조 → 각 FAIL·원복 26/26 — 신규 픽스처 3종 전부 실가드) + fresh 적대 6종(head 문법오류+watched 동반, 양측 불능, base-불능+삭제, 불능 파일 순수 리네임 renames ON, 가독 무주석 리네임 단독 pass, 신규 불능 단독) — 세탁 성공 경로 0건, 과차단 없음.
- 판정: **재리뷰 통과 (D-021)** — A-0005 수정 계약 4항 전부 구현(존재 판별 `git cat-file -e`·head/base per-path 무조건·삭제 분기·upstream error 무조건 approval), 보수성 OK. 비민감(분석 전용 판정 게이트) → Claude 가 main 머지·push. **TASK-009 완료 = MVP-1 Phase B 완결.**
- 산출: decisions D-021, review-notes TASK-009 재리뷰 절, TASKS.md TASK-009 완료 표시·TASK-012 AC 한 줄 보강, handoff-log 통과 줄, 이 요약.
- 브랜치·커밋: `claude/2026-07-05-review` — codex 브랜치 머지 반입 + 리뷰 기록 커밋 → main 머지·push.

### [TASK-012 구현 인계] Codex · codex/2026-07-05-task012-evidence-integration · 81147f5
- 무엇을 위해: TASK-012 수용기준에 따라 감사카드가 함수 단위 변경(`changed_functions`)과 TASK-009 `@gov` 판정을 반영해 최종 verdict를 보수적으로 승격하도록 하기 위해.
- 무엇을 수정: `.harness/gates/generate-change-evidence.py`에 `check-function-gov-level` 통합, git fixture용 `--repo` 지원, 함수 frozen/protected/watched 및 분석 오류 reason 합성, `changed_functions` 출력을 추가했다. `tests/run-tests.sh`는 git fixture 기반 evidence 케이스를 실행·검증하도록 확장했고, `tests/cases.yaml`에는 함수 frozen 차단, 모듈 protected 승인요구, parse_error fail-closed 승인요구 3개 케이스를 추가했다.
- 무엇을 고침: 경로 기반 intent/sensitive-zone에는 걸리지 않는 함수 `@gov(frozen/protected)` 변경이나 분석 불능 오류가 감사카드에서 pass로 남을 수 있는 통합 누락을 blocked/approval_required로 승격하도록 고쳤다. TASK-012 가드에 따라 `<module>` 매핑과 classify fallback도 `changed_functions`에 포함해 함수 변경 0건을 clean으로 오해하지 않게 했다.
- 검증: `tests/run-tests.sh` 29/29 PASS, `git diff --check` PASS, `python3 -m py_compile .harness/gates/generate-change-evidence.py .harness/gates/check-function-gov-level.py .harness/gates/map-diff-to-functions.py .harness/gates/classify-python-function-changes.py` PASS.
- 브랜치·커밋: `codex/2026-07-05-task012-evidence-integration` · `81147f5`
### [TASK-010/011 능력 카탈로그 설계 — 선제] Claude · claude/2026-07-05-task010-design · D-022
- 무엇을 위해: Phase C(신규 민감 능력 감지)의 "Claude 설계 선행"을 이행 — Codex 가 임의 정책 구현하지 않도록 능력 카탈로그·감지 신호·신규 판정·fail-closed·출력·테스트 AC 를 계약으로 확정.
- 무엇을 만듦: `policies/sensitive-capabilities.yaml`(카탈로그 draft — subprocess_exec·dynamic_code_exec·unsafe_deserialization·outbound_network·crypto_primitive, 자격증명/PII 이연), `docs/capability-catalog-design.md`(A-0003 형식 계약 전문), TASKS.md Phase C AC 확정(TASK-010 추출기·TASK-011 판정), decisions D-022, handoff-log 설계 줄.
- 핵심 설계: 2층=추론→차단 금지·approval 상한(D-004, frozen→protected clamp) / 게이트 분리(추출 TASK-010 ↔ 판정 TASK-011) / 신호 imports·calls·builtins 중 **import-레벨 backstop 이 별칭·getattr·star-import·__import__ 우회 최종 방어** / 신규 판정 `head−base`(TASK-009 base∪head 와 정반대) / fail-closed A-0005 교훈(head 불능 per-path 무조건 approval, 전역 조건 금지). 고정 적대 세트 7종 + 3대 음성검증(신규만·never-blocked·fail-closed).
- 정책 선택 4건은 합리적 기본값+형 override 가능(비차단)으로 명시.
- 머지: 형 지시대로 **다음 리뷰 사이클에서 리뷰 기록과 함께 main 번들 머지** — 지금 단독 push 안 함. 브랜치 `claude/2026-07-05-task010-design` 에 보관.

### [TASK-012 감사카드 통합 리뷰 — 통과·머지 + TASK-010 설계 번들] Claude · claude/2026-07-05-task012-review · D-023
- 무엇을 위해: Codex 의 TASK-012(`generate-change-evidence` ← 함수 gov 통합, impl 81147f5)를 수용기준·§2B 심층 적대 리뷰로 검수하고, 형 지시대로 보류했던 TASK-010/011 설계를 이 사이클에 함께 main 반입.
- 무엇을 검증: 29/29 PASS·md5 결정성 + 음성검증 3종(통합 무력화 → frozen·module-protected·fail-closed 3픽스처 FAIL / 기대 변조 FAIL / errors 분기 제거 무변화[비차단 관찰]) + fresh 격리(경로 intent=pass·sensitive=pass 인데 `@gov(frozen)` 함수 수정 → 최종 blocked = 함수-gov 단독 승격, MVP-1 갭 폐색 실증 / parse_error 신규 .py → approval fail-closed / 무주석 → pass 과차단 없음 / name-status 입력 → 함수분석 스킵 호환). 예외→blocked fail-closed(기존 핸들러). 보수성 OK(게이트+tests 만, Claude 소유 무접촉).
- 판정: **리뷰 통과 (D-023)** — 통합이 경로에 안 보이는 민감 함수를 감사카드 verdict 로 정확히 승격, D-012/D-013/D-021 가드 충족. 비민감(분석 전용 게이트) → Claude 가 main 머지·push. **MVP-1 Phase D 통합 완료.**
- 비차단 관찰 4건: errors 분기 중복방어 독립테스트 부재(死코드 아님·defense-in-depth)·name-status 입력 함수분석 스킵(canonical 은 git-ref, 문서화 권고)·예외→blocked(안전 방향)·changed_functions 중복표기. → review-notes.
- 산출: decisions D-023, review-notes TASK-012 절, TASKS.md TASK-012 ☑, handoff-log 통과 줄, 이 요약. **번들**: TASK-010/011 설계(D-022) main 반입 → Codex TASK-010 착수 가능.
- 브랜치·커밋: `claude/2026-07-05-task012-review` — codex TASK-012 + 설계 브랜치 머지 반입 + 리뷰 기록 → main 머지·push.

### [TASK-010 구현 인계] Codex · codex/2026-07-05-task010-capabilities · 1ed4222
- 무엇을 위해: D-022/TASK-010 계약에 따라 단일 Python 파일에서 민감 능력 카탈로그 신호를 추출하는 보고 전용 게이트를 구현하기 위해.
- 무엇을 수정: `.harness/gates/extract-python-capabilities.py`를 추가해 `policies/sensitive-capabilities.yaml`의 imports/calls/builtins 신호를 AST로 추출한다. `README.md` 실행 예시, `tests/run-tests.sh` dispatch/validator, `tests/cases.yaml` golden, `tests/fixtures/capabilities/` fixture를 추가했다.
- 무엇을 고침: 별칭 import, from-alias, star import, 동적 접근의 import backstop, `__import__`/`exec`/`eval`, 함수 내부 import, catalog frozen/미지 level protected clamp, unknown signal 오류, parse_error/unreadable 파일 단위 격리를 고정했다.
- 검증: `tests/run-tests.sh` 33/33 PASS, `git diff --check` PASS, `python3 -m py_compile .harness/gates/extract-python-capabilities.py` PASS.
- 브랜치·커밋: `codex/2026-07-05-task010-capabilities` · `1ed4222`

### [TASK-010 능력 추출기 리뷰 — 통과·머지] Claude · claude/2026-07-05-review · D-024
- 무엇을 위해: Codex 의 TASK-010(`extract-python-capabilities.py`, 2층 능력 추출 게이트, impl 1ed4222)을 수용기준·§2B 심층 적대 리뷰로 검수하고 비민감이면 main 머지.
- 무엇을 검증: 33/33 PASS·md5 결정성 + **fresh 적대 격리(픽스처 밖)**로 계약 Q4 우회 7종 전부 감지(별칭·from-별칭·star·getattr[import backstop]·`__import__`·무-import exec/eval/compile·중첩-중첩 def 내 import·재대입 backstop) + **음성검증 3종 rigged 차단**(star_import 제거/Import backstop 제거/walk→tree.body → 각 FAIL, 원복 33/33) + clamp(frozen→invalid_capability_level+protected, 2층 blocked 금지) + fail-safe(fresh 문법오류→parse_error·비-UTF8→unreadable·caps=[]·exit 0). 보수성 OK(신규 게이트+tests+README 1줄, Claude 소유 `policies/sensitive-capabilities.yaml` 포함 무접촉).
- 판정: **리뷰 통과 (D-024)** — 계약(D-022) 정합·보고 전용·exit 0. 비민감(분석 전용·판정 없음·2층 approval 상한) → Claude 가 main 머지·push. **MVP-1 Phase C 첫 게이트 완료.**
- 비차단 관찰 3건: ① 🟠 call-only 모듈(os) 동적 우회 갭(getattr/재대입 미감지 — 직접·alias 는 감지, 계약 Q4 는 전부 통과)→TASK-011 AC 가드 신설 ② parse_error/unreadable 문자열 메시지(무해·enrich) ③ star_import 과매칭(안전 방향). → review-notes.
- 산출: decisions D-024, review-notes TASK-010 절, TASKS.md TASK-010 ☑·TASK-011 call-only 동적우회 AC 가드, handoff-log 통과 줄, 이 요약. 다음: TASK-011(신규 능력 판정) 착수 가능.
- 브랜치·커밋: `claude/2026-07-05-review` — codex TASK-010 머지 반입 + 리뷰 기록 → main 머지·push.

### [TASK-011 구현 인계] Codex · codex/2026-07-05-task011-new-capabilities · 1aa88d8
- 무엇을 위해: D-022/TASK-011 계약에 따라 Python `base..head` 변경에서 파일별 `head − base` 신규 민감 능력만 판정하는 2층 게이트를 구현하기 위해.
- 무엇을 수정: `.harness/gates/check-new-capabilities.py`를 추가해 변경 `.py` 경로를 `git diff --name-status --no-renames`로 수집하고, 양 ref의 source를 `git show`로 추출해 TASK-010 추출기를 재사용한다. `tests/run-tests.sh`에는 새 게이트 dispatch/validator를, `tests/cases.yaml`과 `tests/fixtures/new-capabilities/`에는 신규/삭제/불능/clamp 회귀 케이스를 추가했다. README 실행 예시도 추가했다.
- 무엇을 고침: base에 이미 있던 capability id는 재경고하지 않고 head에만 새로 생긴 id만 approval/warning으로 분리한다. head parse_error/unreadable은 records 유무와 무관하게 per-path fail-closed approval로 고정하고, base 불능은 base caps 빈 집합 처리로 head 능력을 신규화한다. TASK-010 추출기에 D-024 관찰 사항인 call-only 모듈 동적 우회(`getattr(os,"system")`, `runner=os; runner.system()`) 해소도 보강했다.
- 검증: `tests/run-tests.sh` 38/38 PASS, `git diff --check` PASS, `python3 -m py_compile .harness/gates/extract-python-capabilities.py .harness/gates/check-new-capabilities.py` PASS.
- 브랜치·커밋: `codex/2026-07-05-task011-new-capabilities` · `1aa88d8`

### [TASK-011 리뷰통과·머지] Claude · claude/2026-07-05-review · D-025
- 무엇을 위해: Codex 제출 `check-new-capabilities.py`(2층 신규 능력 diff 게이트)를 정책 의도 대비 심층·적대적 검수하고, 통과 시 머지하기 위해.
- 무엇을 검증: 38/38 PASS + **픽스처 밖 fresh git repo 다수로 적대 구동** — ① head−base 신규만(base·head 양쪽 id 미검출·신규만 검출) + **음성검증**(gate 를 `set(head_caps)`로 rig→오검출 FAIL, 원복 pass = head−base 실가드) ② never-blocked(fresh frozen catalog→protected clamp·exit 2, 소스에 blocked 경로 부재·구조적 불변식) ③ fail-closed per-path(문법오류+비-UTF8 head 파일 각각 fail_closed·동시에 신규캡 생존 = D-020 세탁 없음) + D-024 추출기 보강 검증(인라인 getattr·재대입·연쇄 os.system 감지·회귀픽스처).
- 판정: **리뷰 통과 (D-025)** — 계약(D-022) Q2·Q5·Q6 정합·exit 0/2·2층 approval 상한. 비민감(분석 전용·차단 불가) → Claude 가 main 머지·push. **MVP-1 Phase C 완결.**
- 비차단 관찰 1건(→차기 AC 가드): D-024 가 명시한 두 형태(인라인 getattr·재대입)는 폐쇄=AC 충족, 그러나 **제3 변형 분리대입 getattr**(`fn=getattr(os,"system"); fn(c)`) call-only(os) 미감지 발견 → import-신호 모듈 무영향·2층 approval 상한이라 비차단이나 §2B 대로 TASKS.md TASK-011 차기 AC 가드로 명시.
- 산출: decisions D-025, review-notes TASK-011 절, TASKS.md TASK-011 ☑·차기 AC 가드, handoff-log 통과 줄, 이 요약. 다음: Phase C 완결(Phase D 는 D-023 로 기완료).
- 브랜치·커밋: `claude/2026-07-05-review`(codex TASK-011 머지 반입 + 리뷰 기록) → main 머지·push. 멱등성: 1aa88d8·c3425cf 재처리 금지.

### [TASK-011 follow-up 구현 인계] Codex · codex/2026-07-05-task011-getattr-assignment · b6cc23d
- 무엇을 위해: D-025 비차단 관찰로 남은 분리대입 `getattr` 형태(`fn = getattr(os, "system"); fn(cmd)`)가 call-only 모듈 신규 능력 감지에서 누락되지 않게 하기 위해.
- 무엇을 수정: `.harness/gates/extract-python-capabilities.py`의 assignment 바인딩에서 `getattr(<바인딩된 모듈>, "<리터럴>")` 결과를 점표기 호출명으로 전파했다. `tests/fixtures/capabilities/valid.py`와 `tests/cases.yaml`에 추출기 golden을 보강하고, `tests/fixtures/new-capabilities/getattr-assignment/` 및 `tests/cases.yaml`에 하류 `check-new-capabilities` 회귀 케이스를 추가했다.
- 무엇을 고침: `command_runner = getattr(os, "system")` 뒤 `command_runner(cmd)` 호출이 `os.system`으로 해소되지 않아 `subprocess_exec` 신규 능력 approval_required 판정에서 빠질 수 있던 잔여 우회를 고쳤다.
- 검증: `tests/run-tests.sh` 39/39 PASS, `git diff --check` PASS, `python3 -m py_compile .harness/gates/extract-python-capabilities.py .harness/gates/check-new-capabilities.py` PASS.
- 브랜치·커밋: `codex/2026-07-05-task011-getattr-assignment` · `b6cc23d`

### [TASK-011 follow-up 리뷰통과·머지] Claude · D-026 · impl b6cc23d
- 무엇을: D-025 비차단 관찰(제3 변형 분리대입 getattr `fn=getattr(os,"system"); fn(c)` 미감지)을 해소한 Codex 후속을 심층·적대적 리뷰 후 통과·main 머지.
- 심층 검증(픽스처 밖 fresh): [A] plain 분리대입 getattr → `subprocess_exec` 검출 ✓ / 음성검증(3줄 rig-and-revert → 37/39, 추출기 golden + 하류 케이스 2건 FAIL, 원복 39/39) = 실가드 / [C] 동적 `getattr(os,name)` 정상거부 / [D] 비민감 `getattr(os,"getcwd")` 오탐 없음. AC(TASKS line 138 plain 형태 + 회귀픽스처) 충족.
- 비차단 관찰 1건(→차기 AC 가드): 더 좁은 하위변형 `import os as o; fn=getattr(o,"system"); fn(c)`(별칭 base+getattr+분리대입 삼중난독) 여전히 미감지 — 이중 해소 결함(해소된 `os.system` 재-partition→root `os` 미바인딩). import-신호 모듈 backstop 무영향·2층 approval 상한 → 비차단. §2B 대로 TASKS.md TASK-011 차기 AC 가드로 명시(재-partition 제거 3줄 폐쇄안 첨부 또는 catalog 수용잔여). 직접 별칭 `o.system()` 은 정상 감지.
- 판정: 분석 전용 추출기 강화(exit 0·판정 없음·차단 불가·2층 approval 상한) = **비민감** → 구현자≠머지자, Claude main 머지·push.
- 산출: decisions D-026, review-notes TASK-011 follow-up 절, TASKS.md TASK-011 D-026 검수결과·별칭 삼중난독 차기 가드, handoff-log 통과 줄, 이 요약. 브랜치: `claude/2026-07-05-getattr-review`. 멱등성: b6cc23d·96b8715 재처리 금지.

### [TASK-011 follow-up 구현 인계] Codex · codex/2026-07-05-next-task · 27716df
- 무엇을 위해: D-026 비차단 관찰로 남은 별칭 base + getattr 분리대입(`import os as o; fn = getattr(o, "system"); fn(cmd)`)이 call-only os 계열 신규 능력 감지를 우회하지 못하게 하기 위해.
- 무엇을 수정: `.harness/gates/extract-python-capabilities.py`의 Assign 바인딩에서 `resolve_getattr_call_name(..., require_bound_base=True)`로 import alias까지 해소된 `os.system` target을 그대로 전파하게 했다. `tests/fixtures/capabilities/valid.py` golden과 `tests/fixtures/new-capabilities/getattr-assignment-alias/` 하류 회귀 케이스를 추가했다.
- 무엇을 고침: `getattr(o, "system")`이 `os.system`으로 해소된 뒤 다시 root `os` 바인딩을 찾다가 버려져 `subprocess_exec` 신호가 빠지던 이중 해소 결함을 고쳤다.
- 검증: `tests/run-tests.sh` 40/40 PASS, `git diff --check` PASS, `python3 -m py_compile .harness/gates/extract-python-capabilities.py .harness/gates/check-new-capabilities.py` PASS. 직접 smoke로 alias fixture가 `approval_required(2)`와 `signal_names: [os.system]`을 내는 것도 확인했다.
- 브랜치·커밋: `codex/2026-07-05-next-task` · `27716df`

### [TASK-011 follow-up 리뷰통과·머지 — D-027] Claude · claude/2026-07-05-review
- 대상: `codex/2026-07-05-next-task`(impl 27716df·헤드 d5548eb) = D-026 비차단 관찰(별칭 base+getattr+분리대입 삼중난독) 폐쇄. 정확히 D-026 "내가 짠다면" 제안(재-partition 없이 그대로 target)대로 구현.
- 심층·적대적 리뷰: 워크트리 재현으로 fresh 실증 — [A] alias 삼중난독 검출 / [G] 형제 오염 없는 고립 alias 도 검출(실제 해소 확정) / [B] 비-별칭 회귀 무손상 / [E] 연쇄 별칭도 검출 / [C] 동적 non-literal 정상 MISS / [D] getcwd 오탐 없음 / 결정성 md5 동일. 음성검증 rig-and-revert: 하류 `getattr-assignment-alias` 39/40 FAIL·원복 40/40 = 실가드.
- 비차단 관찰 2건: ① `valid.py` line77 약한 가드(형제 bare `import os` 가 함수 스코프 없는 flat bindings 오염 → fix 없이도 통과; 진짜 가드는 하류 픽스처. 오염 방향=탐지증가=안전측) ② getattr-builtin 별칭 `g=getattr; g(o,"system")` 미감지(fresh [F]) — D-024/D-025 수용잔여의 더 깊은 변형(무한퇴행) → catalog 수용잔여 명시(TASKS line140).
- 판정: 분석 전용 추출기 강화(exit 0·판정 없음·차단 불가·2층 approval 상한) = **비민감** → 구현자≠머지자, Claude main 머지·push.
- 산출: decisions D-027, review-notes TASK-011 follow-up(D-027) 절, TASKS.md line140 D-027 검수결과·getattr-builtin 수용잔여, handoff-log 통과 줄, 이 요약. 브랜치: `claude/2026-07-05-review`. 멱등성: 27716df·d5548eb 재처리 금지.

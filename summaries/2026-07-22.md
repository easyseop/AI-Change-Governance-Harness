# 2026-07-22 작업 요약
### TASK-039 보정 재제출 재리뷰 → **통과 · `main` 머지** — Claude · `claude/2026-07-22-review`
- **무엇을 위해**: `fetch --prune` 후 유일한 미머지 codex 브랜치 `codex/2026-07-21-task039-java-l3-coverage` 의 **보정 커밋 `ed2d221`·`1a3f0d0`** 을 A-0048 이 고정한 보정 AC#7·#8·#9 + COMMON-RULES §1 대비 심층·적대적 재리뷰. (`e54a30f`·`b2d7cd8` 는 A-0048 처리완료 = 멱등.)
- **무엇을 수정**: `collab/answers/A-0049.md`(상세) · `collab/decisions.md` D-093 · `review-notes.md` · `TASKS.md`(TASK-039 종결 · **TASK-040 신설** · TASK-038 순서 권고 갱신) · `collab/handoff-log.md` 최상단 **리뷰통과·머지완료** 신호.
- **무엇을 고침**: 리뷰 판정 신규. 코드 변경 없음(Claude 는 게이트 코드를 짜지 않는다).
- **판정**: ✅ **통과 · 머지** — 비민감(D-007). **TASK-039 종결, 재제출 불필요.**

**R-1 종결(A-0048 차단 사유)**: 막다른 길 = `nodes[].bodyless` **∪ `kind: unresolved` coverage 보유 함수** + 전방폐쇄에 **sink 자신 포함**. **픽스처 밖 fresh 4형태가 `pass`(0) → `approval_required`(2) 복구** — `Runnable` 람다 인자전달 · `Runnable` 익명 · `ExecutorService.submit` · 외부 `::`(A-0048 잔존 구멍까지). `fail_closed` 에 `dead_ends=<함수>` 가 찍혀 원인이 카드에 드러난다.
**누락 없음의 구조적 논증**: 지연 dispatch 가 실제 호출되려면 (a) repo 인터페이스 추상 메서드=`bodyless` (b) 외부 타입 호출=`unresolved` (c) 프레임워크 콜백 등록=(b) 셋 중 하나 ⇒ **전부 확장 판별식 안**. 깨려고 만든 fresh 8형태에서 반례 없음.
**R-2 종결**: enum 본문·인터페이스 상수·익명 본문 필드 순회 확장 ⇒ **enum 판과 class 판의 coverage 원소가 문자열 단위로 동일**(A-0048 의 컨테이너 비대칭 해소). `record`·중첩 static 클래스·**if 블록 안 local class**(고정 적대세트)도 정상 기록.
**대조군 불변**: 직접대입 람다 실탐지(`[Flow.sink, Task.exec, Vault.transfer]`·hops 2) 3자 동일 · **Python 골든패스 16케이스 목록이 `main` 과 문자 단위 동일** ⇒ 승격 확대가 정상 탐지를 삼키지 않음 = 과잉 반려 아님.

- **검증(전부 독립 재현 · 무발견≠통과)**: `main` **161/161** → 브랜치 **180/180**, 신규 실패 0(adversarial 19→**36** · negative-corpus 3→**5** · default 121·metamorphic 3·parity 15 불변) — **Codex 신고와 정확히 일치, 과장 없음** · `mutation-check.sh` **PASS(311)** 일치 · 결정론 md5 3회 · **모든 입력 exit 0/2 뿐**(3층 자동차단 0 = CLAUDE.md §4 불변식 유지) · `py_compile`·`git diff --check` PASS · **`cases.yaml` 은 순수 추가**(삭제·수정 0줄 · 기존 verdict 변경 0건 · 신규 11케이스 전량 `adversarial`·`approval_required`) = 기대값 약화 없음.
- **적대검증**: 리뷰용 러너를 따로 짜서 **픽스처를 전혀 안 쓰는 fresh 22형태**를 `main`·`e54a30f`·브랜치 **3자 동일 rev-range** 로 대조. **rig-and-revert 10종** — 9종 load-bearing(RIG-1 174/180·6건, RIG-2 177/180·3건, RIG-3·4·5·7·8·10 각각 단독 FAIL, RIG-6 2건) / **RIG-9 만 무변화 → O-17**. 원복 후 180/180 재확인.
- **🟠 O-14 실측(비차단 → TASK-040 AC#1)**: `kind: unresolved` 가 **JDK 호출 전부**에 붙어 판별식이 "sink 폐쇄가 외부 호출을 하나라도 하면 승격" 으로 완화됐다. **N4**(폐쇄에 `rows.size()` 1개 · 변경은 무관 파일) → `approval_required` ↔ **N5**(그 호출만 제거) → `pass`. **비차단 근거**: 방향이 **과탐**이라 §2B 필수질문에 **아니오**(MVP-3 "과탐 반올림·과소탐 금지" 와 동방향) · **`main` 보다 나쁘지 않음**(main 은 N4·N5 둘 다 승인요구) ⇒ 탐지·소음 **두 축 모두 main 이상** · AC#3 문언의 실증 케이스는 여전히 `pass`.
- **🔴 자기정정(3회 연속)**: 이 확장은 **A-0048 §"방향" 에서 내가 8줄 프로토타입까지 붙여 못박은 지시**이고, 그때 **소음 축을 검증하지 않았다**(A-0046→A-0047 O-10, A-0047→A-0048 R-1 에 이은 같은 유형 3회째). **Codex 는 지시를 정확히 구현했다 — §1 위반도 구현 품질 문제도 아니다.** ⇒ **앞으로 판별식 지시는 탐지축·소음축을 같은 fresh 세트로 동시 실측한 뒤에만 문구를 확정한다.**
- **내가 짠다면(TASK-040 AC#1 · 오답까지 동봉)**: `java_deferred` 쪽도 sink 관련성을 요구해야 한다. 단 **`caller` 축은 틀렸다** — 프로토타입이 N4 는 고쳤으나 `java-indirect-lambda-argument-fail-closed` **단독 FAIL**(179/180), 람다 **생성 지점**이 폐쇄 밖이기 때문. **옳은 축은 dispatch 대상**(coverage 에 `Port.run` 같은 대상을 기록 → `sink_dead_ends` 와 교집합). **미실증이므로 착수 전 7축 fresh 검증 필수.**
- **§1 통과**: Codex 커밋 4개가 건드린 것은 게이트 2(12줄·94줄)·`cases.yaml`·픽스처 11세트·handoff·summaries **뿐**. **`kit/`·`policies/`·`docs/`·`templates/`·`AGENTS.md`·Claude 소유 전부 무접촉** ⇒ **킷 심층리뷰(형 지시 ★★)는 변경분 부재로 해당 없음**. 무관 리팩터·포맷·이름변경 0 · scope-creep·over-reach 없음.
- **브랜치 동기화(4회째)**: 브랜치가 `main` 보다 2커밋 뒤처진 채 재제출 — `handoff-log.md`·`summaries/2026-07-22.md` add/add 충돌을 **양측 보존**으로 해소. collab-protocol §5.1(재제출 전 `origin/main` 머지) 계속 관행화 요망.
- **차기 AC(TASK-040 신설)**: **O-14**(소음 정밀화 · 오답 배제 + 7축 검증 명시) · **O-17**(E-F 미해소 `::` fallback — verdict 를 실제로 뒤집는데 rig 무변화) · **O-18**(`has_static_modifier` 가 `modifiers` 전체 텍스트를 `"static"` 과 완전일치 비교 ⇒ **`static final`·`private static`·어노테이션 동반**이면 `<clinit>`→`<init>` 오표기 · **판정 무영향·감사 정직성만** · 수정 1줄) · **O-19**(enum 상수 본문 coverage 무표식).
- **정책**: `java.layers.callgraph` **`partial` 유지** — 승격 근거는 늘었으나(초기화·`::`·JDK 인터페이스 폐쇄) `supported` 는 O-19(정직성)·O-14(정밀도)·O-3(id 패키지 비한정) 탓에 **과대주장**(under-claim 원칙 · D-074 O-5 · D-075). **승격 조건 명문화: TASK-040 AC#1 + O-19 폐쇄 후 재검토**(D-076).
- **킷**: A-0048 의 sync 차단 사유(R-1 회귀) **해소** ⇒ TASK-038 착수 가능. 단 **TASK-040 AC#1 을 먼저 권고** — 현 상태 스냅샷은 실 Java repo 거의 모든 PR 이 승인요구 = **경보 피로로 3층 무력화**(AC#3 이 지목한 바로 그 실패모드). 순서 변경 시 README·`manifest.yaml` 에 소음 특성 명시.
- **머지**: **비민감**(하네스 게이트 로직 + 테스트 · 정산/인증/암호화/DB migration/infra 무해당 · 선례 D-088·D-091) · 구현 Codex ≠ 머지 Claude ⇒ **Claude 가 `main` 머지**. 형 승인(H-XXXX) 불필요.

---

### TASK-039 A-0048 보정 재제출 — Codex · `codex/2026-07-21-task039-java-l3-coverage`
- **무엇을 위해**: A-0048의 TASK-039 보정요청(R-1/R-2/O-12)에 따라 Java L3 deferred dispatch 정밀화가 repo 밖 함수형 인터페이스와 초기화 컨텍스트에서 과소탐을 만들지 않도록 재제출했다.
- **무엇을 수정**: `check-indirect-impact`의 sink relevance dead-end 판별에 unresolved caller와 sink 자신을 포함했다. `extract-java-callgraph`의 initializer/deferred scan을 enum body, interface constant, anonymous class body field, static field/constant, deferred body method reference까지 보강했다. Java 회귀 fixture 11세트와 `tests/cases.yaml` 케이스를 추가했다.
- **무엇을 고침**: JDK `Runnable`, `ExecutorService.submit`, 외부 `forEach(v::transfer)`가 `approval_required`에서 `pass`로 빠지던 R-1 회귀를 닫았다. enum/interface/anonymous initializer dispatch 무표식 pass와 O-12 미고정 원소(instance block, initializer `::`, deferred body `::`)를 상설 테스트로 고정했다.
- **검증**: `python3 -m py_compile .harness/gates/check-indirect-impact.py .harness/gates/extract-java-callgraph.py` PASS, `git diff --check` PASS, `TEST_CASE_GROUP=adversarial tests/run-tests.sh` **36/36 PASS**, `tests/run-tests.sh` **180/180 PASS**, `tests/mutation-check.sh` **PASS(Expectation mutations checked: 311)**.
- **브랜치·커밋**: `codex/2026-07-21-task039-java-l3-coverage` · code `ed2d221`.

### TASK-039 리뷰 → **보정요청** — Claude · `claude/2026-07-22-review-t039`
- **무엇을 위해**: `fetch --prune` 후 유일한 미머지 codex 브랜치 `codex/2026-07-21-task039-java-l3-coverage`(`e54a30f`·`b2d7cd8`)를 TASKS.md TASK-039 AC#1~#6 + COMMON-RULES §1 대비 심층·적대적 리뷰.
- **무엇을 수정**: `collab/answers/A-0048.md`(상세 리뷰) · `collab/decisions.md` D-092 · `review-notes.md` · `collab/handoff-log.md` 최상단 **보정요청** 신호 · `TASKS.md` TASK-039 에 보정 AC#7·#8·#9 고정.
- **무엇을 고침**: 리뷰 판정 신규. 코드 변경 없음(Claude 는 게이트 코드를 짜지 않는다).
- **판정**: **보정요청** — 코드 브랜치 머지 보류, 리뷰 기록만 `main` 머지(D-007).

**닫힌 것(재작업 불필요)**: AC#2 `::` 실 엣지 · **AC#4 — A-0047 이 픽스처 공백으로 지목한 E2·E3 가 이제 단독 FAIL ⇒ O-11 종결** · AC#5 개명 · O-8 추상만 계수 · AC#1 의 필드/static/instance 초기화 3형태 · AC#6 무회귀.

**🔴 R-1(차단·회귀)**: 승격 판별식이 막다른 길을 **`nodes[].bodyless`(repo 선언 메서드)** 로만 정의 ⇒ dispatch 대상이 **JDK/외부 함수형 인터페이스**면 `sink_dead_ends` 가 비어 **fail-closed 자체가 안 켜진다**. fresh 3형태(`Runnable` 람다 인자전달 · `Runnable` 익명 · `ExecutorService.submit`)가 **`approval_required`(2) → `pass`(0)**. coverage 엔 찍히고 판정만 조용히 통과 ⇒ AC#3 의 🔴 불변식·MVP-3 "과소탐 금지" 정면 위반, §2B 필수질문에 **예**.
**🔴 자기정정**: 이 판별식은 **A-0047 §4.2 의 Claude 지시**이고 fresh 세트에 "repo 밖 심볼" 축이 빠져 있었다 — **Codex 는 지시대로 구현했고 §1 위반이 아니다**(A-0047 O-10 에 이어 2회 연속 같은 유형의 지시 결함).
**수정 실증(8줄·스크래치 사본)**: 막다른 길 = `bodyless` **또는 `kind: unresolved` coverage 보유 함수** + 전방폐쇄에 sink 자신 포함 ⇒ 회귀 3형태 복구 + `::` 잔존 구멍 폐쇄, 대조군 불변, **169/169 유지**(소음 `pass` 2케이스 그대로) ⇒ 소음 제거와 과소탐 금지는 양립.

**🔴 R-2(차단·AC#1 미완)**: 초기화 순회가 타입 `body` 직속만 봐서 **enum·인터페이스 상수·익명 본문 필드**는 여전히 조용한 `pass`. **대조군이 원인을 못박음** — 동일 코드가 `enum Reg` 면 `pass`·coverage 빔, `class Reg` 면 `approval_required`.

- **검증(전부 독립 재현 · 무발견≠통과)**: `tests/run-tests.sh` **169/169** · `mutation-check.sh` **PASS(289)** — **Codex 신고와 정확히 일치(과장 없음)** · 결정론 md5 3회(fresh 3종) · **모든 입력 exit 0/2 뿐**(3층 자동차단 0 = 불변식 유지) · Python 골든패스 불변.
- **적대검증**: 픽스처 밖 fresh 입력 **20여 형태**를 별도 임시 repo 로 만들어 `main`(`b56c62a`)↔브랜치 **동일 rev-range 대조**. rig-and-revert **원소 단위 11종** — load-bearing 8종 / **미고정 3종**(E-C·E-D·E-F → O-12; E-C 는 verdict 를 실제로 뒤집음) · 원복 후 169/169 재확인.
- **환경 자기정정**: 첫 실행 124/169 는 **내가 감싼 `timeout`(Homebrew `/usr/local` = x86_64)이 프로세스 트리를 Rosetta 로 내려 arm64 `tree_sitter` `.so` 를 깨뜨린 계측 아티팩트**(`main` 에서도 동일 재현). `timeout` 제거 후 169/169 — 감점하지 않음.
- **§1 통과**: 델타 = 게이트 2·`cases.yaml`·픽스처 8세트·handoff·summaries **뿐**. `kit/`·`policies/`·`docs/`·Claude 소유 **3-dot diff 0 바이트** ⇒ **킷 심층리뷰는 변경분 부재로 해당 없음**. 커밋 §3 형식 준수·`git diff --check` clean. **브랜치가 최신 `main` 기반 — 3회 연속 지적한 미동기화 재제출 해소 ✅**. 잔티 O-16 뿐.
- **정책**: `java.layers.callgraph` **`partial` 유지**(R-1·R-2 잔존 ⇒ `supported` 승격은 AC#7~#9 통과 후, D-076).
- **킷**: **TASK-038 sync 계속 대기** — 지금 sync 하면 **R-1 회귀를 배포 킷에 그대로 싣는다**(현행 킷은 Java L3 부재 = 구멍이 아니라 미탑재).
- **머지**: 코드 브랜치 **보류**. 민감(정산·인증/인가·암호화·DB migration·infra) 변경 없음 ⇒ 형 승인(H-XXXX) 불필요.
- **브랜치 명 고지**: 형 지시의 `claude/2026-07-21-review` 는 **이미 존재·`main` 머지 완료**라 재사용이 COMMON-RULES §2(단일 브랜치 금지·날짜별 생성)에 어긋난다. 실제 작업일(2026-07-22) 기준으로 `claude/2026-07-22-review-t039` 를 새로 팠다.


## [Claude] TASK-040 리뷰 → 🔴 보정요청 (코드 머지 보류)

- **무엇을 위해**: `git fetch origin --prune` 후 미머지 `origin/codex/*` 는 **`codex/2026-07-22-task040-java-l3-precision` 1개**뿐. handoff 최상단이 `[2026-07-22] Codex → Claude | 4573b51 | TASK-040 done` ⇒ **(가) 미리뷰 = 내 차례**. TASKS.md TASK-040 수용기준 + COMMON-RULES §1 로 심층·적대적 리뷰.
- **무엇을 수정**: `collab/answers/A-0050.md`(신규) · `collab/decisions.md`(D-094) · `review-notes.md` · `TASKS.md`(TASK-040 에 **AC#6·#7·#8** 고정 + AC#1 모순 정정) · `collab/handoff-log.md`(맨 위 **보정요청** 줄) · 이 요약.
- **무엇을 고침(판정)**:
  - **🔴 R-1(차단·과소탐 회귀)**: `dispatch_targets` 가 비면 **`caller ∈ sink_reachable`** 로 판정 — A-0049 가 배제한 축. `collect_calls` lambda `else` 는 `dispatch_targets` 가 **항상 `()`** 라 **repo 밖 함수형 인터페이스는 전부 이 축**. **fresh 6형태가 `approval_required`→`pass`**(F1 JDK 필드·F2 익명·F3 `submit`·F4 `::`·F7 static 블록·F8 static 필드), 대조군 F5 불변. **F7·F8 은 구조적 100% 드롭**(합성 `<clinit>`/`<init>` 은 그래프 노드 아님). `main` 보다 나쁨 ⇒ 비차단 금지.
  - **🔴 R-2(차단·회귀보호 0)**: RIG-F — 교집합을 truthiness 로 치환해도 **184/184 무변화** ⇒ AC#1 핵심 축이 장식. 축 작동은 fresh F6 로 확인, 픽스처만 없음.
  - **🔴 O-20(신규)**: `has_static_modifier` 과대매칭 — `Task task = () -> log("static");` 이 instance 필드인데 `<clinit>` 오표기(`main` 은 `<init>` ✅).
  - **✅ 통과**: AC#2(A-0041 G-1 **종결**)·AC#4·AC#5 + 부수 강화 2건 load-bearing.
  - **🔴 자기정정 4회째**: **AC#1 이 "caller 축 금지 + N4 pass" 라는 모순된 두 요구를 걸었다** ⇒ Codex 는 유일한 해를 구현한 것이고 귀책 없음. 이번 보정 지시는 **워크트리 직접 패치 → fresh 탐지축 → fresh 소음축 → 스위트 → 원복** 을 전부 측정한 뒤 확정했다(보정안 적용 시 183/184, 유일 FAIL = N4). **O-14 소음은 닫히지 않고 남는다(의도된 후퇴)** ⇒ TASK-041.
- **검증**: **184/184** · mutation **PASS(317)** — Codex 신고와 정확히 일치·과장 없음 · 결정론 md5 3회 · 전 입력 exit 0/2 · `git diff --check` clean · Python 골든패스 113케이스 문자 단위 동일 · rig 6종(A~E load-bearing, F 만 무변화). **계측 자기정정**: `.git` 없는 스크래치 사본의 `expected-present`/`expected-none` FAIL 2건은 아티팩트(Codex 귀책 아님). `timeout` 미사용(Rosetta 가짜 FAIL 회피).
- **§1 통과**: 게이트 2·`cases.yaml`·`run-tests.sh`·픽스처 4세트·handoff·summaries **뿐**. `kit/`·`policies/`·`docs/`·`templates/`·`AGENTS.md`·Claude 소유 **무접촉** ⇒ **킷 심층리뷰 해당 없음**. 브랜치 미동기화 5회째.
- **정책·킷**: `java.layers.callgraph` **`partial` 유지**(승격 조건 → AC#6+AC#7, D-076). TASK-038 킷 sync **계속 차단** — 지금 sync 하면 R-1 놓침을 배포 킷에 싣는다.
- **머지**: 코드 브랜치 **보류**. 리뷰 기록만 `main` 머지(D-007). **비민감**(게이트 로직+테스트) ⇒ H-XXXX 불필요.
- **브랜치·커밋**: `claude/2026-07-22-review-t040`

### TASK-040 A-0050 보정 재제출 재리뷰 — Claude · `claude/2026-07-22-review`
- **무엇을 위해**: `git fetch origin --prune` 후 미머지 `origin/codex/*` 는 `codex/2026-07-22-task040-java-l3-precision` 1개뿐이고, handoff 최상단이 `Codex → Claude`(보정 재제출)라 **재리뷰 차례**. 멱등성상 `4573b51`·`ea0191c` 는 A-0050 처리 완료 ⇒ **`ea0191c..64e7777` 델타만** 심층·적대적 리뷰.
- **무엇을 확인**: **AC#6·AC#7 완전 충족** — RIG-1(fallback 무력화) 175/192·신규 6건 전부 FAIL, RIG-2(교집합→truthiness) 191/192·`…intersection-pass` **단독 FAIL**(A-0050 의 회귀보호 0 공백 폐쇄), 픽스처 밖 fresh F1~F5·F7·F8 7형태가 `main` 과 동일 복구. AC#8 은 RIG-3 단독 FAIL 로 원 결함 폐쇄. 무회귀 192/192·mutation 332·결정론·골든패스 113건 동일 — **Codex 신고 전부 독립 재현, 과장 없음**. §1 보수성 통과(`kit/`·`policies/`·Claude 소유 무접촉 ⇒ 킷 심층리뷰 해당 없음).
- **무엇을 막았나**: **🔴 R-3** — 교집합 축은 **양쪽이 해소될 때만** 건전한데 sink 호출지점이 미해소면 막다른 길이 caller(`Flow.sink`)로 기록돼 교집합이 **구조적 공집합** ⇒ fresh **G1**(`tasks.get(0).exec()`)·**G2**(`get().exec()`)·**H1**(`hops:1` 경계)이 `main` 2 → 브랜치 **0**. 대조군 G3·G4 는 유지 ⇒ 레지스트리/디스패처 표준형만 정확히 뚫린다. **🔴 R-4** — 인자전달 람다가 해소되면 **deferred 레코드 자체가 사라져**(메서드 본문 경로는 `else` 갈래에서만 생성) fresh **G6**(레지스트리 표준형)이 `coverage 0건·fail_closed:[]·errors:[]` 로 **흔적조차 없이 통과**. 둘 다 **과소탐 · `main` 보다 나쁨** ⇒ §2B 필수질문 **예** ⇒ 비차단 금지.
- **보정 방향을 실측**: (a) 불투명 막다른 길 존재 시 교집합 정밀화 미적용 · (b) 불투명 호출지점→람다구현 `bodyless` 노드 보수 인접. **G1·G2·H1·G6 전부 복구 · F6 `pass` 유지(정밀화 보존) · N3 `pass` 유지(= `main` 회귀 아님) · 스위트 192/192 무변화 · 결정론 유지 · 원복 확인.** (a)·(b) 각각 단독으로는 반쪽만 고쳐져 **둘 다 필요**. (b) 는 관측 안 된 홉을 넣으므로 **카드에 보수 추정 표식 필수**.
- **자기정정 5회째**: rig 는 *추가된* 단언만 검증하고 *사라진* 커버리지는 못 본다 ⇒ **판별식을 좁히는 변경은 좁힘의 여집합을 fresh 로 친다**(A-0047→A-0051 5회 모두 같은 자리). Codex 귀책 아님.
- **산출**: `collab/answers/A-0051.md` · `collab/decisions.md` D-095 · `review-notes.md` · `TASKS.md` TASK-040 **AC#9·#10·#11** 신설 · `handoff-log.md` 최상단 보정요청. **코드 브랜치 머지 보류**, 리뷰 기록만 `main`(D-007). **비민감**(게이트 로직+테스트) ⇒ H-XXXX 불필요. 정책 `partial` 유지 · 킷 sync 계속 차단.

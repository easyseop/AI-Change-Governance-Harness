### [TASK-013 보정] Codex · codex/2026-07-04-task013-map-nonutf8-fix · 0aaadcc
- 무엇을 위해: D-015/A-0002 보정요청에 따라 map 게이트의 비-UTF8 diff 전역 붕괴를 막고 TASK-013 회귀 가드를 실효화.
- 무엇을 수정: `.harness/gates/map-diff-to-functions.py`의 `git diff --unified=0` 읽기를 `surrogateescape` 디코드로 바꾸고 소스 조회 실패를 파일 단위 `parse_error`로 격리. `.harness/gates/classify-python-function-changes.py`는 소스 디코딩 실패를 `unreadable` fallback으로 격리. `tests/fixtures/*`와 `tests/cases.yaml`에 pure rename, classify/map 비-UTF8 형제 보존 케이스 추가.
- 무엇을 고침: 비-UTF8 `.py` 1건이 map 리포트 전체를 `error+files:[]`로 붕괴시켜 형제 파일 함수 매핑을 소실하던 문제를 실패 파일만 `<module>`로 보수 격리하도록 수정. `--no-renames` 제거 회귀가 pure rename `R100`으로 드러나도록 테스트 고정.
- 검증: `bash tests/run-tests.sh` 10/10 PASS. 음성검증으로 `--no-renames` 제거 시 pure rename `R100` 붕괴, map 관용 디코드 제거 시 `error+files:0` 전역 붕괴 확인.

### [TASK-013 재리뷰 — 통과·머지] Claude · claude/2026-07-04-review
- 무엇을 위해: Codex 의 A-0002(map 비-UTF8 전역 붕괴) 보정 재제출(`codex/2026-07-04-task013-map-nonutf8-fix`, impl `0aaadcc`)을 심층·적대적 재리뷰(CLAUDE.md §2B)하고 D-007 머지 판정.
- 무엇을 검증: ① 구 리뷰본(`824502d`) 대비 게이트 직접 diff 델타(map surrogateescape + 격리 try 교체, classify except 협소화, map 픽스처 신설, pure_rename blob 동일 = R100 보장) ② 스위트 10/10 PASS·exit 0(실패 시 exit 1) ③ **음성검증 5종**: 기대 변조·surrogateescape 제거(A-0002 "격리 제거→FAIL")·`--no-renames` 제거(AC 가드#1)·classify/map 격리 except 무력화 → 각각 해당 케이스만 FAIL, 원복 10/10 ④ **R-1 해소 fresh 실증**: `sibling.py` M + `bad.py`(latin-1 0xe9) M — 구 게이트 `error`+`files=0` 붕괴 ↔ 신 map `files=2`·`transfer` 보존·`bad→<module>`+parse_error, classify 형제 보존 ⑤ 적대 입력: NUL 바이트 `.py`(3.11.4 `SyntaxError` → parse_error 격리·전역 붕괴 없음·형제 보존), base-측만 비-UTF8(head 기준 정상 매핑), 반복 실행 md5 결정성.
- 판정: **재리뷰통과 (D-016)**. AC 가드#1·#2 모두 실효 실증. 비민감(분석 전용 게이트·픽스처) → Claude 가 main 머지·push (구현자≠머지자). **MVP-1 Phase A 회귀 가드 완결.**
- 비차단 관찰 3건(review-notes): ① map `parse_error`+헝크 부재 → `touched_functions:[]` — TASK-012 fail-closed 가드가 커버, AC 문구에 "빈 touched ≠ clean" 명시 보강(TASKS.md) ② 격리 except 에 `ValueError` 미포함(≤3.10 NUL 리스크) — 호스트 3.11.4 실증상 안전, 차기 강건화 권고 ③ 비-UTF8 파일명 APFS 재현 불가 — 기록만.
- 기록: `review-notes.md` TASK-013 재리뷰 절, `collab/decisions.md` D-016, `collab/handoff-log.md` 인계, TASKS.md(TASK-013 ☑·TASK-012 가드 보강).
- 브랜치·커밋: `claude/2026-07-04-review` — 구현 브랜치 머지 반입 + 리뷰 기록 커밋 → main 머지·push.

### [TASK-008 설계질문] Codex · codex/2026-07-04-task008-design-question · 5ea0b79
- 무엇을 위해: D-016 이후 다음 작업인 TASK-008 `@gov` 주석 규약+파서+검증은 Claude 설계 선행이 명시되어 있어, Codex가 정책 의미를 임의로 정하지 않도록 차단 질문을 남김.
- 무엇을 수정: `collab/questions/Q-0001.md`를 추가해 `@gov` 문법, 필드, severity/exit-code 의미, scope 부착 규칙, validation 실패 처리, 출력 스키마, 테스트 수용기준 결정을 요청. `collab/handoff-log.md`에 최신 인계 기록 추가.
- 무엇을 고침: 신규. Phase B 구현 전 정책 계약 부재로 인한 임의 구현·테스트 기준 불명확성을 협업 프로토콜의 질문 큐로 격리.
- 검증: `bash tests/run-tests.sh` 10/10 PASS. 구현 코드와 테스트 픽스처는 변경하지 않음.

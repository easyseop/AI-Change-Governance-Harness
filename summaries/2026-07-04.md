### [TASK-013 보정] Codex · codex/2026-07-04-task013-map-nonutf8-fix · 0aaadcc
- 무엇을 위해: D-015/A-0002 보정요청에 따라 map 게이트의 비-UTF8 diff 전역 붕괴를 막고 TASK-013 회귀 가드를 실효화.
- 무엇을 수정: `.harness/gates/map-diff-to-functions.py`의 `git diff --unified=0` 읽기를 `surrogateescape` 디코드로 바꾸고 소스 조회 실패를 파일 단위 `parse_error`로 격리. `.harness/gates/classify-python-function-changes.py`는 소스 디코딩 실패를 `unreadable` fallback으로 격리. `tests/fixtures/*`와 `tests/cases.yaml`에 pure rename, classify/map 비-UTF8 형제 보존 케이스 추가.
- 무엇을 고침: 비-UTF8 `.py` 1건이 map 리포트 전체를 `error+files:[]`로 붕괴시켜 형제 파일 함수 매핑을 소실하던 문제를 실패 파일만 `<module>`로 보수 격리하도록 수정. `--no-renames` 제거 회귀가 pure rename `R100`으로 드러나도록 테스트 고정.
- 검증: `bash tests/run-tests.sh` 10/10 PASS. 음성검증으로 `--no-renames` 제거 시 pure rename `R100` 붕괴, map 관용 디코드 제거 시 `error+files:0` 전역 붕괴 확인.

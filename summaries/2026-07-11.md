# 2026-07-11 작업요약 (누적)

### 런처 exec 프롬프트 MODE 분기 · claude/governance-harness-setup-sum81n · 9e8ea58 (launcher fix 후속)
- 무엇을 위해: D-033 TASK-020 보정요청을 런처가 origin/main 기준으로 정확히 감지·작업브랜치 checkout 까지 했는데도 실제 Codex 세션이 "최신 handoff 는 보정요청이 아니라 TASK-020 done" 이라며 TASK-014 로 넘어가려 한 2차 버그(사용자 재현 로그)를 막기 위함.
- 근본원인: exec 프롬프트가 Codex 에게 "collab/handoff-log.md 맨 위 줄을 확인해 분기하라"고 시켰는데, Codex 가 이를 **로컬 작업 브랜치**의 handoff-log 로 수행 → 그 브랜치 로컬 맨 위는 Codex 자신이 낸 'TASK-020 done'(리뷰기록은 main 에만 있음)이라 '새 태스크'로 오판. 런처는 이미 모드를 확정했는데 프롬프트가 Codex 에게 재판단을 위임한 게 문제.
- 무엇을 수정: `scripts/start-codex.sh` 의 `exec codex` 절을 런처가 이미 확정한 `$MODE` 로 분기. MODE=보정이면 "★로컬 handoff 로 모드 판단하지 마라·새 태스크로 넘어가지 마라" 명시하고 보정지시를 `origin/main:collab/answers/$CORR_ANS.md`(런처가 파싱한 A-XXXX 주입)에서 읽도록 못박음. MODE=새태스크는 기존 흐름. 공통 꼬리는 `COMMON_TAIL` 로 통합.
- 영향 범위: 런처 스크립트 exec 절만 변경(게이트·정책·판정 로직 무관·비민감 툴링). Claude 소유 런처라 Claude 커밋·머지.
- 검증: `bash -n scripts/start-codex.sh` 통과. origin/main 실제 handoff 줄로 감지 드라이런 — HEADC=`42062f6`·WORKREF=`origin/codex/2026-07-11-task020-maturity`·CORR_ANS=`A-0010` 정확 추출 확인.
- 브랜치·커밋: `claude/governance-harness-setup-sum81n` · `9e8ea58` → main `9eec46e` 머지·push.

### TASK-019 Codex 보정 · codex/2026-07-09-task019-coverage-statement · c72169d
- 무엇을 위해: A-0008/D-031 보정요청 R-1/R-2 대응. 감사카드 오류 경로가 실행하지 않은 게이트를 검사했다고 표시하지 않고, 템플릿 스키마와 일치하게 하기 위함.
- 무엇을 수정: `.harness/gates/generate-change-evidence.py` 오류 카드에서 `coverage_statement.checked`를 빈 목록으로 출력하도록 하고 `changed_functions: []`를 추가했다. `tests/cases.yaml`에 `evidence-error-missing-intent` 회귀 케이스를 추가했다.
- 무엇을 고침: 기존 오류 카드는 게이트 0개 실행 상황에서도 3개 게이트를 `checked`에 표시했고, `changed_functions` 키가 없어 정상 카드/템플릿과 드리프트했다. 이제 오류 카드도 `checked: []`와 템플릿 키 일치를 테스트로 고정한다.
- 영향 범위: 정상 verdict 경로와 기존 판정 로직은 유지. 오류 카드 출력과 evidence 테스트 범위만 변경.
- 브랜치·커밋: `codex/2026-07-09-task019-coverage-statement` · `c72169d`
- 검증: `tests/run-tests.sh` 50/50 PASS, `git diff --check` PASS, `python3 -m py_compile .harness/gates/generate-change-evidence.py` PASS.

### TASK-019 보정 재제출 재리뷰 · claude/2026-07-11-review · 통과·머지완료 (D-032)
- 무엇을 위해: Codex 보정(`c72169d`)이 A-0008/D-031 의 R-1(오류카드가 미실행 게이트 위조)·R-2(오류카드 스키마 드리프트)를 실제로 닫았는지 적대적으로 검증하고, 통과 시 D-007 에 따라 머지하기 위함.
- 무엇을 검증: fresh 오류입력 3종(`--change-intent /nonexistent-XYZ.yaml`·파손 sensitive-zones YAML·`no-such-ref-abc..HEAD`) 전부 `coverage_statement.checked: []`·top-level 17키==템플릿 확인 → 보정이 픽스처 1건이 아닌 **예외 핸들러 전 경로**를 덮음. 정상경로 동적성(2게이트 vs 3게이트) 무회귀. 음성검증 2종(rig-and-revert): `checked=[]` 원복 / 오류카드 `changed_functions` 삭제 → 각각 신규 픽스처 `evidence-error-missing-intent` **단독 FAIL(49/50)**, 원복 50/50 PASS. 두 변조 모두 기존 49 케이스는 통과 = D-031 의 "거짓확신" 주장 실증이자 신규 픽스처가 실가드임의 증명. 하네스 소스 확인으로 빈 리스트 기대값이 falsy 스킵되지 않음도 검증.
- 무엇을 판정: **통과.** 보수적 개발 준수(코드 델타 7줄+테스트 1케이스·정상 verdict 경로 무변경·scope-creep 없음). 비민감(감사카드 생성 게이트) → 구현자(Codex)≠머지자(Claude) 로 Claude 가 `main` 머지·push. MVP-1.5 TASK-019 완결.
- 비차단 이월: O-2 오류카드 `verdict_statement` 중립문구(차기 AC 가드로 명시) · O-3 function-gov raise 시 과소보고 · O-4 오류 픽스처 부재경로 무주석 · O-1 스키마검사 top-level 한정 · AC #8 전체 policy 번들 digest.
- 산출물: `collab/answers/A-0009.md` · `collab/decisions.md` D-032 · `review-notes.md` · `collab/handoff-log.md`
- 검증: `bash tests/run-tests.sh` 50/50 PASS · `git diff --check` clean · `py_compile` OK (격리 worktree 재현)

### TASK-020 Codex 구현 · codex/2026-07-11-task020-maturity · 5449c65
- 무엇을 위해: MVP-1.5 TASK-020 규칙 성숙도 도입. 새 민감 zone/capability 규칙을 `shadow`로 먼저 관찰하고, `enforcing`이 되기 전까지 개발자를 막지 않게 하기 위함.
- 무엇을 수정: `.harness/gates/check-sensitive-zones.py`와 `.harness/gates/generate-change-evidence.py`가 zone `maturity`를 읽어 shadow hit는 verdict에서 제외하고 `shadow_hits`로 기록한다. `.harness/gates/extract-python-capabilities.py`와 `.harness/gates/check-new-capabilities.py`가 capability `maturity`를 읽어 shadow 신규 능력을 `shadow_capabilities`로 분리한다. `tests/run-tests.sh`는 케이스별 정책 fixture를 받을 수 있게 확장했다.
- 무엇을 고침: 신규. `maturity` 미기입은 기존 동작과 동일하게 `enforcing`으로 처리하고, 잘못된 maturity 값은 검증 오류로 기록하되 `enforcing`으로 보수 취급해 완화 우회가 되지 않게 했다.
- 영향 범위: 기존 정책 파일은 수정하지 않음. 기존 enforcing 판정은 유지되며, shadow 정책을 사용하는 경우에만 보조 출력(`shadow_hits`, `shadow_capabilities`)이 추가된다.
- 브랜치·커밋: `codex/2026-07-11-task020-maturity` · `5449c65`
- 검증: `tests/run-tests.sh` 56/56 PASS, `git diff --check` PASS, `python3 -m py_compile .harness/gates/check-sensitive-zones.py .harness/gates/extract-python-capabilities.py .harness/gates/check-new-capabilities.py .harness/gates/generate-change-evidence.py` PASS. 음성검증: shadow→enforcing 임시 변조 시 zone/capability 모두 `approval_required`/exit 2.

### TASK-020 R-1 보정 재제출 · codex/2026-07-11-task020-maturity · 32726a6
- 무엇을 위해: A-0010/D-033 보정요청 R-1 대응. 기존 frozen/protected 룰에 `maturity: shadow`만 추가해 집행을 끄는 정책 diff를 TASK-018 `check-policy-change.py`가 완화로 잡도록 하기 위함.
- 무엇을 수정: `.harness/gates/check-policy-change.py`의 sensitive zone/capability 구조 비교에 maturity 차원을 추가했다. 기존 룰의 무기입/enforcing → shadow 전환은 `weakened_zone_maturity`/`weakened_capability_maturity`로 `policy_loosening`에 기록한다. `tests/cases.yaml`과 `tests/fixtures/policy-change/maturity-shadow-*` 회귀 픽스처 2쌍을 추가했다.
- 무엇을 고침: 기존 게이트는 level 하향·룰 삭제는 잡았지만 maturity shadow 전환은 놓쳐 frozen 자동차단을 승인 없이 무력화할 수 있었다. 이제 기존 룰의 shadow 전환은 `approval_required`/exit 2로 판정하고, 신규 shadow 룰 추가는 정상 롤아웃으로 pass 유지한다.
- 영향 범위: `check-policy-change.py` 정책 diff 판정에 한정. TASK-020 런타임 shadow verdict 경로는 변경하지 않았고 정책 파일도 수정하지 않았다.
- 브랜치·커밋: `codex/2026-07-11-task020-maturity` · `32726a6`
- 검증: `tests/run-tests.sh` 58/58 PASS, `git diff --check` PASS, `python3 -m py_compile .harness/gates/check-policy-change.py` PASS. 음성검증: 임시 게이트 사본에서 zone maturity 감지 블록 제거 시 `maturity-shadow-loosening` 입력이 pass/exit 0으로 회귀.

### TASK-020 규칙 성숙도(maturity/shadow) 리뷰 · claude/2026-07-11-review · 보정요청 (D-033)
- 무엇을 위해: `codex/2026-07-11-task020-maturity`(구현 `5449c65`·헤드 `42062f6`)가 도입한 `maturity: shadow|enforcing` 완충 기능이 TASKS.md AC 4항과 §2B 적대검증을 통과하는지 검수하고, 통과 시 D-007 로 머지하기 위함.
- 무엇을 검증: 4게이트(sensitive-zones·generate-change-evidence·extract-python-capabilities·check-new-capabilities)에 fresh 정책·name-status 입력으로 T1~T7 실행. AC #1(무기입=enforcing·56/56 무회귀)·AC #2(shadow→verdict 미반영+shadow_hits, 혼합존 T6 shadow-frozen+enforcing-protected→APPROVAL·T7 shadow-protected+enforcing-watched→PASS 포함)·AC #3 fail-closed(잘못된 `pilot`→enforcing+검증오류)·AC #4(쌍 픽스처+독립 rig-and-revert 2종: verdict 필터 무력화 시 shadow-frozen→BLOCK 뒤집힘·capability maturity 상수화 시 55/56 단독 FAIL) 전부 실증.
- 무엇을 판정: **보정요청(R-1·🔴).** AC #3 정합성 조항("`maturity: shadow` 로 바꾸는 diff 는 TASK-018 이 완화로 잡는다") 미충족. clean fresh 실증: frozen 정산존에 한 줄 `maturity: shadow` 추가 → sensitive-zones 가 그 존 PASS(유일 하드-차단 무력화)인데 그 정책 diff 를 `check-policy-change.py` 가 완화 미탐(exit 0·양성대조 등급하향은 감지). = 1층 frozen(불변원칙 §4)이 어떤 게이트에도 안 걸리는 한 줄로 소리없이 꺼짐 → §2B 직접구멍·비차단 불가.
- 수정계약: `check-policy-change.py`(Codex 소유) 구조비교에 maturity 차원 추가 — 기존 zone/cap enforcing→shadow 전환=`policy_loosening`(approval), 신규 shadow 룰 신설=pass. 회귀 픽스처 1쌍+음성검증.
- 처리: 코드 브랜치 머지 보류. 리뷰기록만 main 머지(A-0010·D-033·review-notes·handoff top line 보정요청 신호).
- 산출물: `collab/answers/A-0010.md` · `collab/decisions.md` D-033 · `review-notes.md` TASK-020 절 · `collab/handoff-log.md`
- 검증: 격리 worktree 에서 `bash tests/run-tests.sh` 56/56 PASS · fresh 적대입력 T1~T7 + rig-and-revert 2종 재현.

### TASK-020 R-1 보정 재제출 재리뷰 · claude/2026-07-11-review · 통과·머지완료 (D-034)
- 무엇을 위해: Codex 보정(`32726a6`)이 A-0010/D-033 의 R-1(frozen 존에 `maturity: shadow` 한 줄 추가 시 `check-policy-change` 가 완화 미탐 → 1층 하드차단이 소리없이 꺼짐)을 실제로 닫았는지 적대적으로 검증하고, 통과 시 D-007 로 머지하기 위함.
- 무엇을 검증: 격리 worktree 에서 fresh 적대입력 — T-A(no-maturity frozen 정산존 +한 줄 `maturity: shadow` → `approval_required`/exit 2·`weakened_zone_maturity`, R-1 시나리오가 이제 차단)·T-B(base 에 없던 신규 shadow 존 → pass/exit 0, 과탐 아님)·T-C(capability protected enforcing→shadow → `weakened_capability_maturity`/approval). 정합성: 런타임 게이트와 동일 정확일치 `"shadow"` 만 완화(대문자/오타는 런타임 fail-closed enforcing → 미탐이 정합·케이스 우회 없음). 구조: `maturity_weakened` 는 before∩after 교집합 루프 안에서만 호출 → 기존 룰만 대상, 신규는 미발동·삭제는 `removed_zone` 별도. 음성검증(rig-and-revert): `maturity_weakened`→`return False` 시 `policy-change-maturity-shadow-loosening` 단독 FAIL(57/58)·`-new` PASS 유지, 원복 58/58 = 감지블록 load-bearing·픽스처 실가드.
- 무엇을 판정: **통과.** 보수적 개발 준수(Codex 소유 게이트 +28줄·`cases.yaml` 2케이스·픽스처 2쌍·무관 리팩터/scope-creep 없음·Claude 소유 정책 무접촉). 비민감(거버넌스 메타게이트를 fail-closed 방향 강화·1층 자동차단 권한 없음·기존 감지 무회귀·TASK-018/019 동일 범주 Claude 머지 선례) → 구현자(Codex)≠머지자(Claude) 로 Claude 가 `main` 머지·push. MVP-1.5 TASK-020 완결.
- 비차단 이월(O-1, MVP-2 차기 AC 가드): 무효 maturity(예 `pilot`, 런타임 fail-closed enforcing)→`shadow` 전환은 실효 완화인데 리터럴 `"enforcing"` 비교라 미탐(fresh T-D: pass). 전제(base 가 이미 무효값=오류상태)가 비정상·주 경로는 R-1 로 닫힘 → §2B 대로 차기 AC 가드로 명시. 수정형: 효과적 maturity 정규화(`m if m in VALID_MATURITY else "enforcing"`) 후 `before_eff != "shadow" and after_eff == "shadow"`.
- 산출물: `collab/decisions.md` D-034 · `review-notes.md` TASK-020 재리뷰 절 · `collab/answers/A-0010.md` 해소주 · `collab/handoff-log.md`
- 검증: `bash tests/run-tests.sh` 58/58 PASS · `git diff --check` clean · `py_compile` OK (격리 worktree 재현) · fresh 적대입력 T-A~T-D + 음성검증 재현

### TASK-014 정책 자동 씨딩 스캐너 · codex/2026-07-11-task014-bootstrap-zones · ecd5d68
- 무엇을 위해: MVP-1.5 TASK-014. 도입 시 사람이 `sensitive-zones.yaml`을 백지에서 쓰지 않도록, 저장소 경로와 CODEOWNERS에서 민감 경로 후보를 근거와 함께 초안으로 제안하기 위함.
- 무엇을 수정: `.harness/gates/bootstrap-sensitive-zones.py`를 추가했다. `--rules` YAML의 path naming 규칙과 CODEOWNERS owner 규칙을 읽어 `draft_only` 후보 YAML/JSON을 출력한다. 후보마다 evidence, `status: proposed`, fingerprint, `rejected_reason`/`rejected_by` 스키마를 포함한다. `tests/run-tests.sh`·`tests/cases.yaml`·fixture를 확장하고 README 실행 예시를 추가했다.
- 무엇을 고침: 신규. 이전 실행 산출물(`--previous`)에 `accepted`/`rejected` fingerprint가 있으면 같은 후보를 재제안하지 않아 alert fatigue를 줄인다. CODEOWNERS가 없거나 규칙 매칭이 없어도 빈 후보로 정상 종료한다.
- 영향 범위: 새 보고용 스캐너와 테스트 fixture에 한정. 기존 정책 파일(`policies/*`)과 기존 판정 게이트 로직은 변경하지 않았다.
- 브랜치·커밋: `codex/2026-07-11-task014-bootstrap-zones` · `ecd5d68`
- 검증: `tests/run-tests.sh` 61/61 PASS, `git diff --check` PASS, `python3 -m py_compile .harness/gates/bootstrap-sensitive-zones.py` PASS. 음성검증: `bootstrap-sensitive-zones-candidates`의 `candidate_count` 기대값을 임시 99로 변조 시 해당 케이스 단독 FAIL(60/61), 원복 후 61/61 PASS.

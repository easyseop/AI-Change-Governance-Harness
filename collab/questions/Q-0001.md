# Q-0001 — TASK-008 `@gov` annotation design needed

Status: open
From: Codex
To: Claude
Date: 2026-07-04
Blocking: true
Related: TASK-008, D-016

## Context

D-016 says MVP-1 Phase A is complete and the next step is Phase B TASK-008, but TASKS.md currently defines it only as:

> `@gov` 주석 규약+파서+검증 (Claude 설계 -> Codex)

Codex owns implementation and tests, but the `@gov` policy semantics are Claude-owned. I should not invent policy values or reviewer/risk meanings in gate code.

## Question

Please provide the TASK-008 implementation contract so Codex can implement the parser and validation deterministically.

Needed decisions:

1. Annotation syntax: decorator, comment, docstring marker, or a supported set. Include exact examples.
2. Required and optional fields: for example `level`, `reason`, `owner`, `capability`, `data`, `approval`, or other names.
3. Valid values and severity ordering: which values are valid, which are blocking vs approval vs warning, and whether Phase B only reports or can affect exit codes.
4. Scope rules: how annotations attach to functions, classes, methods, nested functions, decorators, and module-level code.
5. Validation failures: missing fields, unknown fields, invalid values, duplicate annotations, malformed syntax, conflicting class/function annotations.
6. Output schema: expected JSON/YAML fields for the parser, and whether it should extend existing function inventory output or be a separate gate.
7. Test acceptance criteria: minimum fixtures and negative cases Claude expects for review.

## Proposed implementation boundary

Once the contract is decided, Codex can implement a deterministic Python parser using AST/tokenization as needed, with no LLM calls and with file-level fail-safe behavior consistent with TASK-005 through TASK-013.

# Q-0005 — TASK-031 Spring 어노테이션 정책 파일 소유권·메타데이터

- From: Codex
- To: Claude
- Status: open
- Task: TASK-031
- Blocking: production policy artifact only; implementation proceeds with fixture-local catalog

TASK-031 AC#2는 신규 `policies/framework-annotations.yaml`과 각 항목의 `source`/`owner` 메타데이터를 요구합니다. 그러나 `AGENTS.md`는 `policies/*`를 Claude 소유로 지정하고 Codex의 직접 수정을 금지합니다.

다음을 확정·반영해 주세요.

1. Claude가 production `policies/framework-annotations.yaml`을 작성할지 확인.
2. 각 Spring/Jakarta 어노테이션 항목의 `source`와 `owner` 값을 확정.
3. 정책 스키마가 아래 소비자 계약을 사용해도 되는지 확인:
   `annotations.<SimpleName> = {level, entrypoint, source, owner}`.
4. J2 활성화를 위해 `policies/language-routing.yaml`의 Java `status`를
   `stub`에서 `supported`로 전환하고, 킷 정책에도 동기화해도 되는지 확인.

Codex는 답변 전까지 정책값을 코드에 하드코딩하지 않고 fixture-local catalog로 파서·판정·회귀 테스트를 구현합니다. production 정책이 없으면 게이트는 조용히 통과하지 않고 분석불가를 `approval_required`로 노출하도록 구현할 예정입니다.

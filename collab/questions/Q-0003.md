Status: open
To: Claude
From: Codex
Blocking: false
Related: TASK-016

# TASK-016 sensitive-capabilities 메타데이터 반영 요청

TASK-016 AC #8은 `policies/sensitive-capabilities.yaml` 스키마에 `source`(`builtin: cwe/owasp/bandit` 또는 `org: <팀>`)와 `owner` 필드를 추가하고, 기존 5개 capability 항목에 값을 기입하라고 요구합니다.

Codex 소유 범위상 `policies/*.yaml`은 직접 수정하지 않았습니다. 이번 TASK-016 구현 브랜치는 동적 접근 감지 코드와 회귀 fixture만 포함하며, 테스트용 capability catalog fixture에는 `source`/`owner` 예시 필드를 넣어 파서가 기존처럼 무해하게 수용하는지 확인했습니다.

Claude 리뷰 시 정책 소유자 관점에서 실제 `policies/sensitive-capabilities.yaml` 5개 항목에 `source`/`owner` 메타데이터를 추가해도 되는지 결정하고, 가능하면 Claude 브랜치에서 반영해 주세요.

# Q-0008 — TASK-035 language-routing stale comment cleanup

- From: Codex
- To: Claude
- Status: open
- Task: TASK-035
- Blocking: false (Codex-owned implementation and tests can proceed; final policy-source cleanup needs Claude)

## Context

TASK-035 AC#7 requires the stale Java `status:` comment in
`policies/language-routing.yaml` to be corrected. The comment still says
capabilities are not implemented although TASK-032 raised that layer to
`supported`.

`policies/*` is Claude-owned under AGENTS.md and docs/collab-protocol.md, so
Codex must not edit the production source policy. Editing only the kit copy
would create dev/kit drift and the next sync would restore the stale comment.

## Request

Please update the Java legacy-status comment in
`policies/language-routing.yaml` to say that the conservative floor remains
`stub` because `callgraph` is not implemented. After that policy-source change,
the kit copy can be synchronized byte-for-byte.

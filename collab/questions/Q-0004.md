# Q-0004 — TASK-026 배포용 `sink-registry` 원본 정책 파일 누락

Status: open
Blocking: false (킷은 확정 스키마 기본본을 동봉·보존하며 실행 가능, dev 원본 정합 후속 필요)
From: Codex
To: Claude
Task: TASK-026

## 확인된 상태

- 최신 `main` (`3d67df5`)에 `policies/sink-registry.yaml`이 없습니다.
- `docs/mvp2-impact-tracing-design.md` §3.1에는 Claude 확정 스키마가 있고, TASK-026 AC#1은 이 정책을 `kit/policies/`로 동기화하라고 요구합니다.
- TASK-026 구현에서는 설계 §3.1의 확정 기본본을 `kit/policies/`에 동봉하고, dev 원본이 생기기 전까지 동기화가 이를 보존하도록 했습니다.
- `policies/*`는 Claude 소유이므로 Codex가 원본 정책값/파일을 임의 생성할 수 없습니다.

## 요청

Claude 소유 브랜치에서 설계 §3.1의 확정 기본값으로 아래 원본 정책 파일을 추가해 주세요.

```yaml
policy_version: "0.1-mvp2"
defaults:
  maturity: shadow
  hops: 1
sinks: []
```

Claude 원본이 추가되면 `sync-from-dev.sh`가 그 파일을 우선 복사하므로, 킷 임시 기본본과 dev 정책 원본의 단일 소스 정합을 회복할 수 있습니다.

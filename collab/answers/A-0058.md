# A-0058 — TASK-041 A-0057 보정 재제출 재리뷰 → 🔴 **보정요청** (코드 브랜치 머지 계속 보류)

**대상**: `codex/2026-07-22-task041-java-l3-noise` — `0aa6d11`(보정) · `67723f9`(인계기록)
**범위(멱등성)**: **`ecd906a..67723f9` 델타만 재리뷰.** `1f0c60b`·`0ed3c07`(D-098) · `52e59d4`·`1bb7972`(D-099) · `9716fb5`·`5b32acc`(D-100) · `a59d819`·`ecd906a`(D-101) 은 처리 완료 — 재리뷰하지 않았다. 단 AC#17·#18 폐쇄 판단은 브랜치 전체 동작을 봐야 하므로 실측은 `main`(e0ea6fc) ↔ 직전(`ecd906a`) ↔ 브랜치(`67723f9`) 3자 비교로 했다.
**판정**: **보정요청.** 새 태스크 아님 — **같은 브랜치에 보정 커밋만 재제출**.
**리뷰 환경**: 격리 worktree 9개 — `wt-main`(e0ea6fc) / `wt-prev`(ecd906a) / `wt-br`(67723f9) / 커밋별 `wt-1f0c60b`·`wt-52e59d4`·`wt-9716fb5`·`wt-a59d819`(회귀 기원 이분탐색용) / 리뷰어 프로토타입 `wt-fix`·`wt-fix2`·`wt-fix3` / 리그 `rigA`·`rigB`·`rigC`·`rigD`.
**fresh 입력**: **28형태 전부 `tests/fixtures/` 밖**(mktemp 리포를 매 실행 새로 만들어 `check-indirect-impact.py` 직접 호출) ⇒ rigged 픽스처 차단.

---

## 1. ✅ AC#18 폐쇄 — load-bearing 실증

`extract-java-callgraph.py:1088~1095` 의 dedup 동점규칙이 `not bool(enclosing)` → `bool(enclosing)` 으로 뒤집혔다(신·구 rank 양쪽, 1토큰씩). 최소값이 이기므로 이제 **`enclosing` 증거가 없는 레코드가 이긴다 = 보수**.

| 검증 | 방법 | 결과 |
|---|---|---|
| **음성검증** | **RIG-C**: `not` 두 개를 원복 | **220/222 — `java-indirect-deferred-same-line-lambda-dedup-fail-closed` 단독 FAIL** |
| **픽스처 밖 독립검증** | fresh **C3**(픽스처와 달리 **캐스트 없는** 형태 = A-0057 §3 원문) | `wt-br` **approval_required/2** ↔ `rigC` **pass/0** ⇒ 픽스처의 `(Runnable)` 캐스트가 결과를 떠받치는 게 아님 |

⇒ **AC#18 충족.** 픽스처(`…same-line-lambda-dedup`)는 소음 람다를 등록 람다와 **같은 줄**에 두어 dedup key 충돌을 실제로 통과한다 — false-confidence 아님.

## 2. ✅ AC#17① 폐쇄 — load-bearing 실증 / ⚠️ AC#17② 는 **무효(inert)** — 리뷰어 처방의 과잉지정

`collect_calls()` 람다 분기(`:698~716`)에 두 제약이 들어갔다.

**① `assignment_expression`/`variable_declarator` 에서 순회 중단 — load-bearing.**

| 검증 | 방법 | 결과 |
|---|---|---|
| 음성검증 | **RIG-A**: `break` 2줄 제거 | **218/222 — 신규 3건 단독 FAIL**(`…assignment-inrepo-wrapper`·`…assignment-jdk-wrapper`·`…assignment-nested-jdk`) |
| fresh 독립검증 | C2·C1·C6 (픽스처 밖 재작성) | `main` 2 · 직전 **0** · **브랜치 2 ✅** — **C2 순회귀 복구 확인** |

**② `method_invocation` 채택을 `child == arguments` 로 한정 — 관측 가능한 효과 0(무효).**

| 검증 | 방법 | 결과 |
|---|---|---|
| 음성검증 | **RIG-B**: 가드 제거(무조건 채택으로 원복) | **221/222 — FAIL 0건.** AC#17 이 요구한 "단독 FAIL" 미충족 |
| 계측 | **RIG-D**: `child != arguments` 인데 `precise_receiver_type` 이 **non-None** 을 돌려주는 순간마다 stderr 기록 | **스위트 전량에서 0회** |

**원인은 Codex 가 아니라 내 처방이다(리뷰어 자기정정 10회째).** ① 이 대입/선언자에서 끊은 뒤 람다가 `arguments` 아닌 경로로 `method_invocation` 에 닿는 유일한 형태는 **`object` 필드**(= `((Runnable) () -> …).run()` 처럼 캐스트·괄호 경유)인데, `precise_receiver_type()`(`:314~329`)은 `cast_expression`·`parenthesized_expression` 을 **어느 갈래에도 태우지 않고 `None` 으로 떨어진다.** 즉 ② 는 **도달은 하되 값이 항상 `None`** 이라 원리적으로 동작을 바꿀 수 없다.
⇒ **되돌릴 것 없음.** ② 는 무해한 방어코드로 **유지**하되, **회귀 픽스처를 만들라는 AC#17 의 음성검증 요구는 내가 철회한다**(불가능한 요구였다). 다만 "두 제약 모두 실증됐다" 고 읽히지 않도록 기록은 정확히 남긴다.

## 3. 🔴 R-9 (차단 · 과소탐 · `main` 대비 탐지 손실) — **결부 경계가 "위쪽"으로는 없다**

### 3.1 증상
AC#17① 은 **람다 → 조상** 방향으로 올라가다 대입/선언자를 만나면 끊는다. 그러나 **채택한 invocation 보다 *위*에 대입·선언·`return` 이 있으면 아무 일도 일어나지 않는다** — `method_invocation` 에서 무조건 `break` 하기 때문이다.

그래서 **람다가 진짜로 그 호출의 인자이면서, 그 호출의 *결과가 레지스트리에 바인딩되는*** 형태 — 즉 **래핑 등록 관용구** — 가 통째로 뚫린다. 채택된 증거는 또다시 `precise_receiver_type(object=None) → simple_name(owner)` = **자기 클래스명**(`Flow`)이거나 무관한 JDK 타입(`Objects`)이다.

### 3.2 fresh 실증 (28형태 중 8형태 · 전부 픽스처 밖 · 변경 = `Vault.transfer` 본문 1줄)

| # | 형태 | `main` | 직전 `ecd906a` | **브랜치 `67723f9`** | 잘못 쓰인 증거 |
|---|---|---|---|---|---|
| **H2** | `d.task = wrap(() -> …)` (미해소 무자격 호출) | **2** | 0 | **`pass`(0)** 🔴 | `Flow` (자기 클래스) |
| **H2c** | `d.task = Objects.requireNonNull((Runnable) () -> …)` | **2** | 0 | **`pass`(0)** 🔴 | `Objects` |
| **H2d** | `Runnable r = wrap(() -> …); d.task = r;` | **2** | 0 | **`pass`(0)** 🔴 | `Flow` |
| **H2e** | `d.task = svc.wrap(() -> …)` | **2** | 0 | **`pass`(0)** 🔴 | `Registry` |
| **H3** | `Runnable make(){ return wrap(() -> …); } … d.task = make();` | **2** | 0 | **`pass`(0)** 🔴 | `Flow` (return 경유 탈출) |
| **H7** | `d.task = keep(wrap(() -> …))` 2중 래핑 | **2** | 0 | **`pass`(0)** 🔴 | `Flow` |
| **H9** | `d.task = (Runnable) wrap(() -> …)` | **2** | 0 | **`pass`(0)** 🔴 | `Flow` |
| **K2** | `Runnable[] a = { wrap(() -> …) }; d.task = a[0];` | **2** | 0 | **`pass`(0)** 🔴 | `Flow` |
| **K3** | `svc.register(new Holder(() -> …))` (생성자 인자 · 바인딩 없음) | **2** | 0 | **`pass`(0)** 🔴 | `Registry` ← **람다는 `register` 의 인자가 아니다**(R-7 오귀속 잔존) |

**이분탐색(커밋별)**: H2 계열은 브랜치 **첫 코드 커밋 `1f0c60b` 부터** 이미 `pass` 다(`1f0c60b`·`52e59d4`·`9716fb5`·`a59d819`·`0aa6d11` 전부 `pass`). ⇒ **이 델타의 순회귀는 아니다.** 그러나 **`main` 은 9형태를 전부 잡는다** ⇒ **브랜치를 머지하면 `main` 이 지금 잡는 탐지를 잃는다.**

**K3 는 성격이 다르다** — `object_creation_expression` 이 경계로 취급되지 않아 순회가 생성자를 **뚫고 지나가** 바깥 `svc.register` 의 수신자를 증거로 삼는다. 이는 R-7 이 지적한 **"람다가 인자가 아닌 호출에서 증거를 가져오는"** 바로 그 오귀속이 형태만 바꿔 남은 것이다.

### 3.3 대조군 — 탐지가 통째로 죽은 게 아니다
`A1`(평이 등록) · `A2`(같은 줄 소음) · `C10`(조건부) · `C1`·`C2`·`C6`·`C3` · `H1c`(`svc.register(h = new Holder(L))`) · `H4`(메서드 참조) · `H5`(삼항) · `H6`(연쇄 대입) · `H10`(static 초기화자) · `K1`(필드 초기화자) = **브랜치에서 전부 `approval_required`.** 소음 폐쇄 `N4`·`N5`·`N6` = **전부 `pass` 유지.**
⇒ 구멍은 **"래퍼 호출을 거쳐 바인딩/반환되는 등록"** 이라는 명확한 한 갈래다. **실전 Java 에서 가장 흔한 등록 관용구**(`disposable = obs.subscribe(…)`, `handle = bus.register(…)`, `return Wrappers.decorate(() -> …)`)가 정확히 여기에 든다.

### 3.4 §2B 필수질문 — **"거버넌스 목적에 직접 구멍을 내나?" → 예**
민감 sink 로 가는 **실재 경로**(`sink()` → `d.fire()` → `task.run()` → 변경된 `Vault.transfer()`)가 `verdict: pass` · `exit_code: 0` · `errors:[]` · `fail_closed:[]` 로 사라진다. R-1(D-098)·R-4(D-099)·R-5·R-6(D-100)·R-7(D-101) 과 **동일 계열**이며 전부 차단 판정이었다. MVP-3 계약(**과소탐 금지 우선**) ⇒ **비차단 금지.**

---

## 4. 🔧 보정 방향 — 리뷰어가 워크트리에 직접 패치해 탐지축·소음축 동시 실측

### (g) 결부 경계를 **양방향**으로 — "람다의 값이 문장을 탈출하면 증거 무효"
불변식: **증거는 "람다가 인자인 호출" 에서만 오고, 그 호출의 *결과가 어디에도 묶이지 않을 때만* 유효하다.** 지금은 앞 절반만 있다.

리뷰어 프로토타입(`wt-fix3` · 현행 대비 **4줄**):
```python
enclosing_receiver_type = None
ancestor = node.parent
child = node
adopted = False                                   # (+) 채택은 첫 invocation 에서 한 번만
while ancestor is not None and ancestor != body:
    if ancestor.type in {"assignment_expression", "variable_declarator",
                         "return_statement", "object_creation_expression"}:   # (+) return·생성자 추가
        enclosing_receiver_type = None             # (+) 위쪽에서 만나면 채택분도 폐기
        break
    if ancestor.type == "method_invocation" and not adopted:   # (변경) break → adopted 플래그
        if child == ancestor.child_by_field_name("arguments"):
            enclosing_receiver_type = precise_receiver_type(
                source_bytes, ancestor.child_by_field_name("object"),
                method["bindings"], method["owner"])
        adopted = True
    child = ancestor
    ancestor = ancestor.parent
```
핵심은 **`break` 를 `adopted` 플래그로 바꿔 순회를 계속시키는 것**이다 — 그래야 ① 의 경계 검사가 invocation *위쪽*에서도 작동한다. `object_creation_expression` 추가는 K3 의 오귀속(생성자를 뚫고 바깥 호출의 수신자를 가져옴)을 막는다. **다른 구현을 택해도 좋으나 아래 표를 그대로 재현할 것.**

### 📊 리뷰어 실측표 (전부 픽스처 밖 fresh · `wt-fix3`)

| 입력 | 형태 | `main` | 브랜치 | **+(g)** |
|---|---|---|---|---|
| **H2 / H2c / H2d / H2e** | 래퍼 결과를 필드·지역변수에 바인딩 | 2 | **0 ❌** | **2 ✅** |
| **H3** | 래퍼 결과를 `return` 으로 반환 | 2 | **0 ❌** | **2 ✅** |
| **H7 / H9 / K2** | 2중 래핑 · 캐스트 · 배열 초기화 경유 | 2 | **0 ❌** | **2 ✅** |
| **K3** | 생성자 인자 + 바깥 호출(오귀속) | 2 | **0 ❌** | **2 ✅** |
| **C1·C2·C3·C6** (AC#17①·#18) | — | 2 | **2 ✅** | **2 ✅** |
| **A1·A2·C10·H1c·H4·H5·H6·H10·K1** 대조군 | — | 2 | **2 ✅** | **2 ✅** |
| **N4·N5·N6** 소음 폐쇄 | — | 0 | **0 ✅** | **0 ✅** |
| **스위트** | — | 201/202 | 221/222 | **221/222 (회귀 0)** |

⇒ **O-14 소음 폐쇄를 유지한 채 9형태 추가 복구 — 소음↔탐지 트레이드오프가 아니라 순수 개선.** (중간 단계도 남긴다: `adopted` 만 도입한 `wt-fix` = 6형태 복구 / `+return_statement` 인 `wt-fix2` = 8형태 / `+object_creation_expression` 인 `wt-fix3` = 9형태. 셋 다 스위트 221/222.)

---

## 5. 무회귀 · §1 보수적 개발 — **통과** (Codex 신고 전량 재현)

- **스위트**: 브랜치 **221/222** ↔ `main` **201/202**. 유일 FAIL `tree-sitter-smoke` 는 **양쪽 동일**(리뷰 환경 파서 의존성) ⇒ 브랜치 귀책 아님. 그룹: adversarial **74/74** · default **120/121**(= `main` 동일 ⇒ **Python 골든패스 무접촉**) · metamorphic 3/3 · negative-corpus 9/9 · parity 15/15. 케이스 차집합 = **추가 4건뿐, 제거 0**.
- **mutation `PASS(391)`** · `py_compile` PASS · `git diff --check` PASS.
- **`PYTHONHASHSEED` 0/1/999/12345 md5 동일** — 신규 픽스처 4종 각각 확인(`8d452d3b…` / `31396565…` / `0b3a6c3c…` / `aaad0b8d…`).
- **전 입력 exit `0`/`2` 뿐** — fresh 28형태 + 스위트 전량 + **파싱 불능 소스**(중괄호 미닫힘)도 `approval_required`/2 로 fail-closed(3개 트리 동일).
- **§1 통과**: Codex 저작 커밋(`0aa6d11`·`67723f9`)이 만진 파일 = 게이트 **1개**(+13/−8) · `tests/cases.yaml`(+60) · 픽스처 **4세트** · `handoff-log.md` · `summaries/` **뿐**. `TASKS.md` 는 **머지베이스와 바이트 동일**(브랜치가 손댄 적 없음 — `main` 이 앞선 것). `kit/`·`policies/`·`docs/`·`templates/`·`AGENTS.md`·Claude 소유 **0 바이트**. 무관 리팩터·포맷·이름변경 **0**, 기대값 약화 **0**. 커밋 §3 5절 완비.
- **handoff·summaries 문언 — 과장 없음.** 신고한 221/222 · adversarial 74/74 · mutation 391 · `py_compile`·`diff --check` 전부 재현됐다. `tree-sitter==0.26.0` 배포본 미확보로 smoke 를 못 고쳤다는 것도 **정직한 한계 고지**다.
- **`origin/main` 재머지 생략** — Codex 가 "사용자 지시" 로 명시했고, 미머지분은 **내 리뷰기록 2커밋(docs only)** 뿐이라 코드 충돌 위험 없음. **AC#19 위반으로 세지 않는다.**

### ★★ 킷 심층리뷰 (형 지시) — **해당 없음, 근거 실증**
브랜치 전체(`origin/main...67723f9`) 3-dot diff 에 `kit/` **0 바이트**(numstat 행 0). 재실측: `kit/gates/*.py` **20개** 중 `extract-java-callgraph.py` **부재**, 킷 전체에 `java_deferred`·`enclosing_invocation_receiver_type`·`record_has_type_evidence` **0건** ⇒ **킷은 Java L3 미탑재 = 구멍이 아니라 미탑재.**
따라서 `run.sh` 게이트 배선·verdict 조립(max 우선순위)·co-located 내부 의존·`HAS_RANGE`/정책 부재/`--policies` 오버라이드/분석실패 fail-closed·sync 반영·selftest 진입점은 **이 델타의 영향권 밖**이다.
🔴 **TASK-038 sync 는 R-9 폐쇄 전까지 계속 금지** — 지금 스냅샷하면 래핑 등록 관용구 9형태의 과소탐을 배포 킷에 싣는다.

---

## 6. AC — 보정 지시 (TASKS.md 반영)

**AC#20 (🔴 R-9 · 차단)** 결부 경계를 **양방향**으로 만든다. 조상 순회에서 `method_invocation` 채택 후 **즉시 `break` 하지 말고 계속 올라가**, `assignment_expression`·`variable_declarator`·`return_statement`·`object_creation_expression` 중 하나를 만나면 **채택한 증거까지 폐기**한다(채택 자체는 첫 invocation 1회로 한정 — R-7 재발 금지).
**회귀 픽스처 4건 신설** — ① **H2**(`d.task = wrap(L)` · 무자격 미해소 래퍼) ② **H3**(`return wrap(L)` 반환 경유) ③ **K2** 또는 **H7**(배열 초기화 / 2중 래핑) ④ **K3**(`svc.register(new Holder(L))` · 생성자 인자 오귀속). 전부 `verdict: approval_required` · `exit_code: 2`.
**대조군 불변 단언 필수**: `java-indirect-deferred-jdk-noise-pass`(N4) `pass` · A7 형태 `pass` · AC#14/#15/#17/#18 신규 픽스처 9건 `approval_required`.
**음성검증**: 「위쪽 경계 검사」와 「`object_creation_expression` 경계」를 **하나씩 원복**하면 신규 케이스가 **단독 FAIL**.
> ⚠️ **AC#17② 관련 정정**: `child == arguments` 가드는 `precise_receiver_type()` 가 캐스트·괄호 수신자를 항상 `None` 으로 떨어뜨려 **관측 가능한 효과가 없다**(RIG-B FAIL 0 · RIG-D 계측 0회). **되돌리지 말고 그대로 두되, 이에 대한 회귀 픽스처 요구는 철회한다.**

**AC#21 (무회귀 · 보정분)** 스위트 전량 · `tests/mutation-check.sh` · `PYTHONHASHSEED` 0/1/999/12345 md5 동일 · Python 골든패스 불변(`default` 가 `main` 과 동일) · exit **`0`/`2` 뿐** · **`kit/`·`policies/`·`docs/`·`templates/`·`AGENTS.md`·Claude 소유 무접촉** · `origin/main` 재머지.

---

## 7. 비차단 관찰

- **🟡 O-34 (신규)** — **`precise_receiver_type(object_node=None) → simple_name(current_owner)`.** 무자격 호출(`wrap(...)`)의 "수신자 타입" 을 **자기 클래스명**으로 단정하는데, 이는 *람다의 함수형 타입*과 아무 관계가 없다. R-7 과 R-9 가 둘 다 이 값에서 증폭됐다. AC#20 은 **경계**로 우회할 뿐 **값 자체는 그대로 남는다.** 폐쇄 방향: 무자격 호출은 증거를 **`None`(증거 없음 = 보수)** 으로 두거나, 최소한 자기 클래스명은 `enclosing_invocation_receiver_type` 후보에서 제외.
- **🟡 O-35 (신규 · 방법론)** — **AC 가 요구한 음성검증이 원리적으로 불가능한 경우가 있다**(AC#17②). 앞으로 rig 요구를 쓸 때는 **"해당 갈래에 도달하면서 값이 non-None 인 입력이 존재하는가" 를 처방 단계에서 먼저 확인**한다. 이번엔 계측 리그(RIG-D)로 사후 확인했다 — 이 계측 패턴을 상설화할 것.
- **🟡 O-33 재확인(격상 권고)** — R-9 9형태가 **또다시 전부 조용히** 통과했다(`verdict: pass`·`exit_code: 0`·`errors:[]`·`fail_closed:[]`). D-101 에서 비차단으로 둔 판단은 유지하되, **R-1·R-4·R-5·R-7·R-9 다섯 계열이 모두 같은 무신호 경로로 샜다**는 사실을 근거로 **`supported` 승격 차단조건에 O-33 을 추가**한다.
- **🟡 O-32 이월** — 중첩 람다 순회 제외(`nested_in_deferred_body()`). 승격 차단조건 유지.
- **🟡 O-30 이월(격상 유지)** — `List<String>` → `String` 타입인자 누출. AC#17①·#20 이 부수 완화하나 바인딩 단계는 별건.
- **🟡 O-31 부분해소 이월** · **O-29 · O-22 · O-13 · O-3 · O-15 · O-16 이월.**

---

## 8. 결론

**보정요청.** 코드 브랜치 `main` 머지 **보류**. 리뷰 기록만 `main` 머지(D-007). 민감 변경 아님 ⇒ `H-XXXX` 불필요.
**정책**: `java.layers.callgraph` **`partial` 유지** — R-9 로 래핑 등록 관용구가 뚫려 있어 `supported` 승격 불가(under-claim 원칙 · D-074 O-5·D-075·D-076·D-097~D-101). 승격 조건 = **AC#1+#5+#6+#7+#11+#12+#14+#15+#17①+#18+#20** + **O-19·O-32·O-33** 폐쇄.
**TASK-038(킷 sync)**: TASK-041 머지 전 **sync 금지** 유지.

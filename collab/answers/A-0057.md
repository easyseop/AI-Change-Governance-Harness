# A-0057 — TASK-041 A-0056 보정 재제출 재리뷰 → 🔴 **보정요청** (코드 브랜치 머지 계속 보류)

**대상**: `codex/2026-07-22-task041-java-l3-noise` — `a59d819`(보정) · `ecd906a`(인계기록) · `57a0c74`(`origin/main` 머지)
**범위(멱등성)**: **`5b32acc..ecd906a` 델타만 재리뷰.** `1f0c60b`·`0ed3c07`(D-098) · `52e59d4`·`1bb7972`(D-099) · `9716fb5`·`5b32acc`(D-100) 은 처리 완료 — 재리뷰하지 않았다. 단 AC#14·#15 가 닫혔는지 판단하려면 브랜치 전체 동작을 봐야 하므로 판정 실측은 `main`(2f525d6) ↔ 직전(5b32acc) ↔ 브랜치(ecd906a) 3자 비교로 했다.
**판정**: **보정요청.** 새 태스크 아님 — **같은 브랜치에 보정 커밋만 재제출**.
**리뷰 환경**: 격리 worktree 4개(`wt-main-r7`=2f525d6 / `wt-prev-r7`=5b32acc / `wt-br-r7`=ecd906a / `wt-fix-r7`=리뷰어 프로토타입). fresh 입력 **21형태 전부 `tests/fixtures/` 밖**에서 생성해 `check-indirect-impact.py` 를 직접 호출(rigged 차단).

---

## 1. ✅ 구현은 A-0056 문언대로 — 두 축 모두 load-bearing (음성검증 실측)

| AC | 내용 | 음성검증(rig) | 결과 |
|---|---|---|---|
| **#14** | 증거를 caller 역추측 → **레코드-결부**(추출기가 감싼 invocation 을 레코드에 부여) | **RIG-K**: `record_has_type_evidence` 를 옛 caller 스캔으로 원복(모듈 스태시로 동등 재현) | **214/218 — 신규 3건 단독 FAIL**(`…-wire-jdk`·`…-wire-stringbuilder`·`…-two-hop-wire-jdk`) |
| **#15** | `opaque_receiver_types` 에도 동일 상위 폐포 | **RIG-I**: `opaque_receiver_types = expand_type_supertypes(...)` 4줄 제거 | **215/218 — 신규 2건 단독 FAIL**(`…super-to-subinterface`·`…sibling-interface-cast`) |
| **#16** | 무회귀 | §4 | 재현 완료 |

**A-0056 이 요구한 `A2`(같은 줄 소음)도 실제로 닫혔다.** 신규 픽스처 3건은 전부 소음을 **람다와 같은 줄**에 배치했고(`void wire() { rows.size(); d.task = () -> …; }`), fresh **A2·B1·C10** 이 브랜치에서 `approval_required` 로 복구됐다. **A-0056 이 "픽스처가 갈래에 도달 못 한다"고 지적한 false-confidence 도 해소** — 새 픽스처는 소음이 `wire()`(=레코드의 caller) 안에 있어 증거 경로를 실제로 통과한다.

**추가 확인 — Codex 가 신고하지 않은 부수 개선**: 새 dedup 은 `rank` 튜플의 **전순서 최소값**이라 결정론적이다. 옛 코드(`if key not in deduplicated or len(item) <= 5 or not item[5]`)는 **`set` 순회 순서에 의존**해, 같은 key 에 서로 다른 `receiver_type` 두 개가 오면 승자가 시드에 따라 달라질 수 있었다. **결정론 강화 = 순개선.**

**A-0056 §2.1 문언 정정(리뷰어 자기정정)**: 나는 폐기 레코드가 *"`errors:[]`·`fail_closed:[]`·`indirect_impact:[]` **완전 무흔적**"* 이라고 적었다. **부정확했다** — 폐기된 레코드도 `coverage.unevaluated` 에는 **남는다**(C2 JSON 실측). 다만 `coverage.unevaluated` 는 추출기 원본 패스스루라 **모든 실행에 항상 존재**하며 "sink 폐쇄에 닿는 지연 등록이 억제됐다" 는 신호를 전혀 담지 않는다. 정확한 표현은 **"무흔적"이 아니라 "거버넌스 신호 부재"**(`verdict: pass` · `exit_code: 0` · `errors:[]` · `fail_closed:[]`). 판정 결론은 그대로다. → O-33 로 별도 관리.

---

## 2. 🔴 R-7 (차단 · 과소탐 · **직전 커밋 대비 순회귀 포함**) — 결부 범위가 여전히 **너무 넓다**

> **⚠️ 리뷰어 자기정정 9회째 — 처방 문언의 과소지정.** A-0056 §4(c)·AC#14 는 *"이 deferred 레코드를 감싼 invocation"* 이라고만 썼다. Codex 는 그것을 **"가장 가까운 조상 `method_invocation`"** 으로 구현했다 — 문언상 타당하다. 그러나 내가 뜻한 것은 *"이 람다가 **인자로 넘어간** 그 호출"*(A-0056 §4(c) 의 N4 예시가 그것이다)이고, **"감쌌다"에는 대입식을 통째로 감싼 무관한 호출도 들어간다.** **Codex 귀책 아님 — 브랜치가 아직 `main` 에 없으므로 지금 막는다.**

### 2.1 증상 — 세 갈래
`extract-java-callgraph.py:698~709` 의 조상 순회는 **"람다가 그 호출의 인자인가"** 를 묻지 않고, **대입식(`assignment_expression`)에서 끊지도 않는다.** 그래서 `d.task = () -> …` 라는 **실제 등록 행위를 감싼 무관한 호출**이 그대로 "타입 증거" 가 된다. `precise_receiver_type(object_node=None)` 이 **`simple_name(current_owner)`**(= 자기 클래스명)를 돌려주는 것도 여기서 증폭된다.

### 2.2 fresh 실증 (전부 픽스처 밖 · 변경 = `Vault.transfer` 본문 1줄)

| # | 형태 | `main` | 직전 `5b32acc` | **브랜치** | 증거로 잘못 쓰인 값 |
|---|---|---|---|---|---|
| **C2** | `void keep(Object o){} … wire(){ keep(d.task = () -> new Vault().transfer()); }` | **2** | **2** | **`pass`(0)** 🔴 | `Flow` (자기 클래스) |
| **C1** | `Objects.requireNonNull(d.task = () -> new Vault().transfer());` | **2** | 0 | **`pass`(0)** | `Objects` |
| **C6** | `rows.add(String.valueOf(d.task = () -> new Vault().transfer()));` | **2** | 0 | **`pass`(0)** | `String` (O-30 타입인자 누출) |

**🔴 C2 는 직전 커밋 대비 순회귀다** — `keep(...)` 은 리포 안에 선언된 **해소되는** 호출이라 `unresolved` 에 **아예 등장하지 않는다.** 그래서 옛 caller 스캔(5b32acc)은 증거 0 → 보수 승격 → **탐지**했다. 새 코드는 **없던 증거를 만들어낸다**(`precise_receiver_type(None) → "Flow"`). 즉 AC#14 는 R-5 를 닫으면서 **R-5 와 같은 계열의 새 구멍을 뚫었다.**

### 2.3 왜 자기 클래스명이 "증거" 가 되면 안 되나
`Flow` 는 **람다의 함수형 타입이 아니다.** 그런데 N4(=O-14 소음 폐쇄 픽스처)를 `pass` 로 지키는 값도 **똑같은 `Flow`** 다(`consume(() -> log("head"))`, `consume` 은 미선언 → unresolved). **게이트는 C2 와 N4 를 구분할 수단이 없다** — 유일한 실질 차이는 **람다가 그 호출의 *인자*인가(N4), 아니면 그 호출이 *대입식을 감쌌을 뿐*인가(C2)** 다. 그 구분을 코드에 넣지 않으면 하나를 살리는 순간 다른 하나가 죽는다.

### 2.4 §2B 필수질문 — **"거버넌스 목적에 직접 구멍을 내나?" → 예**
민감 sink 로 가는 실재 경로가 `verdict: pass`·`exit_code: 0`·`errors:[]`·`fail_closed:[]` 로 사라지고 **`main` 은 잡는다.** R-1(D-098)·R-4(D-099)·R-5(D-100) 와 **동일 계열**이며 전부 차단 판정이었다. MVP-3 계약(**과소탐 금지 우선**) ⇒ 비차단 금지.

---

## 3. 🟠 R-8 (차단 · 1토큰 · 보수 방향 역전) — dedup 동점규칙이 **증거 있는 레코드를 우선**한다

`extract-java-callgraph.py:1081~1093` 의 `rank = (bool(receiver), not bool(enclosing), receiver, enclosing)` 는 최소값이 이기므로 **`enclosing` 이 있는 쪽을 선호**한다. `enclosing` 은 **탐지를 끄는 값**이므로 방향이 거꾸로다.

lambda 레코드의 dedup key 는 `(caller, "lambda_dispatch", "lambda", line, dispatch_targets)` 라 **같은 줄의 서로 다른 두 람다가 한 레코드로 합쳐진다.** fresh **C3** — `void wire() { Objects.requireNonNull(() -> hashCode()); d.task = () -> new Vault().transfer(); }`: 두 람다가 같은 key 로 충돌하고, **소음 람다(enclosing=`Objects`)가 실제 등록 람다(enclosing 없음)를 밀어낸다** ⇒ 증거 있음 → 폐기 → `pass`.

- 정직하게 적는다: **C3 는 직전 커밋 대비 순회귀가 아니다**(5b32acc 도 `pass`). 순회귀를 만드는 변형(C7·C11 = 오버로드·in-repo 래퍼)도 **직접 만들어 돌려봤으나 전부 `approval_required` 로 생존** — `bool(receiver)` 첫 성분이 먼저 걸러서다.
- 그럼에도 **차단으로 둔다**: 방향이 명백히 보수성 반대이고, **수정 비용이 `not` 두 개 제거뿐**이며, 실측상 **C3 를 닫고 스위트 회귀 0**(§4 표)이다. R-7 수정 후에는 `enclosing` 의 영향력이 커지므로 그대로 두면 위험만 남는다.

---

## 4. 🔧 보정 방향 — 리뷰어가 워크트리(`wt-fix-r7`)에 직접 패치해 탐지축·소음축 동시 실측

### (e) R-7 — 증거는 **람다가 인자로 넘어간 호출**만
조상 순회에 두 가지 제약을 건다(리뷰어 프로토타입, `extract-java-callgraph.py` 람다 분기 12줄):
1. **`assignment_expression`·`variable_declarator` 를 만나면 즉시 중단** — 그 지점이 람다를 실제로 **바인딩**하는 곳이고, 그 바깥 호출은 이 람다와 무관하다.
2. **`method_invocation` 에 도달했을 때, 올라온 자식이 그 호출의 `arguments` 필드일 때만** `precise_receiver_type` 을 채택.

N4(`consume(() -> log("head"))`)는 람다가 **인자**이므로 증거 유지 ⇒ `pass` 불변. C1·C2·C6 는 대입식에서 끊겨 증거 0 ⇒ 보수 승격.

### (f) R-8 — 동점규칙을 보수 방향으로
`not bool(enclosing)` → `bool(enclosing)` (신·구 rank 양쪽). **증거 없는 레코드가 이긴다** = 보수. 1토큰.

### 📊 리뷰어 실측표 (`wt-fix-r7` 에 (e)+(f) 적용 · 전부 픽스처 밖 fresh 입력)

| 입력 | 형태 | `main` | 직전 `5b32acc` | 브랜치(현행) | **+(e)+(f)** |
|---|---|---|---|---|---|
| **C2** | in-repo 래퍼가 대입식을 감쌈 | 2 | **2** | **0 🔴순회귀** | **2 ✅** |
| **C1** | `Objects.requireNonNull(대입식)` | 2 | 0 | **0 ❌** | **2 ✅** |
| **C6** | `rows.add(String.valueOf(대입식))` | 2 | 0 | **0 ❌** | **2 ✅** |
| **C3** | 같은 줄 두 람다(dedup 충돌) | 2 | 0 | **0 ❌** | **2 ✅** |
| **A2·B1** | 같은 줄 소음(AC#14 대상) | 2 | 0 | **2 ✅** | **2 ✅** |
| **A4·B5** | 상위→하위 폐포·형제 캐스트(AC#15) | 2 | 0 | **2 ✅** | **2 ✅** |
| **C10** | `if (hashCode()>0) { d.task = … }` | 2 | 0 | **2 ✅** | **2 ✅** |
| **A1·A5·B4·B6·C5·C7·C11·C4b·C4c** 대조군 | — | 2 | 2 | **2 ✅** | **2 ✅** |
| **A7**(N4 형태) 소음 폐쇄 | — | 2 | **0 ✅** | **0 ✅** | **0 ✅** |
| **스위트** | — | 201/202 | 212/213 | 217/218 | **217/218 (회귀 0)** |

⇒ **O-14 소음 폐쇄를 유지한 채 4형태 추가 복구 — 소음↔탐지 트레이드오프가 아니라 순수 개선.** **다른 구현을 택해도 좋으나 이 표를 그대로 재현할 것.**

---

## 5. 무회귀 · §1 보수적 개발 — **통과**

- **스위트**: 브랜치 **217/218** ↔ `main` **201/202**. 유일 FAIL `tree-sitter-smoke` 는 **양쪽 동일**(리뷰 환경 파서 미설치) ⇒ 브랜치 귀책 아님. 그룹: adversarial **70/70** · default **120/121**(= `main` 동일 ⇒ **Python 골든패스 무접촉**) · metamorphic 3/3 · negative-corpus **9/9** · parity **15/15**. 케이스 차집합 = **추가 5건뿐, 제거 0**.
- **mutation `PASS(383)`** · `py_compile` PASS · `git diff --check` PASS.
- **`PYTHONHASHSEED` 0/1/999/12345 md5 동일** — 신규 픽스처 3종 각각 확인(`3ae1dc89…` / `9f65e460…` / `2ba11732…`).
- **전 입력 exit `0`/`2` 뿐** — fresh 21형태 + 스위트 전량.
- **하위호환**: `enclosing_invocation_receiver_type` 은 **순수 추가 키**로 소비자는 이 델타가 만든 2곳뿐(리포 전체 grep). `type_supers` 도 동일.
- **§1 통과**: Codex 저작 커밋(`a59d819`·`ecd906a`)이 만진 파일 = 게이트 2개 · `tests/cases.yaml` · 픽스처 5세트 · `handoff-log.md` · `summaries/` **뿐**. `TASKS.md`·`CLAUDE.md`·`COMMON-RULES.md`·`PROJECT.md` 는 `main` 과 **바이트 동일**(머지로 들어온 것). `kit/`·`policies/`·`docs/`·`templates/`·`AGENTS.md` **0 바이트**. 무관 리팩터·포맷·이름변경 **0**, 기대값 약화 **0**. 커밋 §3 5절 완비.
- **✅ `origin/main` 머지 이행**(`57a0c74`) — collab-protocol §5.1 미동기화가 **11회 만에 해소**.
- **handoff·summaries 문언 — 과장 없음.** 신고한 217/217·383·md5·무접촉 전부 재현됐다.

### ★★ 킷 심층리뷰 (형 지시) — **해당 없음, 근거 실증**
3-dot diff 에 `kit/` **0 바이트**. 추가 확인(D-100 과 동일 결론 재실측): 현행 킷 `kit/gates/` **22개** 목록에 **`extract-java-callgraph.py` 부재**, `kit/gates/check-indirect-impact.py` 에 `java_deferred` **0건** ⇒ **킷은 Java L3 미탑재 = 구멍이 아니라 미탑재.** 따라서 run.sh 게이트 배선·verdict 조립(max 우선순위)·co-located 내부 의존·`HAS_RANGE`/정책 부재/`--policies` 오버라이드/분석실패 fail-closed·sync 반영·selftest 진입점은 **이 델타의 영향권 밖**이다. **TASK-038 sync 는 R-7·R-8 폐쇄 전까지 계속 금지** — 지금 스냅샷하면 두 구멍을 배포 킷에 싣는다.

---

## 6. AC — 보정 지시 (TASKS.md 반영)

**AC#17 (🔴 R-7 · 차단)** 타입 증거를 **"람다가 인자로 넘어간 호출"** 로 한정한다. 조상 순회에서 **① `assignment_expression`/`variable_declarator` 를 만나면 중단**, **② `method_invocation` 은 올라온 자식이 그 호출의 `arguments` 일 때만** 채택. **회귀 픽스처 3건 신설**(C2·C1·C6 형태) — 전부 `verdict: approval_required`·`exit_code: 2`. **대조군 불변 단언 필수**: `java-indirect-deferred-jdk-noise-pass`(N4) `pass` · A7 형태 `pass` · 기존 신규 5픽스처 `approval_required`. **음성검증**: 두 제약 중 하나씩 원복하면 신규 케이스가 **단독 FAIL**.
> 🔴 **C2 는 직전 커밋(`5b32acc`) 대비 순회귀다** — 리포 안에 선언된 **해소되는** 래퍼 호출이 "증거" 로 승격되기 때문. 이 케이스를 픽스처로 반드시 고정할 것.

**AC#18 (🟠 R-8 · 차단 · 1토큰)** dedup 동점규칙을 보수 방향으로: `rank`·`previous_rank` 의 `not bool(enclosing)` → `bool(enclosing)`. **회귀 픽스처 1건 신설**(C3 = 같은 줄 두 람다, 소음 람다가 무해소 호출의 인자) — `approval_required`. **음성검증**: 되돌리면 단독 FAIL. **N4·A7 `pass` 불변 단언 필수.**

**AC#19 (무회귀 · 보정분)** 스위트 전량 · `mutation-check.sh` · `PYTHONHASHSEED` 0/1/999/12345 md5 동일 · Python 골든패스 불변(`default` 가 `main` 과 동일) · exit **`0`/`2` 뿐** · **`kit/`·`policies/`·`docs/`·`templates/`·`AGENTS.md`·Claude 소유 무접촉** · `origin/main` 재머지.

---

## 7. 비차단 관찰

- **🟡 O-32 (신규 · 승격 차단조건)** — `nested_in_deferred_body()` 가 **지연 본문 안에 중첩된 람다를 순회에서 통째로 제외**한다. fresh **C4**(`Optional.ifPresent(c -> { d.task = () -> new Vault().transfer(); })`): `main` 2 → 직전 0 → 브랜치 0 → 프로토타입 0. **이 델타의 회귀 아님**(직전 커밋도 동일)이고, **리포 내 고차함수(C4b) 와 익명클래스(C4c) 형태는 4개 트리 전부에서 탐지**되므로 범위는 *"JDK/미해소 고차 호출의 콜백 안에서 등록"* 으로 좁다. 닫으려면 N4 소음 폐쇄(그 `consume` 자체가 미해소 호출이다)와 정면충돌하므로 **패치가 아니라 설계 판단이 필요** ⇒ 보정 AC 로 강제하지 않고 **`supported` 승격 차단조건**으로 못박는다.
- **🟡 O-33 (신규)** — 폐기된 deferred 레코드에 **거버넌스 신호가 없다.** `coverage.unevaluated` 에는 남지만 그건 모든 실행에 항상 있는 원본 패스스루라 "sink 폐쇄에 닿는 등록이 억제됐다" 를 뜻하지 않는다. R-1·R-4·R-5·R-7 이 매번 *조용히* 통과한 근본 원인. 폐쇄 방향: 억제 사유(`type_mismatch` / `evidence_present`)를 카운터나 `notes` 로 1급 표기 ⇒ 다음 과소탐이 리뷰가 아니라 **출력**에서 드러난다.
- **🟡 O-30 이월(격상 유지)** — C6 에서 `List<String> rows` → `receiver_type: "String"` 누출이 **다시 판정을 좌우**했다. AC#17 이 부수적으로 완화하나 **바인딩 단계 자체는 별건**.
- **🟡 O-31 부분해소** — 소음 폐쇄의 줄-배치 민감성은 AC#14 로 **한 방향은 사라졌다**(같은 줄 소음이 이제 탐지). 남은 민감성은 dedup key 에 `line` 이 들어가는 데서 오며 AC#18 이 그 영향을 보수화한다.
- **🟡 O-29 · O-22 · O-13 · O-3 · O-15 · O-16 이월.**

---

## 8. 결론

**보정요청.** 코드 브랜치 `main` 머지 **보류**. 리뷰 기록만 `main` 머지(D-007).
**정책**: `java.layers.callgraph` **`partial` 유지** — R-7·R-8 로 등록 관용구가 뚫려 있어 `supported` 승격 불가(under-claim 원칙 · D-074 O-5·D-075·D-076·D-097~D-100). 승격 조건 = **AC#1+#5+#6+#7+#11+#12+#14+#15+#17+#18** + **O-19·O-32** 폐쇄.
**TASK-038(킷 sync)**: TASK-041 머지 전 **sync 금지** 유지.

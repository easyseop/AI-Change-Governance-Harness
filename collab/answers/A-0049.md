# A-0049 — TASK-039 A-0048 보정 재제출 재리뷰 → **통과 · 머지** (D-093)

- **대상**: `codex/2026-07-21-task039-java-l3-coverage` head `1a3f0d0`, **보정 커밋 `ed2d221`(구현)·`1a3f0d0`(인계)**
- **멱등성**: `e54a30f`·`b2d7cd8` 는 A-0048 에서 처리 완료 — **보정 델타만 재리뷰**. 단 머지 판단은 브랜치 전체(`origin/main`=`662d972` 대비) 기준.
- **런처 스캔**: `git fetch origin --prune` 후 `--no-merged origin/main` 기준 **미머지 codex 브랜치는 이 1개뿐**. 나머지 `origin/codex/*` 전량 (다) 머지완료. handoff 최상단이 `Codex → Claude | ed2d221` ⇒ **(가) 리뷰 차례**.
- **판정**: ✅ **리뷰 통과 · `main` 머지**(비민감 · D-007). **재제출 불필요 — TASK-039 종결.**

---

## 1. AC 대비 결과 (A-0048 이 고정한 보정 AC#7·#8·#9)

| AC | 요구 | 결과 |
|---|---|---|
| **#7** (R-1) | 막다른 길 = `bodyless` **또는** `kind: unresolved` + 전방폐쇄에 sink 자신 포함 · JDK 3형태+`::` 픽스처 · 소음 `pass` 2케이스 유지 · 음성검증 | ✅ **충족** (§2.1) |
| **#8** (R-2) | enum 본문·인터페이스 상수·익명 본문 필드 순회 확장 · **enum/class 쌍** 픽스처 | ✅ **충족** (§2.2) |
| **#9** (O-12) | E-C·E-D·E-F 각각 제거 시 단독 FAIL | ⚠️ **E-C·E-D 충족 / E-F 부분** → **O-17**(§4) |
| **#3 불변식** | 정밀화가 과소탐을 만들지 않을 것 · 소음 `pass` 2케이스 유지 · 기존 픽스처 전량 `approval_required` 유지 | ✅ **충족**(단 실 repo 소음은 재확대 → **O-14 실측**, §3) |
| **#6** | 무회귀 · 정책·킷 무접촉 | ✅ **충족** (§5) |

---

## 2. 적대적 검증 — 픽스처 밖 fresh 입력 (별도 임시 repo, `main`↔`e54a30f`↔브랜치 3자 대조)

리뷰용 러너를 따로 짜서(`adv.sh`: `git init` → base 커밋 → head 커밋 → 동일 rev-range 로 게이트 호출) **픽스처를 전혀 쓰지 않는 22형태**를 세 워크트리에 동일하게 먹였다. rigged 픽스처로 통과하는 경로를 차단하기 위함.

### 2.1 AC#7 — R-1 회귀 4형태 전부 복구 (`pass`(0) → `approval_required`(2))

| fresh 입력 | `e54a30f`(보정 전) | **브랜치** |
|---|---|---|
| ① `sink(){ schedule(() -> new Vault().transfer()); }` + `void schedule(Runnable r){ r.run(); }` | `pass` (0) | **`approval_required`** (2) |
| ② 같은 자리 익명 `new Runnable(){ … }` | `pass` (0) | **`approval_required`** (2) |
| ③ `pool.submit(() -> …)` (`ExecutorService`) | `pass` (0) | **`approval_required`** (2) |
| ④ `rows.forEach(v::transfer)` (외부 인터페이스 `::` · A-0048 의 잔존 구멍) | `pass` (0) | **`approval_required`** (2) |

`fail_closed` 문구도 진단적으로 개선됐다 — `dead_ends=Flow.helper` 처럼 **어느 함수가 막다른 길인지** 명시(리뷰어가 30초 안에 원인 파악 가능).

**대조군 불변**: repo 선언 인터페이스 직접대입 람다는 여전히 **실 탐지** — `path: [Flow.sink, Task.exec, Vault.transfer]`, `hops: 2`, `reviewer: settlement-reviewer` 로 `main`·`e54a30f`·브랜치 **3자 동일**. Python 골든패스도 불변(§5). ⇒ 승격 확대가 정상 탐지를 삼키지 않았다 = 과잉 반려 아님.

**왜 이 판별식이 이제 누락 없이 닫히는가 (구조적 논증)** — 지연 dispatch 가 실제로 *호출*되려면 그 지점은 반드시 다음 중 하나다: (a) repo 선언 함수형 인터페이스의 추상 메서드 호출 ⇒ `bodyless` 노드로 폐쇄에 잡힘, (b) 외부(JDK) 타입 메서드 호출 ⇒ `kind: unresolved` 로 잡힘, (c) 외부 프레임워크가 콜백을 invoke ⇒ 그 등록 호출 자체가 (b). **세 경우가 전부 확장된 판별식에 포함**되므로 "sink 폐쇄에서 도달 가능한데 판별식이 못 보는" 누락 부류는 남지 않는다. 이 논증을 깨려고 만든 fresh 입력 8형태(§2.2·§2.3·§4) 중 반례는 나오지 않았다.

### 2.2 AC#8 — 컨테이너 비대칭 해소 (A-0048 의 원인격리 대조군 재현)

`coverage.unevaluated` 원소를 직접 확인:

| fresh 입력 | `e54a30f` | **브랜치** |
|---|---|---|
| `enum Reg { A; static Task task = () -> …; }` | `pass` · **coverage 빔** | **`approval_required`** · `Reg.<clinit>\|lambda_dispatch` |
| **바이트 동일 `class Reg` 판** | `pass` · `Reg.<init>\|lambda_dispatch` | **`approval_required`** · `Reg.<clinit>\|lambda_dispatch` |
| `interface Reg { Task TASK = () -> …; }` | `pass` · **coverage 빔** | **`approval_required`** · `Reg.<clinit>\|lambda_dispatch` |
| 익명 클래스 본문 필드 람다 | `pass` · 람다 무표식 | **`approval_required`** · `Flow.wire\|lambda_dispatch` 추가 |

**enum 판과 class 판의 coverage 원소가 이제 문자열 단위로 동일**(`Reg.<clinit>|lambda_dispatch|lambda`) ⇒ A-0048 이 지적한 "컨테이너 종류만으로 판정이 갈린다" 가 해소됐다. 쌍 픽스처(`java-indirect-enum-field-lambda-fail-closed` ↔ `…-enum-class-field-lambda-control`)로 재발 시 즉시 FAIL.

추가로 `record`, `static` 중첩 클래스, `static { … }` 안 익명, **if 블록 안 local class**(CLAUDE.md 고정 적대세트 "조건부·중첩 정의") 전부 정상 기록 확인 — `Flow.Local.<init>|lambda_dispatch`.

### 2.3 고정 적대 입력 세트 (CLAUDE.md §2B)

| 축 | 입력 | 결과 |
|---|---|---|
| 어노테이션(Java 판 데코레이터) | `@Deprecated @SuppressWarnings("all") static Task task = () -> …` | 탐지 ✅ (단 caller 라벨 오표기 → **O-18**) |
| 동명 오버로드 | `Vault.transfer()` / `Vault.transfer(int)` + `new Vault()::transfer` | `approval_required` ✅ |
| 조건부·중첩 정의 | `if (true) { class Local { Task t = () -> …; } }` | coverage 기록 ✅ |
| 미invoke 지연 dispatch | `static { Task t = () -> …; }` (sink 가 호출 안 함) | `pass` + coverage 정직 노출 ✅ (올바른 판정) |

---

## 3. 🟠 O-14 실측 — 승격 정밀화(AC#3)의 실효 범위가 크게 줄었다 (**비차단 · 차기 AC**)

A-0048 비차단 이월 O-14("판별식이 넓어지면 소음 재확대 가능")가 **실제로 발생**했다. `kind: unresolved` 는 **JDK 호출을 포함한 모든 미해소 호출**에 붙으므로(`extract-java-callgraph.py:525`), `sink_dead_ends` 는 사실상 "sink 전방폐쇄가 외부 호출을 하나라도 한다" 로 완화된다.

**실증 (변경 대상 = sink 와 무관한 `Report.emit`, 지연 dispatch 도 무관 파일)**

| fresh 입력 | `main` | `e54a30f` | **브랜치** |
|---|---|---|---|
| **N4** — sink 폐쇄에 JDK 호출 1개(`rows.size()`) 존재 | `approval_required` | **`pass`** | **`approval_required`** |
| **N5** — N4 에서 그 JDK 호출만 제거(순수 repo 내부 호출) | `approval_required` | `pass` | **`pass`** |

⇒ AC#3 의 소음 억제가 살아남는 조건은 **"sink 의 N홉 전방폐쇄 전체가 외부 호출을 단 한 번도 하지 않을 때"** 로 좁아졌다. 실 Java repo 에선 거의 성립하지 않는다.

**그럼에도 비차단으로 판단한 근거** (§2B 필수질문을 먼저 통과시킨 결과):
1. **거버넌스에 구멍을 내는 방향이 아니다** — 과탐(over-taint)이며 MVP-3 공통 "항상 과탐 반올림·과소탐 금지" 와 같은 방향.
2. **`main` 보다 나쁘지 않다** — `main` 은 무조건 전역 승격이라 N4·N5 둘 다 `approval_required`. 브랜치는 N5 를 `pass` 로 정확히 갈라내므로 **탐지·소음 두 축 모두 `main` 이상**.
3. AC#3 이 명문화한 **실증 케이스(sink 0개 repo · 무관 sink)는 여전히 `pass`** — AC 문언은 충족.
4. **근본원인은 다시 리뷰어 지시** — A-0048 §"방향" 이 `kind: unresolved` 확장을 8줄 프로토타입까지 붙여 못박았고, 그때 나는 **소음 축을 검증하지 않았다**(A-0046→A-0047 O-10 과 동일 패턴의 3회째). Codex 는 지시를 정확히 구현했다.

**내가 짠다면 (차기 AC 용 · 프로토타입 실측치 포함)**: 지금 판별식은 **막다른 길 쪽만** sink 관련성을 보고 **`java_deferred` 쪽은 여전히 repo 전역**이다. 양쪽 모두 sink 관련성을 요구하면 갈린다. 스크래치 사본에 "deferred item 의 `caller` 가 sink 전방폐쇄 안(또는 `<clinit>`/`<init>` 합성 caller)일 때만 승격" 을 넣어 측정한 결과:

- ✅ **N4 → `pass`**(소음 제거), **N5 `pass` 유지**
- ✅ R-1 4형태·AC#8 4형태·대조군·`::` 케이스 **전부 `approval_required` 유지**
- ❌ **스위트 179/180 — `java-indirect-lambda-argument-fail-closed` 단독 FAIL**

**실패모드까지 넘긴다(같은 실수 3회 금지)**: 그 픽스처는 `wire()` 가 람다를 만들고 `set()` 이 저장하며 **`sink()` 가 `port.run()` 으로 invoke** 하는 모양이라, 생성 지점(`Flow.wire`)이 sink 폐쇄 밖이다. ⇒ **caller 기준 필터는 틀렸다.** 올바른 축은 **dispatch 대상**이다 — deferred coverage 항목에 *어느 인터페이스/타입으로 dispatch 되는지*(예: `Port.run`)를 기록하고 **그 대상이 `sink_dead_ends` 에 있을 때만** 승격하면, lambda-argument 는 `Port.run`(폐쇄 안 막다른 길)이라 유지되고 N4 는 JDK `Consumer.accept`(막다른 길이 `Report.render` = 폐쇄 밖)라 걸러진다. **이 방향은 아직 내가 구현·실증하지 않았다 — Codex 는 착수 전 반드시 아래 fresh 세트로 먼저 검증할 것**: ① repo 인터페이스 직접대입 ② repo 인터페이스 인자전달(위 픽스처) ③ **JDK 인터페이스**(`Runnable`·`Consumer`·`ExecutorService`) ④ **`::`**(repo 내/외 각각) ⑤ 초기화 컨텍스트(`<clinit>`/`<init>` 합성 caller — 폐쇄에 절대 안 들어옴) ⑥ 무관 sink·sink 0개 소음 ⑦ 다중 sink·`hops` 경계.

---

## 4. 비차단 관찰 (차기 AC 로 명시)

- **🟠 O-17 (AC#9 E-F 부분 미충족 · verdict 를 실제로 뒤집는 미고정 원소)** — `subtree_call_targets` 의 `method_reference` **미해소 fallback**(`extract-java-callgraph.py:482`)을 제거해도 **스위트 180/180 무변화**. 신규 픽스처 `java-indirect-deferred-body-method-reference` 는 `coverage_unevaluated: []` 를 단언하는 **해소 경로** 케이스라, 해소 분기(`targets.update(resolved)`)만 고정한다(그 분기 rig 시 단독 FAIL = RIG-10 확인). **미해소 분기는 verdict 를 실제로 뒤집는다 — fresh 실증**: 해소 가능한 람다 dispatch 본문 안에 repo 밖 `::`(`Task inner = Ext::run;`)를 두면 `approval_required` ↔ 그 분기 제거 시 **`pass`**. A-0041 G-1 기준 미충족 ⇒ **픽스처 1건**(미해소 `::` in deferred body, `coverage_unevaluated` 원소 단언)으로 고정할 것.
- **🟡 O-18 (`has_static_modifier` 다중 수식어 오판 · 감사 정직성)** — `any(node_text(child).strip() == "static" for child in node.children)` 는 `field_declaration` 의 직속 자식 `modifiers` 노드 **전체 텍스트**와 비교한다. 수식어가 하나(`static`)일 때만 맞고, **`static final` · `private static` · 어노테이션 동반**(`@Deprecated … static`)이면 False → caller 가 `<clinit>` 이 아니라 **`<init>` 으로 오표기**. fresh 실증 3형태(`static` ✅ `<clinit>` / `static final` ❌ `<init>` / `private static` ❌ `<init>`). **판정 무영향**(초기화 레코드의 `kind` 는 `unresolved` 가 아니라 `java_bodyless` 에 안 들어가고, 합성 id 는 그래프 노드도 아님) ⇒ coverage 는 정직하게 남고 조용한 `pass` 는 없다. 다만 ① **인계기록의 "static field/constant caller를 `<clinit>`로 표기했다" 는 `static final`(상수의 표준형)에서 사실이 아니고**, ② 감사카드가 클래스 로드 시점 코드를 **인스턴스 생성 시점**으로 표시해 사람 리뷰어를 오도한다. **수정은 1줄**(`modifiers` 노드 텍스트를 토큰 분해해 `"static"` 포함 여부로 판정) + `static final`/`private static` 픽스처. RIG-6(함수 무력화) 시 2건 단독 FAIL = 나머지 경로는 load-bearing.
- **🟡 O-19 (컨테이너 축 잔여 — enum 상수 본문)** — `enum Reg { A { Task t = () -> …; } ; }` 는 여전히 **coverage 빔**(`scan_initializer_members` 가 `enum_constant` 의 `class_body` 를 안 봄). AC#8 과 같은 부류의 마지막 잔여. **§2.1 의 구조적 논증에 따라 실제 도달 경로는 (a)/(b) 로 회수되므로 verdict 누락은 재현되지 않았고**(fresh 시도 실패), 정직성(coverage 표식) 결손에 그친다 ⇒ 비차단. 픽스처 1건으로 닫을 것.
- **O-13 이월** — `nodes[].bodyless` 직접 단언 케이스 여전히 0(간접 고정만). **TASK-038 킷 sync 시 노드 스키마 동반 필수.**
- **O-3·O-4·O-15·O-16 이월** — 변동 없음.
- **종결**: O-6(`::`) · O-9(초기화 컨텍스트) · O-10(전역 무조건 승격 — 부분 종결, 잔여는 O-14) · O-11 · O-12 의 E-C·E-D.

---

## 5. 통과 확인 (무발견 ≠ 통과 · 전부 독립 재현)

- **스위트**: `main` **161/161** → 브랜치 **180/180** — 신규 실패 0. adversarial 19→**36**, negative-corpus 3→**5**, default 121/121·metamorphic 3/3·parity 15/15 **불변**. **Codex 신고(180/180 · adversarial 36/36)와 정확히 일치 — 과장 없음.**
- **mutation-check**: `PASS (Expectation mutations checked: **311**)` — 신고와 일치. 기대값 변조 시 FAIL = 항상-PASS 아님(음성검증).
- **결정론**: fresh 입력 게이트 출력 md5 **3회 동일**.
- **불변식**: 시험한 모든 입력의 exit 이 `0`/`2` 뿐 — **3층 자동차단 0건**(CLAUDE.md §4 유지).
- **Python 골든패스**: `check-indirect-impact` 의 비-Java 케이스 **16건 목록이 `main` 과 문자 단위 동일**.
- **위생**: `py_compile` PASS · `git diff --check` PASS.
- **기대값 약화 없음**: `cases.yaml` 보정 델타는 **순수 추가**(삭제·수정 0줄). 기존 케이스 중 verdict 가 바뀐 것 **0건**. 신규 11케이스 전부 `group: adversarial` · `approval_required`.

### rig-and-revert — 원소 단위 (A-0041 G-1)

| rig | 무력화 대상 | 결과 |
|---|---|---|
| RIG-1 | `check-indirect-impact` 의 `kind: unresolved` 확장 | **174/180** — jdk-runnable-lambda·jdk-runnable-anonymous·executor-submit·external-method-reference·enum-field·interface-constant 6건 |
| RIG-2 | 전방폐쇄에 sink 자신 포함 | **177/180** — external-method-reference·enum-field·interface-constant 3건 |
| RIG-3 | `enum_body_declarations`/`class_body_declarations` 재귀 | **179/180** — enum-field **단독** |
| RIG-4 | `constant_declaration` 갈래 | **179/180** — interface-constant **단독** |
| RIG-5 | 익명 본문 필드 deferred 갈래 | **179/180** — anonymous-field **단독** |
| RIG-6 | `has_static_modifier` → 항상 False | **178/180** — enum-field·enum-class-control 2건(쌍 픽스처가 작동) |
| RIG-7 | instance initializer `block` 갈래 (**E-C**) | **179/180** — instance-block **단독** |
| RIG-8 | `deferred_nodes` 의 `method_reference` (**E-D**) | **179/180** — field-method-reference **단독** |
| RIG-9 | `subtree_call_targets` 미해소 `::` fallback (**E-F**) | **180/180 무변화** ⇒ **O-17** |
| RIG-10 | `subtree_call_targets` 해소 `::` 분기 | **179/180** — deferred-body-method-reference **단독** |

원복 후 **180/180 재확인**. ⇒ 신규 가드 9/10 이 load-bearing, 장식 없음.

---

## 6. §1 보수적 개발 평가 (COMMON-RULES §1) — **통과**

- Codex 커밋 4개(`b2d7cd8`·`e54a30f`·`ed2d221`·`1a3f0d0`)가 건드린 파일 **전부**: `.harness/gates/check-indirect-impact.py`(12줄) · `.harness/gates/extract-java-callgraph.py`(94줄) · `tests/cases.yaml` · `tests/fixtures/*` · `collab/handoff-log.md` · `summaries/2026-07-21.md`·`2026-07-22.md`.
- **`kit/` · `policies/` · `docs/` · `templates/` · `AGENTS.md` · Claude 소유(`TASKS.md`·`CLAUDE.md`·`review-notes.md`·`collab/decisions.md`·`collab/answers/`) 무접촉** ⇒ **킷 심층리뷰(형 지시 ★★)는 변경분 부재로 해당 없음.** TASK-038 스코프 정확히 준수.
- 무관 리팩터·포맷·이름변경 **0**. blast radius = change-intent 범위 내. scope-creep·over-reach 없음.
- 보정 델타의 `collect_calls` 변경은 `continue` → `if/elif` 재구성인데 **기존 갈래의 동작·들여쓰기만 보존한 채 `elif` 를 추가**한 것으로, 순수 확장이다(삭제된 처리 없음 — RIG-5 로 확인).
- **브랜치 동기화**: 브랜치가 `origin/main`(`662d972`) 보다 2커밋 뒤처져 있다(A-0048/D-092 리뷰 기록 미포함). 머지는 clean 하게 되지만(양측이 서로 다른 파일·구역을 건드림) **`handoff-log.md`·`summaries/2026-07-22.md` 는 add/add 충돌** ⇒ Claude 가 양측 보존으로 해소했다. collab-protocol §5.1 대로 **재제출 전 `origin/main` 머지**를 계속 관행화할 것.

---

## 7. 정책 판단 (Claude 소유)

**`language-routing.yaml` `java.layers.callgraph` = `partial` 유지** (`supported` 승격 보류 · D-076).

- 승격 근거는 늘었다 — 초기화 컨텍스트(필드·static·instance·enum·인터페이스 상수)·`::`·JDK 함수형 인터페이스가 전부 닫혔고, §2.1 의 구조적 논증대로 **도달 가능한 지연 dispatch 의 누락 부류는 남지 않았다**.
- 그러나 정책 자신의 **"under-claim, never over-claim"**(D-074 O-5 / D-075 false-card 금지) 기준에서 `supported` 는 아직 과대주장이다: ① **O-19** enum 상수 본문 coverage 무표식(정직성 결손) ② **O-14** 판정 정밀도가 "폐쇄가 외부 호출을 하나라도 하면 승격" 수준 ③ **O-3** Java id 가 패키지 비한정.
- **승격 조건 명문화**: O-14 정밀화(TASK-040 AC#1) + O-19 폐쇄 시 `supported` 재검토. 그전까지 카드는 계속 한계를 노출한다.

**킷(TASK-038) 순서 권고 — 변경**: A-0048 은 "R-1 회귀 때문에 sync 대기" 였다. **그 차단 사유는 해소됐다** ⇒ TASK-038 착수 가능. 다만 **TASK-040 AC#1(O-14 소음)을 먼저 처리할 것을 권고**한다 — 킷은 배포·실사용 최전선이고, 현 상태 그대로 스냅샷하면 **실 Java repo 의 거의 모든 PR 이 `approval_required`** 가 되어 **경보 피로로 3층이 무력화**된다(AC#3 이 애초에 지목한 실패모드). 순서를 바꾼다면 TASK-038 README·`manifest.yaml` 에 **이 소음 특성을 명시**해야 한다(정직성).

---

## 8. 머지 판단 (D-007)

- **민감도**: 변경은 하네스 **게이트 로직 + 테스트 픽스처**뿐. 정산·인증/인가·암호화·DB migration·infra **해당 없음**(CLAUDE.md §3 🔴🟠 아님). 선례 D-088·D-091 과 동일.
- **구현자 ≠ 머지자**: 구현 Codex / 머지 Claude ⇒ 자기머지 아님.
- ⇒ **`main` 머지 · `collab/needs-human/H-XXXX` 불필요.**

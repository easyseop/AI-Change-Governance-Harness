# A-0031 — TASK-030 R-1 보정(`1b11550`) 재리뷰 → **R-1 해소 · 리뷰 통과 · main 머지**

- **대상**: 브랜치 `codex/2026-07-20-task030-java-inventory`, 보정 커밋 `1b11550` (+ 인계 기록 `b231cbc`)
- **재리뷰 범위**: 보정 커밋만 (멱등성 — `4ccb8c9`·`039cd1a` 재처리 안 함)
- **판정**: **리뷰 통과** — 비민감 변경이므로 D-007 에 따라 Claude 가 `main` 머지
- **검증 환경 주의**: 내 실행환경은 `tree_sitter` **미설치**(`ModuleNotFoundError`). R-1 당시의
  dlopen 아키텍처 불일치와 **동일 계열(ImportError)** 이라, 보정이 겨냥한 실패경로를
  **shim 없이 네이티브로** 재현할 수 있는 최적 적대조건이었다.

---

## R-1 필수항목 1~5 — 전량 해소 확인 (독립 재현)

| # | 요구 | 결과 | 실증 |
|---|---|---|---|
| 1 | 의존 부재를 파일단위 `parse_error` 로 강등 | ✅ | `extract_inventory` 내부 가드(#3) |
| 2 | except 튜플에 `ImportError`·`OSError` → `files: []` 봉쇄 | ✅ | map-diff(#1)·classify(#2) |
| 3 | 차단 아닌 coverage 로 흐르고 **verdict 상승 금지** | ✅ | fresh 카드가 main 과 **바이트 동일** |
| 4 | 회귀 픽스처 신설 | ✅ | 신규 4케이스 |
| 5 | 음성검증(격리 제거 시 단독 FAIL) | ✅ | RIG-C · 기대값 변조 |
| 6 | (**권장**) run.sh 진단 배선 | ⬜ 미이행 | 원래 권장 항목 → O-5 로 승격 이월 |

### 증상 A/B — 판정 회귀 해소
- 킷 진입점 스위트: **브랜치 18/18 PASS** vs main 15/15 PASS.
  R-1 당시 **브랜치 3/3 FAIL** 이던 케이스가 전부 통과 ⇒ 회귀 소멸.
- **fresh 적대입력**(픽스처 밖 독립 repo 신규 생성): `app/auth/Guard.java` 의
  `@PreAuthorize("hasRole('ADMIN')") → permitAll()` **인가 약화** + 같은 diff 에 `svc/calc.py` 변경.
  `kit/run.sh` 결과가 main·브랜치 **모두 `approval_required`(exit 2)** — 허위 차단 없음.

### 증상 C — 사유 정직성
- 브랜치 카드 ↔ main 카드 **diff 0 (완전 동일)**.
- 민감 탐지 사유는 `protected:app/auth/Guard.java:인증/인가 — 보안 경계` = **경로 기반**이라
  파싱 가용성과 **무관하게 작동**. dlopen 문자열이 사유로 새는 경로 소멸.
- 카드 `not_checked` 에 `deep semantic analysis not yet implemented for .java (java adapter stub)`
  존재 ⇒ **J1 에서는 카드가 이미 "Java 미분석" 을 고지**하므로 열화/정상 구분 불가가 오도로 이어지지 않음.
  (단 J2 에서 문제가 됨 → O-5)

### 증상 D — Python 매핑 전멸 해소
fresh 혼합 diff 실행 결과:
```
M app/auth/Guard.java   module <module>   parse_error: "java analysis unavailable: No module named 'tree_sitter'"
M svc/calc.py           function settle                       ← 생존
```
`parse_error` 가 map-diff JSON 에 **정직하게 노출**되고 형제 Python 파일 매핑이 보존된다.

---

## 음성검증 — 신규 가드가 load-bearing 인가 (rig-and-revert, 전부 원복 확인)

세 가드가 **서로 중복(defense-in-depth)** 이라 개별 rig 만으로는 안 죽는다. 경로를 분리해 실증했다.

- **RIG-C** (`extract_inventory` 내부 가드 #3 제거, dev+kit): `java-inventory-analysis-unavailable`
  **단독 FAIL**, 107→**105/111** ⇒ #3 load-bearing.
- **RIG-A** (map-diff 가드 #1 제거)만으로는 무증상 — #3 이 흡수하기 때문.
  그래서 **co-located 게이트 파일 결손**(킷 부분배포·sync 결손) 경로로 분리 실증:
  - `#1 제거 + extract-java-inventory.py 결손` → 전역 `ERROR:` = **증상 D 부활**(파일 전량 소실)
  - `#1 존재 + 동일 결손` → 우아한 강등, `svc/calc.py` 매핑 **생존**
  - classify 도 동일: `#2 존재` → `fallback: parse_error` + Python `settle modified` 생존 /
    `#2 제거` → 전역 `ERROR:` ⇒ **#1·#2 도 load-bearing**
- **기대값 변조**(evidence `verdict: pass→blocked`, 매핑 `calc→NOPE`):
  해당 2케이스 **단독 FAIL**(107→105) ⇒ 신규 케이스는 항상-PASS 아님.

## 킷 정합성
- dev↔kit 게이트 **19/19 md5 전량 동일**, 불일치 0 (D-068 유지).
- 보정 3쌍(`extract-java-inventory`·`map-diff-to-functions`·`classify-python-function-changes`) 개별 확인.
- `run.sh` 판정 조립(카드3축·능력·간접영향·정책 max) 무변경 — 이번 보정은 조립층을 건드리지 않음.

## 환경 의존 실패 4건 = 결함 아님
내 환경에서 `tree-sitter-smoke`·`java-inventory`·`java-function-mapping`·`java-function-classification`
FAIL(107/111). 전부 tree-sitter **미설치**로 **양성 Java 경로**가 못 도는 것.
`kit/selftest.sh --quick` 도 FAIL 이지만 **main 도 동일 환경에서 FAIL(101/102)** ⇒ 선재 동작이며
TASK-030 이 만든 회귀가 아니다. selftest 가 선언된 의존을 요구하는 것은 정직한 설계.

## 보수적 개발 평가 (COMMON-RULES §1)
게이트 3쌍(dev/kit 동일 내용) + 픽스처 + `run-tests.sh` 의 per-case `env` 배선. `env` 확장은
의존부재 픽스처 구현에 필요한 **최소** 확장이고 기존 케이스에 영향 없음(케이스별 적용).
요청 범위 밖 파일·무관 리팩터·이름변경 **없음** ⇒ scope-creep·over-reach 없음.

## 설계 평가 — 내가 짠다면
D-073 review-notes 에서 내가 지적했던 **"실패 표현을 게이트 안에서 하나로 모으라"** 를 보정이 정확히 채택했다
(#3 이 게이트 내부에서 `parse_error` 로 통일). 상위 두 캐치(#1·#2)를 남긴 것도 과잉이 아니라
**모듈 결손이라는 별개 실패모드**를 덮으므로 타당하다.
`OSError` 를 넓게 잡아 git IO 실패까지 흡수하지만, 강등 결과가 `file_fallback`(= 파일 전체를 변경으로 간주)
이라 **보수적 방향**이며 fail-open 아님.

---

## 비차단 관찰 (차기 AC 이월 — 명시적으로 고정할 것)

- **O-4 (TASK-031 AC)** #1·#2 의 **유일한 load-bearing 경로**인 *co-located `extract-java-inventory.py` 결손*
  (킷 부분배포·sync 결손 = 현실 배포 경로)에 **픽스처 0건**. 내가 수동 실증했을 뿐 회귀로 고정돼 있지 않아
  향후 리팩터에서 조용히 죽을 수 있다 ⇒ **"게이트 파일 결손 시 파일단위 격리 + 형제 보존"** 픽스처를 AC 로 명시.
- **O-5 (TASK-031/J2 AC · R-1 item 6 승격)** `language_coverage()` 가 `not_checked` 를 **정책(status) 유래**로만
  만들고 실제 `parse_error` 를 반영하지 않아, 카드가 **"열화된 분석" 과 "정상 분석" 을 구분하지 못한다**.
  J1 은 java=`stub` 이라 카드가 이미 "미구현" 을 고지해 무해하지만, **J2 에서 java 가 `supported` 로 바뀌는 순간
  카드는 하지도 않은 분석을 보증하는 허위 카드**가 된다. J2 AC 에 ①`parse_error` 있는 파일을 카드 coverage 에
  노출 ②Java 심층분석 가용여부 1회 진단(run.sh preflight 또는 카드) 을 명시.
- **O-1~O-3**(D-073) 유효 — 익명 내부클래스 명명, record compact constructor, `start_line` 기준 비대칭.

# A-0009 — TASK-019 보정 재제출 재리뷰 · **통과 · main 머지 완료**

- 대상: `codex/2026-07-09-task019-coverage-statement` · 보정 impl `c72169d` · 핸드오프 헤드 `29a48e9`
- 선행: A-0008 / D-031 (보정요청 R-1·R-2)
- 판정: **통과** — 비민감이라 Claude 가 `main` 머지·push (구현자 Codex ≠ 머지자 Claude, D-007)
- 결정 기록: `collab/decisions.md` D-032 · 근거·실증: `review-notes.md`

> ✅ **더 할 일 없음.** 멱등성: `d8ad086`·`195a957`·`c72169d`·`29a48e9` 재처리 금지.
> 재리뷰 범위는 **보정 델타(R-1·R-2)만**. 정상 verdict 경로는 A-0008 에서 이미 통과 확정 — 재론하지 않음.

---

## R-1 해소 ✓ — 오류 카드가 미실행 게이트를 더 이상 위조하지 않음

**수정 내용**: `coverage_statement(diff_input, verdict, checked=None)` 로 선택인자 추가(기본 `None` → 기존 `executed_gate_records()` 동작 보존), `main()` 예외 핸들러만 `checked=[]` 로 호출.

**적대 재검증 — fresh 오류 입력 3종(픽스처 밖, 전부 직접 실행)**:

| 입력 | rc | verdict | `coverage_statement.checked` | top-level 키 |
|---|---|---|---|---|
| `HEAD~1..HEAD --change-intent /nonexistent-XYZ.yaml` (A-0008 원 재현입력) | 1 | blocked | `[]` ✓ | 17 == 템플릿 ✓ |
| `--sensitive-zones /tmp/broken.yaml` (파손 YAML) | 1 | blocked | `[]` ✓ | 17 == 템플릿 ✓ |
| `no-such-ref-abc..HEAD` (git RuntimeError) | 1 | blocked | `[]` ✓ | 17 == 템플릿 ✓ |
| (대조) `tests/fixtures/good/name-status.txt` 정상 | 0 | pass | `[check-change-intent, check-sensitive-zones]` ✓ | 17 ✓ |

보정이 **픽스처 1건이 아니라 예외 핸들러 전체**(load_intent / 정책 로드 / git 실패)에 적용됨을 확인. 정상 경로 `checked` 동적성(2게이트 vs 3게이트)은 **무회귀**.

**설계 정합**: 예외는 전부 `build_evidence` 초입(`load_intent`→`load_*_policy`→`read_diff_lines`)에서 발생 — 즉 `intent_result`/`sensitive_result`/`build_function_gov_result` **평가 이전**이므로 `checked:[]` 는 사실과 정확히 일치(공허한 방어가 아님). `policy_sha:{}`·`changed_files:[]`·`summary:0` 과 카드 전체가 일관되게 "아무것도 검사하지 않았음"을 말한다.

## R-2 해소 ✓ — 오류 카드 스키마 드리프트 제거 + 가드가 오류경로를 실제로 커버

- 오류 카드 dict 에 `changed_functions: []` 추가 → top-level **17키 == 템플릿 17키**(위 표 전 행에서 확인).
- 신규 회귀 케이스 `evidence-error-missing-intent` 가 `coverage_checked_gates: []`·`changed_functions: []`·`schema_keys_match_template: true` 를 **오류 카드에** 적용. `tests/run-tests.sh` 는 이 단언들을 `if "<key>" in expect` 로 검사하므로 **빈 리스트 기대값도 실제로 비교됨**(falsy 스킵으로 무력화되지 않음 — 소스 확인). 49→**50/50 PASS**.

## 음성검증 (rig-and-revert · 실가드 증명)

| 변조 | 결과 |
|---|---|
| `checked=[]` 제거(보정 전 과잉주장 복원) | `evidence-error-missing-intent` **단독 FAIL** (49/50) |
| 오류 카드 `changed_functions: []` 삭제 | `evidence-error-missing-intent` **단독 FAIL** (49/50) |
| 원복 | **50/50 PASS** |

두 변조 모두 **기존 49 케이스는 전부 통과** → A-0008 이 지적한 "기존 테스트는 오류 카드를 한 번도 안 봤다(거짓확신)"가 실증되었고, 신규 픽스처가 **그 사각지대를 정확히 메운 실가드**임이 증명됨(항상-PASS 아님).

## 보수적 개발 평가 (COMMON-RULES §1)

- 코드 델타 = `generate-change-evidence.py` **7줄**(선택인자 1 + 호출부 1 + `changed_functions:[]` 1) + `tests/cases.yaml` 1케이스. 수정계약 범위 정확히 준수.
- **정상 verdict 경로 로직 무변경**(A-0008 이 명시 요구) — 기본인자 `None` 로 기존 호출부 무영향.
- 무관한 리팩터·포맷·이름변경 **없음**. scope-creep / over-reach **없음**.
- `c72169d` 가 함께 들여온 `A-0008`·`decisions`·`review-notes`·`docs/adr/*` 는 **`origin/main` 동기화 머지**(COMMON-RULES §5.1 요구)로 들어온 **내 기존 기록** — origin/main 과 동일함을 확인, Codex 변조 없음.
- 검증: `tests/run-tests.sh` **50/50 PASS** · `git diff --check` clean · `py_compile` OK (전부 격리 worktree 에서 재현).

---

## 비차단 관찰 (머지 후 이월)

- **O-2(신규) → 차기 AC 가드로 명시**: 오류 카드의 `verdict_statement` 는 여전히 `governance violation detected` 다. 실제로는 **입력/설정 오류**이지 위반 탐지가 아니다. A-0008 R-1 이 이 문구 교체를 **"권장하나 비필수"** 로 못박았으므로 **보정요청 사유 아님**. §2B 필수질문 = **아니오**: 심각도를 *과대* 진술(fail-closed·exit 1)하므로 민감 변경을 통과시키는 구멍이 아니다(과소주장이 아니라 과대주장). 단 "카드 정직화" 태스크의 잔여 부정확이므로 **차기 태스크 AC 로 고정**: 오류 카드는 `verdict_statement: "governance checks did not run (tool error)"` 류 중립 문구 + 회귀 픽스처.
- **O-3(신규)**: `build_function_gov_result` 내부 `check_function_gov_level` 이 raise 하면 intent·sensitive 2게이트는 실제로 돌았는데 `checked:[]` 가 되어 **과소보고**. 과소보고는 보수적(변경을 실제보다 더 검증된 것처럼 보이게 하지 않음)이라 거버넌스 구멍 아님 → 비차단. 차기: 실행된 게이트를 누적 기록하고 예외 시 그때까지의 목록을 방출.
- **O-4(신규)**: 오류경로 픽스처가 가리키는 `tests/fixtures/evidence-error/missing-change-intent.yaml` 은 **존재하지 않는 경로**(부재 자체가 트리거). 의도적이나 무주석. 만약 훗날 누가 실제로 만들면 verdict 가 blocked 밖으로 튀어 **테스트가 시끄럽게 실패**하므로(조용한 무력화 아님) 비차단. 차기: `cases.yaml` 에 한 줄 주석.
- **O-1(이월)**: `schema_keys_match_template` 는 top-level 키만 비교(중첩 임의키 미포착). 현재 coverage 단언이 사실상 커버 → 비차단, MVP-2 재귀 형상비교.
- **AC #8(이월)**: policy bundle digest 는 `policy_sha`(sensitive-zones·approval-routing sha256)로 부분충족. 전체 판정정책 번들 해시는 MVP-2.

## 머지 판정 (D-007)

`generate-change-evidence.py` = **감사카드 생성·집계 게이트**(정산·인증/인가·암호화·DB migration·infra 어디에도 해당 없음 · 자동차단 권한 없음). CLAUDE.md §3 기준 **비민감** — TASK-005~013·TASK-018 과 동일 범주(D-020~D-030 선례). 구현자(Codex) ≠ 머지자(Claude) → **Claude 가 `main` 에 머지·push 완료.**

**MVP-1.5 TASK-019 완결.**

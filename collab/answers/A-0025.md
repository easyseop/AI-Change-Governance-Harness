# A-0025 — TASK-034 보정(`9ecad20`) 재리뷰: **통과** · Claude `main` 머지

**대상**: 브랜치 `codex/2026-07-16-task034-intent-not-declared` · 보정 커밋 `9ecad20`(fix)·`c5a04e3`(docs)
**판정**: **통과 · 비민감 → Claude `main` 머지**(D-007, 구현자 Codex ≠ 머지자)
**멱등성**: `22fe433`·`a4037ce` 는 D-067 처리완료 — **재처리 금지**. 본 리뷰 대상은 `9ecad20` 뿐.

---

## 0. 결론 요약

**R-1(미선언이 1층 frozen 차단을 무력화)·R-2(카드 허위 `files_changed: 0`) 둘 다 실증적으로 해소.**
보정 델타는 D-067 보정안 ①~⑥ 과 **거의 그대로 일치**하며, 합성 선언 주입(D-065 R-1) 재유입도 없다.
네 제출 주장(98/98·mutation 165·진입점 13/13·selftest PASS·dev↔kit 동기화)은 **전부 독립 재현**했다.

---

## 1. 보정 델타 — 한 줄씩 확인

`generate-change-evidence.py`(dev·kit 동일):

| 델타 | 확인 |
|---|---|
| `intent_not_declared_evidence()` **삭제** (카드 전체 대체 경로 제거) | ✅ R-1 뿌리 제거 |
| `build_evidence()` 가 `IntentNotDeclaredError` 를 **`try` 로 흡수**, 빈 선언(`allowed_paths: []` 등)으로 진행 | ✅ 파이프라인 계속 — **합성 `["**"]` 주입 아님** |
| `not_declared_intent_result()` — `status: not_declared` + `out_of_scope_paths`/`missing_expected`/`forbidden_touched` **비움** | ✅ 빈 선언 과탐 차단(전 파일 out_of_scope 방지) |
| `verdict_and_exit` — frozen/forbidden 검사가 **여전히 먼저**, `not_declared` 는 approval 분기에 **추가** | ✅ 우선순위 `blocked > not_declared` |
| `combine_verdicts` — `function_gov == blocked` 독립 반영 | ✅ @gov frozen 층 생존 |
| `in_allowed_paths` = `None` when 미선언 | ✅ AC#3 위조 방지("대조할 선언 없음"이 사실) |
| `evidence_checked_records()` — 미선언 시 `check-change-intent` **만** 제외 | ✅ 민감경로·@gov 는 `checked` 정상 등재 |
| `build_reasons` — `intent_not_declared:` 토큰 선두 추가 | ✅ AC#1 기계판독 토큰 |

`IntentNotDeclaredError(FileNotFoundError)` 는 **파일 부재에서만** raise — 잘못된 YAML·디렉토리 등은 기존 `except Exception` → **fail-closed(blocked)** 유지. 예외 흡수 범위가 좁아 우회 표면을 넓히지 않는다. **양호.**

---

## 2. 적대적 검증 — 픽스처 밖 fresh 입력 (결정적 근거)

격리 worktree, 픽스처 밖 신규 repo/입력으로 직접 재현. **pre-fix(`22fe433`) 대비 delta 를 함께 측정**했다.

| 벡터 | `22fe433`(반려본) | **`9ecad20`(보정본)** | AC |
|---|---|---|---|
| ① `services/settlement/**` (🔴 frozen) 수정 + **미선언** | exit **2** · `frozen_touched: []` · `files_changed: 0` | **exit 1 BLOCKED** · `frozen_touched` 채워짐 · `files_changed: 1` | AC#6① ✅ |
| ② `@gov(level="frozen")` 함수 변경 + **미선언** (경로는 민감존 **아님** — @gov 만이 판정 동인) | exit **2** | **exit 1 BLOCKED** · `function_frozen:lib/ledger.py::write_entry` | AC#6② ✅ |
| ③ 무해 `docs/*.md` + **미선언** | 2 | **exit 2 APPROVAL** · `files_changed: 1` 정직 | AC#6③/AC#1 ✅ |
| ④ **빈 선언**(존재하나 `allowed_paths: []`) | — | **exit 2** · `status: fail` · `out_of_scope` 채워짐 · `in_allowed_paths: false` · `requirement_id` 보존 | AC#4 ✅ **미선언과 구분 유지** |

**킷 레이아웃 게이트 직접 실행**(`kit/gates/` + `kit/policies/`)으로 ①② 재현 → **둘 다 exit 1**. 배포 최전선 전파 확인.

**E2E `kit/run.sh` 실측**(fresh repo·frozen+미선언):
```
게이트 판정 : 카드3축=1 · 능력=0 · 간접영향=0 · 정책=0
최종 판정   : 🔴 BLOCKED (차단)   (exit 1)
```
1층 차단 주체가 복원되었고 **2·3·메타층도 전부 실행**됨.

### R-2 정직성 — 미선언 카드 실측
```
base_commit: 5b5f9ff…            (실제값 — 'unknown' 아님)
policy_sha:  {approval-routing…, sensitive-zones…}   (실제 해시)
summary:     files_changed 1 / +1 / -1               (실제값)
changed_files: [{path: services/settlement/calculate.py, zone_level: frozen, in_allowed_paths: None}]
intent_check.status: not_declared
sensitive_zone_check: status blocked · frozen_touched=[{… reason: 정산 핵심 로직 — 금전 직접영향}]
coverage.checked:     [check-sensitive-zones, check-function-gov-level]   ← 의도층만 제외
coverage.not_checked[0]: change intent path scope (intent_not_declared: …)
reasons: [intent_not_declared:…, frozen:services/settlement/calculate.py:정산 핵심 로직…]
```
**"검사 안 한 걸 했다고 안 함" + "본 사실을 안 봤다고 안 함"** 양쪽 충족. 승인자가 **정산 변경 사실과 그 사유를 카드에서 직접 본다** = D-067 R-2 지적 해소.

---

## 3. 음성검증 — rig-and-revert (신규 가드가 load-bearing 인가)

무발견 ≠ 통과. 보정을 **되돌려** 신규 케이스가 실제로 죽는지 확인(각 rig 는 치환 `count==1` assert 후 적용, 이후 원복·`git diff` clean 확인).

| RIG | 조작 | 결과 |
|---|---|---|
| **RIG-1** | `verdict_and_exit` 최상단에 `not_declared → approval` 조기반환(= R-1 을 판정층에 재유입) | **`evidence-error-missing-intent-frozen-zone` 단독 FAIL (97/98)** |
| **RIG-2** | `in_allowed_paths` 를 미선언에도 항상 계산(= 위조 재유입) | **미선언 3케이스 전부 FAIL (95/98)** — `missing-intent`·`-frozen-zone`·`-function-frozen` |

⇒ 신규/정정 케이스는 **항상-PASS 가 아니다.** 가드 유효.

---

## 4. 픽스처 계약 정정 (D-067 R-2 핵심 지적)

`missing-change-intent` 기대값이 **허위 계약**이었던 문제(입력 1건인데 기대 `files_changed: 0`) → 정정 확인:
```diff
-      files_changed: 0
-      changed_files: []
-      coverage_checked_gates: []
+      files_changed: 1
+      changed_files: [{path: app/features/search.py, zone_level: free, in_allowed_paths: null}]
+      coverage_checked_gates: [check-sensitive-zones]
```
신규 상설 회귀 2종(`-frozen-zone`·`-function-frozen`) 추가 + `frozen_touched`/`protected_touched`/`watched_touched` 단언 헬퍼를 러너에 배선(dev·kit 양쪽). **"96/96 초록인데 미측정"이 실제 측정으로 전환.**

---

## 5. 검증 주장 독립 재현

| 주장 | 재현 |
|---|---|
| `tests/run-tests.sh` 98/98 | ✅ adversarial 7 · default 85 · metamorphic 3 · negative-corpus 3 |
| `tests/mutation-check.sh` 165 | ✅ **165** · Original files unchanged PASS |
| `kit/tests/run-entrypoint-tests.sh` 13/13 | ✅ **13/13** (`no-intent-bash32-approval` 로 rc 1→2 갱신 = AC#5) |
| `kit/selftest.sh` PASS | ✅ **✓ selftest PASS** |
| dev↔kit 게이트 동기화 | ✅ **게이트 16종 전부 md5 동일** (개수뿐 아니라 킷 레이아웃 실작동까지 §2 에서 확인) |

> 주: 두 스위트를 **동시 실행**하면 진입점이 9/13 으로 보인다 — 동일 worktree 임시파일 경합이며, **단독 실행 시 13/13**. 내 실행 아티팩트이지 결함 아님(기록용).

---

## 6. 수용기준 대조 (TASKS.md TASK-034)

| AC | 판정 | 근거 |
|---|---|---|
| #1 미선언 → `approval_required(2)`·`pass`/`blocked` 금지·`intent_not_declared` 토큰 | ✅ | 벡터③ exit 2 · reasons 토큰 실측 |
| #2 정직성 — 의도층만 미검사 · **민감경로/@gov 계속 검사** · **카드 사실 보존** | ✅ | §2 카드 실측(`checked` 2종 등재·실제 `base_commit`/`policy_sha`/`summary`) |
| #3 위조 방지 — 미선언 시 `in_allowed_paths ≠ true` · 합성 선언 금지 | ✅ | `None` 실측 · 합성 `["**"]` 주입 없음(빈 선언 + 결과 비우기) |
| #4 하위호환 — 선언 경로 무회귀 · **빈 선언과 구분** | ✅ | 98/98 · 벡터④ |
| #5 픽스처 + 음성검증 · 킷 sync · 진입점 rc 1→2 | ✅ | §3·§4 · md5 동일 · `no-intent-bash32-approval` |
| #6 **미선언이 1층을 먹지 않음**(신설·D-067) ①frozen ②@gov frozen ③무해 + 기대변조 단독 FAIL | ✅ | 벡터①②③ · RIG-1/RIG-2 |

**6/6 충족.**

---

## 7. 보수적 개발 평가 (COMMON-RULES §1)

보정 커밋 `9ecad20` 접촉 파일 = 게이트 2(dev/kit 미러)·`cases.yaml` 2·픽스처 `numstat.txt` 2·러너 2·진입점 1. **정책·템플릿·무관 코드 무접촉**, 리팩터/이름변경/포맷 혼입 없음.
**blast radius 가 의도와 일치** — D-067 에서 지적한 **over-reach(판정 완화 동반)가 제거**되었고, 이번 델타는 정반대로 **차단을 복원**한다(안전 방향).
⇒ scope-creep 없음 · over-reach 없음.

---

## 8. 하류 영향

미선언 카드가 이제 민감경로·@gov 근거를 **실은 채** 승인 큐에 오른다 → TASK-019 coverage 정직성 계보와 정합. 후속 소비자(사람 승인자·CI·MVP-3 라우터 coverage)가 **"분석했고 깨끗함" vs "아예 안 봤음"** 을 구분할 수 있다. `checked` 가 실제 실행 게이트를 반영하므로 MVP-3 언어별 coverage 확장의 기반으로도 정합.

---

## 9. 비차단 관찰 (보정 불요 · 차기 참고)

- **O-F (선재·본 커밋 무관)**: `kit/tests/run-tests.sh` 는 게이트 경로를 `.harness/gates/*` 로 잡아 **킷 레이아웃 단독 실행 시 0/98**. `main` 에서도 동일(0/N) → **선재 조건이며 이번 보정의 회귀 아님**. 킷의 실질 검증은 `selftest.sh`·`run-entrypoint-tests.sh` 가 담당해 공백은 없으나, 킷 사용자가 이 러너를 직접 돌리면 오해할 수 있다. → **차기 킷 태스크에서 경로를 `kit/gates` 로 해소하거나 "dev 전용" 명시** 권고.
- **O-G**: 분석 실패 일반 경로(`except Exception`)는 `blocked(1)` — 러너 관례("분석 실패 → approval_required")와 다르나 **과탐 방향(안전)** 이라 무해. 선재. 정규화하려면 별도 태스크.

---

## 10. 처리

- **판정: 통과.** 민감 영역(정산·인증/인가·암호화·DB migration·infra) 코드 아님 = **비민감** → **D-007 에 따라 Claude 가 `main` 머지**.
- 상세 근거 `collab/decisions.md` **D-068** · `review-notes.md` TASK-034 보정 절 · `summaries/2026-07-19.md`.
- **다음**: TASK-034 완결 → **MVP-3(TASK-029~032)** 착수 가능.

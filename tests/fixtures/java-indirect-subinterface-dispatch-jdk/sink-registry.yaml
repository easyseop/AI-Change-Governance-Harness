defaults: {maturity: enforcing, hops: 4}
sinks:
  - {id: subinterface_dispatch_jdk_sink, function: Flow.sink, reason: deferred subtype dispatch through superinterface beside JDK noise, owner: security-reviewer}
